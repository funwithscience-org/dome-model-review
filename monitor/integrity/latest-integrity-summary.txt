INTEGRITY REPORT — 2026-07-14T01:16Z — STATUS: WARN

CLEAN (pass): build reproducibility, internal anchors, relative hrefs, tab structure/nav
chain, data-prose consistency, WIN detail consistency, WIN-dome alignment (3 known
collisions 057/058/062 reconfirmed, no new drift), progressive disclosure, hardcoded
colors, discoverability infra, expansion-tracker continuity, ISS collision audit,
broken-review parse check, digest coverage, FUSE-prompt staleness, section-new queue
coupling, workspace-sync skip log, mech-A-bypass audit, pages freshness (FRESH),
delete-propagation backlog (71/200).

SKIPPED: external links (weekly cadence — next run Monday 2026-07-20).

NEW FINDINGS (untracked):
1. MAJOR — 2 workspace-only files not yet in git: monitor/curmudgeon/reviews/
   REACTIVE-AUDIT-2026-07-14.json and monitor/analyst/expansions/EXP-664-pred-field-
   2026q2-fresh-pass-verify.json. Both are today's fresh output (~00:38Z and ~01:06Z).
   Confirmed absent via GitHub API. Commit from a fresh clone; check whether this
   morning's curmudgeon/analyst push failed.
2. MODERATE — CLAUDE.md's Agent-Specific References table lists only 5 of 28 reference
   files under monitor/prompts/reference/. Either expand the table or note it's
   illustrative-only.
3. MODERATE — status.json field regression (PROP-130 canary) on commit 19b70279
   (workspace-sync 2026-07-13T04:09Z): last_poll and consecutive_quiet_polls both
   reverted. chimera=false — plain regression, not full two-writer chimera. Watch for
   poller self-heal within 24h.

CARRIED (already tracked, informational):
- ISS-2975 (svs.gsfc.nasa.gov expired cert), ISS-2932 (chime-frb.ca 503) — both from
  last Monday's external-link check, unresolved.
- ISS-2961 (WIN-062 dome-site duplicate) — reconfirmed unchanged today.
- ISS-2977 (digest-reviews under-coverage advisory) — unchanged.

No critical findings. Build matches source data exactly (only a same-day build-date
stamp diff, reverted, not committed).
