INTEGRITY REPORT — 2026-07-16T01:16:00Z — OVERALL: FAIL

MAJOR (1):
- Workspace-only files: monitor/curmudgeon/reviews/REACTIVE-AUDIT-2026-07-16.json (written
  01:10:13Z) and monitor/analyst/expansions/EXP-669-context-page-narrative-read.json (written
  00:52:12Z) exist on FUSE but are not yet in git. Both postdate the 00:03Z workspace-sync run.
  Likely resolves at the next ~04:03Z sync cycle — matches the identical 2026-07-15 pattern
  (REACTIVE-AUDIT-2026-07-15.json), which cleared normally. Recheck if still workspace-only
  after 2 cycles.

MINOR (2):
- digest-reviews.js under-coverage advisory: 259 "processed" reviews missing open-issues.json
  entries (1210 total missing issues), up slightly from 256/1205 on the 2026-06-22 comparable
  snapshot. Not a parse error — long-tracked and repeatedly closed (ISS-279/281/282 lineage) as
  "normal operational state for a pipeline of this size."
- NEW: data/wins.json.bak is git-tracked but not part of CLAUDE.md's file-ownership model
  (git-owned/workspace-owned/append-only), is unreferenced by build.js or any build-scripts/
  monitor script, and is stale by 5 WIN entries (67 vs the live 72 in data/wins.json). Orphaned
  backup artifact with no owner or purge mechanism — recommend delete or document+prune.

PASS (everything else):
- node test.js: 3837 passed, 0 failed.
- Mechanical precompute: 10/10 checks pass (internal anchors, relative hrefs, data-prose
  consistency, WIN detail consistency, progressive disclosure, hardcoded colors, discoverability,
  expansion tracker continuity, ISS collision audit, broken reviews).
- Tab structure: 12 tabs, valid nav chain, no empty tabs, clean heading hierarchy. (Note: site
  uses class "ds-tab-content", not the generically-named "tab-content" from the check prompt.)
- WIN-dome alignment: matches live dome site exactly (69 WINs, 8 PROS, 3 known/documented
  collisions — WIN-057, WIN-058, WIN-062 — no new drift, no new WIN numbers, no title/claim drift).
- External links: skipped (weekly cadence, not Monday). Last run 2026-07-13 found 2 known broken
  links, both now tracked: ISS-2932 (chime-frb.ca 503) and ISS-2975 (svs.gsfc.nasa.gov cert expiry).
- FUSE prompt staleness: 0 stale prompts. Section-new/queue coupling: clean.
- Workspace-sync skip log: 0 direction violations (59 normal mtime-guard skips).
- Digest coverage: 0 unprocessed. Mech-A-bypass audit: 0 flagged.
- Project docs: schedule table matches all 14 scheduled tasks (12 enabled/2 disabled); no
  hardcoded counts; all spot-checked file paths exist.
- Build reproducibility: PASS — only a cosmetic build-date-stamp diff (today's date), no content
  or data drift.
- Pages freshness: FRESH (live site signature matches git HEAD exactly). Delete-propagation
  backlog: 88/200, clean. status.json provenance canary: clean (4 versions checked, 0 regressions).
- Curmudgeon dispatcher-state + drift-audit precomputes both refreshed (were past their staleness
  gates: 24h / 96h respectively) — both clean (drift-audit: 1 tldr_only candidate; dispatcher-state:
  recommended_priority=audit).

Full report: monitor/integrity/report-2026-07-16T01-16.json
