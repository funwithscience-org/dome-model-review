INTEGRITY REPORT — 2026-07-31T01:15:27Z — OVERALL: WARN

MODERATE (1):
- delete-propagation-backlog canary fired again: 250 FUSE delete-propagation candidates
  (threshold 200), category_total=421. Trend since first fire 2026-07-27: 204 -> 229 -> 239
  -> 250, ~10-11/day growth. Already tracked under ISS-3018 (pending-human). Operator drain
  recommended (proven procedure, prior drains 462/1,137 files); not auto-executed — deny-unlink
  FUSE default is load-bearing disaster-recovery per PROP-051/PROP-125. Headroom to the
  abort_abs=500 delete-sanity gate: 250 candidates.

INFO:
- 1 workspace-only expansion file (EXP-676-mode6-win007-PROS002-explicit-arrow.json), FUSE
  mtime ~26 min old — well inside the 9h pending-rescue grace window. Not a finding.
- WIN-070 in our data corresponds to the dome's home-page "Forensic Zoom" exhibit, not a
  wins.html-listed WIN-070 label — resolved as expected, not a renumbering signal.

PASS (everything else):
- node test.js: 3838 passed, 0 failed.
- Mechanical precompute: 10/10 checks pass (internal anchors, relative hrefs, data-prose
  consistency, WIN detail consistency, progressive disclosure, hardcoded colors, discoverability,
  expansion tracker continuity, ISS collision audit, broken reviews).
- Tab structure: 12 tabs (ds-tab-content), valid nav chain, no empty tabs, clean heading
  hierarchy, exactly one active tab on load (overview).
- WIN-dome alignment: all 69 dome-site WIN numbers covered by our 72-entry wins.json (70 base +
  057b/058b); 0 missing, 0 genuine collisions, 0 title/claim drift.
- External links: skipped (weekly cadence, today is Friday). Last run 2026-07-27: 98 links, 0
  broken.
- FUSE prompt staleness: 0 stale prompts. Section-new/queue coupling: 0/0, clean.
- Workspace-sync skip log: 0 direction violations (33 normal mtime-guard skips in 24h).
- Digest coverage: 1 unprocessed (normal lag). Mech-A-bypass false-closure audit: 0 flagged.
- Project docs: schedule table matches all 14 scheduled tasks (11 enabled / 3 disabled) exactly;
  no hardcoded counts; all 8 spot-checked file paths exist. 21 reference/*.md files not
  individually named in the Agent-Specific-References table is a pre-existing, self-disclaimed
  design choice (ISS-2980) — not a new gap.
- Build reproducibility: PASS — only the cosmetic build-date-stamp line differed (today's date),
  no content or data drift.
- Pages freshness: FRESH (live site signature matches git HEAD exactly).
- status.json provenance canary: clean (6 versions checked, 0 regressions).
- Ownership-registration-gap lint: clean (113 candidates scanned across 50 commits, 0 gaps).
- Curmudgeon dispatcher-state + drift-audit precomputes both refreshed (were past their
  staleness gates: 24.1h / 96.0h respectively) — both clean (drift-audit: 2 large_rewrite
  candidates; dispatcher-state: recommended_priority=3).

Full report: monitor/integrity/report-2026-07-31T01-15.json
