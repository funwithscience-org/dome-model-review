Dome Structure & Integrity Report — 2026-07-07T01:15Z
Overall status: WARN (no criticals)

PASS: node test.js (3837/3837), all 10 mechanical precompute checks, tab structure/nav
chain (12 tabs, continuous, overview active by default), internal anchors, relative-href
resolution, data-prose consistency, WIN detail consistency, WIN/dome alignment (69/69
dome-confirmed WINs covered, titles match, WIN-070 home-page provenance already known),
progressive disclosure, hardcoded-color scan, discoverability files, expansion-tracker
continuity, ISS-collision audit, FUSE-vs-git prompt staleness (44/44 match), section-new
queue coupling, workspace-sync skip log (mtime-guard only), digest coverage, broken-review
scan (0 parse errors), drift-audit precompute (fresh, 48h old), curmudgeon-dispatcher-state
precompute (refreshed, was 24h old), mech-A-bypass audit (0 candidates), project-docs
schedule cross-check vs live scheduled-task list (all 12 enabled dome-* crons match),
build reproducibility (only diff vs HEAD is the auto-generated build-date footer), and
live-pages freshness (FRESH vs git HEAD).

External-link check SKIPPED (weekly cadence, runs Mondays; today is Tuesday).

WARN (1 major — none critical):
1. [major] monitor/analyst/expansions/EXP-647-win038-win029-citation-audit.json is
   workspace-only (not yet in git). Written 01:45 local time, after the last
   workspace-sync cycle (00:04Z UTC) and before the next (04:00Z UTC) — reads as
   normal quiet-period sync lag. Tracker status is 'pending' (low priority, citation
   audit for WIN-038/WIN-029 Sentman-year + Collin cite). Re-check tomorrow if still
   workspace-only after the next 1-2 sync cycles.

INFORMATIONAL:
- Dome site's own PROS-003 prospective-claim block carries a stale 2026-03-15 note
  claiming WIN-010/WIN-025 were "removed from confirmed wins," contradicting the
  dome's current wins.html (both still listed, titles unchanged). Dome-side
  self-contradiction, not a defect in our review data — candidate for the analyst's
  self-contradiction tracking.
