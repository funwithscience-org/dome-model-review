DOME INTEGRITY — 2026-08-13T05:39:50Z — STATUS: PASS

All PROP-111 mechanical precompute checks (10/10) passed: internal anchors,
relative hrefs, data-prose consistency, WIN detail consistency, progressive
disclosure, hardcoded colors, discoverability infra, expansion tracker,
ISS collision audit, broken review files. node test.js: 3837/3837 passed.

Tab structure verified by hand: 12 tabs, nav chain continuous
(overview -> ... -> refs), active-on-load = overview, 0 heading-hierarchy
issues, 0 orphaned tabs.

External link check SKIPPED (weekly cadence, runs Mondays only; last ran
2026-08-10, carried forward last_checked_at).

WIN-dome alignment: known-monitoring-condition dome-wins-dup-id-win-062
re-derived and HASH MATCHED (unchanged) -- tracked under ISS-2961,
review_after 2026-10-31. No new author-side renumbering/title drift.

Pipeline continuity (7a.6 FUSE staleness, 7b workspace-only files, 7c
section-new queue coupling, 7d ws-sync skip log, 7e digest coverage,
7f broken reviews, 7h Mech-A-bypass audit): all clean. 7g drift-audit
fresh (skipped). 7i dispatcher-state was stale and refreshed this run.

Project docs (CLAUDE.md): schedule table matches list_scheduled_tasks
exactly (11 enabled + 3 disabled = 14). File-map spot check clean modulo
already-adjudicated wontfix ISS-3040. No hardcoded-count violations.

Build reproducibility: clean (only diff is the expected daily BUILD_DATE
stamp; reverted local build output, nothing committed from that check).

Live Pages freshness: FRESH (exact signature match).
Delete-propagation backlog: 82/200 (clean).
status.json provenance: 0 regressions over 5 versions (clean).
Ownership-registration-gap lint: 0 gaps over 119 candidates (clean).

No new issues requiring decider action this run.

Full report: monitor/integrity/report-2026-08-13T05-39.json
