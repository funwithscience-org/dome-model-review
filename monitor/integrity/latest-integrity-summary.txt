INTEGRITY RUN 2026-09-26T12:30Z — overall_status: FAIL (see note below on why)

TOP LINE — CRITICAL: all 14 dome-* scheduled tasks (poller, analyst, analyst-baby,
curmudgeon, curmudgeon-verify, decider, integrity [this run], tinker, social,
sloppytoppy-score/rewrite, prune-integrity, workspace-sync, dome-mirror) show
enabled=false in the live scheduler right now, even though every one of them ran
once earlier today (2026-09-26, ~07:00-12:30Z). This lines up with decider's own
"first run post-39d-outage" commit (2535c18) and ISS-3091's note that "the
pipeline (tinker, decider) was disabled 2026-08-18 through 2026-09-25/26" —
status.json's last pre-outage commit was 2026-08-18T02:14:48Z, and only poller
(+one stray workspace-sync run 08-27) committed anything in the 39-day gap.
Today's commits look like a one-off manual catch-up, not the scheduler waking
back up. UNLESS THE OPERATOR RE-ENABLES THE TASKS, THE PIPELINE GOES SILENT
AGAIN STARTING TOMORROW. Curmudgeon-verify and the two sloppytoppy agents are
*intentionally* disabled per existing CLAUDE.md policy — not part of this ask.
No open ISS currently tracks this specific "still disabled" fact (ISS-3091 is
about a DW-003 side-effect of the outage, not the outage/re-enable itself).

SITE STRUCTURE: otherwise clean. test.js 3885/3885 green. build.js html
reproduces docs/index.html byte-identical except the cosmetic "Version 6" date
banner (Aug 17 -> today; build-run-date field, not a content field). Live site
signature matches git HEAD exactly (pages_freshness: FRESH). All internal
anchors/hrefs/tab-nav/data-prose/progressive-disclosure/discoverability checks
pass (mostly via the PROP-111 mechanical precompute, 10/10 green). All pipeline
continuity checks clean: expansion-tracker (next_id ok, 0 dupes), ISS-id
collisions (0), FUSE-vs-git prompt staleness (0/45 stale), workspace-only files
at risk (0), curmudgeon->decider digest coverage (0 unprocessed of 331),
drift-audit (refreshed, was 995h stale, 0 candidates), curmudgeon-dispatcher-
state (refreshed, was 947h stale, valid priority=1), Mech-A-bypass false-
closure audit (0 of 2887 closed issues), delete-propagation backlog (0
candidates), status.json two-writer-race canary (29 versions, clean),
ownership-registration-gap lint (0 gaps).

External-link check (Section 3) skipped today — weekly Monday-only cadence,
today is Saturday.

MINOR ITEMS (analyst follow-up, not urgent):
  - WIN-070 ("Forensic Zoom" kappa=1.67 claim) didn't text-match on the dome's
    live wins.html/index page today. Likely a regex-search limitation, not a
    confirmed removal/renumbering — worth a manual glance next analyst pass.
  - CLAUDE.md's "eleven enabled" pipeline-agent count is stale given the
    scheduler-disabled finding above (same root event, not a separate bug).

overall_status is FAIL only to force this into alerts.txt for urgent operator
attention — no structural or data-integrity check actually failed this run.

Full report: monitor/integrity/report-2026-09-26T12-30.json
