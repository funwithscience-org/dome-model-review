INTEGRITY REPORT — 2026-07-26T01:14:29Z — STATUS: PASS

node test.js: 3838 passed, 0 failed.
PROP-111 mechanical precompute: 10/10 checks passed.

All checks clean: internal anchors, relative hrefs, tab structure (12 tabs,
valid nav chain, no orphans), data-prose consistency, WIN detail consistency,
progressive disclosure, hardcoded colors, discoverability infra (llms.txt/
sitemap.xml/robots.txt/meta tags), expansion-tracker continuity, ISS collision
audit, FUSE-vs-git prompt staleness (0 stale), workspace-only-file orphans (0),
priority-queue coupling, workspace-sync skip log (0 direction violations),
digest coverage (0 unprocessed / 0 parse errors), Mech-A-bypass false-closure
audit (0 flagged), project-documentation schedule/file-map checks.

External-link check: skipped (weekly cadence, next real run Monday 2026-07-27).

Build reproducibility: PASS — only the expected auto-date-stamp diff, no
content/data drift.

Live-site freshness (PROP-123): FRESH.

Delete-propagation backlog (PROP-125): 194/200 candidates — under threshold
but rising (176 -> 194 day-over-day). Not yet actionable; watch for the
200-candidate operator-drain trigger over the next 1-3 days.

status.json provenance (PROP-130): clean, no two-writer-race signature.

WIN-dome-alignment (5b): 2 findings, both recurring/already-tracked, neither
affects published verdicts:
  - MODERATE (ISS-3013): dome site still double-renders WIN-062 under two
    ids sharing the same data-claim-key. Same underlying claim as our entry.
  - MINOR (ISS-3014): dome site's WIN-057 div carries a stale WIN-058
    id/data-claim-key pair. Hidden HTML artifact, no visible collision.

Full report: monitor/integrity/report-2026-07-26T01-14.json
