DOME INTEGRITY REPORT — 2026-06-01T01:15Z
Overall: FAIL

BUILD: ✅ PASS — exact match (0 diff lines). Test suite: 3794/3794 passed.

CRITICAL (action required before next decider run):
  ⛔ Mech-A bypass: 4 decider-fixed entries post-PROP-059 cutoff lack verification_pattern
     ISS-2403 (decider-verification-grandfathering — closed its own PROP-059 ISS without pattern)
     ISS-2417 (decider-minor-patch — sections.json background fix)
     ISS-2418 (decider-minor-patch — uncounted-failures.json dome_accuracy_formula)
     ISS-2419 (decider-minor-patch — uncounted-failures.json _meta.placeholders)
     → Filed ISS-2428. Decider must verify each or reopen as false-closure.

MAJOR (tracked, fix this cycle):
  ⚠ ISS cross-file collision: ISS-2416 and ISS-2417 in BOTH open AND closed issues
     Decider 2026-05-31T20:12 allocated IDs already used by open entries.
     → Filed ISS-2429 (tracked under PROP-063). Rewrite closed entries to ISS-2430/2431.
  ⚠ 3 unresolved template placeholders in published HTML (untracked):
     {{V51_NO_MONITOR}} (=27), {{V51_POST_HOC}} (=31), {{V51_NO_DERIVE}} (=31)
     Not in generate-html.js substitution map. Prose about 28 new V51 WINs shows literal {{...}}.
     → Fix: add 3 entries to substitution map in generate-html.js + rebuild.

MODERATE (tracked under ISS):
  • 4 live expansion-tracker items with integrated=true not moved to archive:
    EXP-489, EXP-537, EXP-538, EXP-539 (live→archive move incomplete)
  • FUSE-stale prompt: monitor/prompts/reference/analyst-mode5-prospective.md
    (will rise to major if stale again next run)

MINOR:
  • 1 external link timeout: GRACE L1A PDF at podaac-tools.jpl.nasa.gov (NASA archive, non-critical)
  • 5 curmudgeon reviews in digest lag (normal pipeline): EXP-481-proposal, SEC-8b.3.c9,
    SEC-part2b-stratification-paragraph.c3, SEC-part6-EXP462-P1.c1, SEC-ts-reactive-prose-pattern.c1

ALL CLEAR:
  ✅ Internal anchors (all 72 WIN anchors, showTab targets, nav chain)
  ✅ Tab structure (12 tabs, heading hierarchy, active tab)
  ✅ WIN detail consistency (all anchors, no broken popups)
  ✅ Dome site WIN alignment (known 057/058 collision documented, WIN-070 expected absent)
  ✅ Hardcoded colors (0 light-theme literals in sections.json)
  ✅ Discoverability (llms.txt, sitemap.xml, robots.txt, all 4 meta tags)
  ✅ Workspace-only files (reviews 656=git, expansions 578=git)
  ✅ Priority-queue coupling (0 section-new items need queue entries)
  ✅ Workspace-sync (no direction violations, mtime-guard normal)
  ✅ Curmudgeon digest regenerated clean (exit 0)
  ✅ Broken review files: 0 parse errors
  ✅ Drift audit: 24.1h old, 3 candidates (fresh)
  ✅ Build reproducibility: exact match
