Dome Structure & Integrity Report — 2026-07-06T01:17:57Z
Overall status: WARN (no criticals)

PASS: node test.js (3837/3837), all 10 mechanical precompute checks, tab structure/nav
chain, internal anchors, relative-href resolution, data-prose consistency, WIN detail
consistency, WIN/dome alignment (69/69 dome WINs covered; known WIN-057/058 collisions
already handled via sub-IDs 057b/058b), progressive disclosure, hardcoded-color scan,
discoverability files, expansion-tracker continuity, ISS-collision audit, FUSE-vs-git
prompt staleness, section-new queue coupling, workspace-sync skip log, digest coverage,
broken-review-file scan, mech-A-bypass audit, build reproducibility (only diff vs HEAD
is the auto-generated build-date footer).

WARN (2 major, 1 moderate — none critical):
1. [major] https://www.chime-frb.ca returns HTTP 503 on 2 independent attempts
   (confirmed with browser UA, not a bot-block false positive). Genuine external
   tool-link outage. Re-check next Monday (weekly external-link cadence).
2. [major] 4 files are workspace-only (not yet in git): WIN-032.c7.json (curmudgeon
   review) + EXP-651/652/653 (analyst expansions). All written 00:45-01:13Z UTC today,
   after the last workspace-sync cycle (00:04Z) and before the next (04:00Z) —
   reads as normal quiet-period sync lag. Re-check tomorrow if not rescued.
3. [moderate] CLAUDE.md's dome-workspace-sync row doesn't document the 2026-07-01
   incident + PROP-121 fix that the live task description now references (cron itself
   matches — narrative is stale).

No new ISS matches found in open-issues.json for any of the above (tracked_under: null
on all three) — decider should create new ISSs per standard intake.

Full report: monitor/integrity/report-2026-07-06T01-17.json
