STATUS: PASS — 2026-07-12T01:16:38Z UTC

All structural checks passed. node test.js: 3836/3836. Build reproducibility
confirmed (docs/index.html matches source data; only the auto-advancing date
stamp differs). Live Pages site matches git HEAD exactly (FRESH).

Highlights:
- All 10 PROP-111 mechanical precompute checks pass (anchors, hrefs, data-prose,
  WIN detail, progressive disclosure, hardcoded colors, discoverability,
  expansion tracker, ISS collisions, broken reviews). Yesterday's moderate
  finding (acknowledged_failures schema-key bug reporting 0 instead of 19 in
  compute-integrity-mechanical.js) is confirmed fixed.
- Tab structure + full sectionNav chain independently re-verified: 12 tabs,
  continuous chain, no orphans, active tab on load = overview.
- WIN-dome alignment re-confirmed clean: all 69 dome WINs covered; the
  WIN-058/058b split correctly resolves the dome's own cross-page (wins.html
  vs model.html) WIN-058 number collision; full 69-title keyword-overlap pass
  found no new claim drift.
- Drift-audit and curmudgeon-dispatcher-state precomputes both hit their
  staleness gates and were refreshed cleanly (2 drift candidates found;
  recommended_priority=1 for curmudgeon, 1 item in priority queue unreviewed).
- delete_propagation_backlog: 48/200 candidates, well below canary.
- status_json_provenance: 0 field regressions across 4 versions checked.
- No workspace-only files at risk, no FUSE-vs-git prompt staleness (44/44
  match), no digest-coverage gaps, no Mech-A-bypass false closures, no
  ISS/EXP ID collisions, no workspace-sync direction violations, no
  project-doc schedule/file-path mismatches.

Skipped (by design):
- External-link check (Check 3): weekly Monday-only cadence, today is Sunday.
  Last real check 2026-07-06 found 1 broken link (chime-frb.ca, HTTP 503,
  unresolved). Next runs 2026-07-13.

Open, tracked, no action needed:
- ISS-2961 (moderate, assigned-analyst): dome site's wins.html has two
  same-claim entries both numbered WIN-062 (page redundancy, not drift).
  Unchanged since 2026-07-10. Our WIN-062 verdict already covers the claim
  regardless of which write-up it maps to.

No active alerts. Full report: monitor/integrity/report-2026-07-12T01-16.json
