INTEGRITY ALERT — 2026-05-21T01:18Z — overall_status=fail

CRITICAL OUTAGES (operator action required, both pre-existing):

1. PUBLISHED SITE STALE (ISS-2167)
   Published origin/main missing WIN-070 and multiple decider patches.
   Decider run 2026-05-20T21:15Z produced commit edf2612a — also 403'd on push (PAT issue).
   Site is now >=4 decider runs behind.
   FIX: git -C <clone> push origin main (after fixing PAT)

2. WORKSPACE-SYNC DOWN (ISS-2166 + ISS-2168)
   No sync run since 2026-05-17T14:05:49Z — 4+ days.
   All agent output since then accumulating as FUSE-only files at risk of loss.
   FIX: Restart dome-workspace-sync scheduled task. Investigate PAT 403.

NEW THIS RUN:
3. DISK FULL — build reproducibility check could not execute.
   /tmp at 100% from other-session clones. No operator action possible.
