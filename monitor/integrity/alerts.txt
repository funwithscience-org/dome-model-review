ALERT — integrity run 2026-07-17T01:14:29Z — overall_status=FAIL

MAJOR: Workspace-only file (1).

monitor/analyst/expansions/EXP-669.json (written 2026-07-17T00:47:56Z) exists on the FUSE
workspace but is not yet committed to git. It postdates the most recent dome-workspace-sync
run (00:04Z) and predates the next scheduled slot (04:03Z UTC). The expansion-tracker.json
entry for EXP-669 (status: pending) is correctly present in git, so tracking metadata is safe
-- only the deliverable content file is at risk until the next sync.

This matches the exact same pattern flagged on 2026-07-15 and 2026-07-16 (different
EXP-669-named file each day due to analyst rewrites, same underlying pending item), which
cleared normally at the next sync cycle both times. No action needed unless this file is still
workspace-only on tomorrow's integrity run -- if so, escalate as a possible workspace-sync
regression. Tracked under ISS-2988.

MINOR: data/wins.json.bak remains orphaned/stale (67 vs 72 WINs), unchanged from yesterday.
Tracked under ISS-2990.

MINOR (informational): Dome site reuses WIN-057 across two different claims (already split
as 057/057b in our own data -- both covered) and has a cosmetic duplicate <h3> for WIN-062
(same claim, reworded). No action needed.

No critical findings this cycle (build reproducibility, live-pages freshness, status.json
provenance, and delete-propagation backlog are all clean).

Full detail: monitor/integrity/report-2026-07-17T01-14.json
