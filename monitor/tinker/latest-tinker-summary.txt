TINKER 2026-06-22T02-40 — Mode 2 (Infrastructure)
==========================================
SELECTED: Mode 2 — PROP-099 prune-resurrection canary TRIPPED (major).

KEY FINDING (major): the prune<->workspace-sync re-add loop is STILL LIVE.
  312 prune-tombstoned integrity artifacts back in git HEAD in 48h
  (107 narrative-cite-audit + 205 verify-pending-run). monitor/integrity ~170MB.
  Mechanism PROVEN: prune deletes (b24f419, 06-21 08:06) -> workspace-sync
  re-adds 1h later (3e1924e, 09:13). The run that re-added them
  (run-2026-06-20T09-13-21Z) had git_deleted_count=70375 (vs ~20631 real
  archive lines) with untrusted=false. PROP-103's trust gate is LOWER-bound
  only (count<50) so a garbage OVERCOUNT escapes -> real paths unmatched ->
  re-added. PROP-103's own "residue stays 0" criterion is failing.

ACTION: Authored PROP-113 (propose-only; workspace-sync.md edits are
  operator-review-gated post 2026-05-21 disaster). Proposes: (A) two-sided
  trust gate (untrusted when count > archive_lines), (B) robust set build
  with per-path regex validation, (C) post-sync resurrection assertion that
  fails loud on the cycle that caused it.

OPERATOR ACTION NEEDED: review/approve PROP-113. The 312-file residue is a
  steady disk leak; manual deletion alone recurs until the set build is fixed.

PIPELINE OTHERWISE GREEN: 3 open issues, net velocity +97/7d, PAT HTTP 200,
  FUSE matches GitHub, root-FS 3204MB free. No stalled agents, no stranded
  patches. PROP-102/108 auto-close still shadow (0 would-close).
