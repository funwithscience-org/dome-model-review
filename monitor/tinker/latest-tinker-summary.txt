TINKER RUN 2026-07-29T02-40 (Mode 0 → defer)

TOP LINE: No changes shipped this run (metrics row + report only). ISS-3012's remaining
scope is fully covered by PROP-142 (authored 07-28, operator-gated) — this run initially
began re-implementing it from the ISS spec, caught the duplication, and reverted
everything before push. Two operator actions are now waiting:

  1) RE-ENABLE dome-curmudgeon-verify (cron `30 1,5,9 * * *`). Verify-class qid 592
     (WIN-031 c11) is in the queue; the documented re-enable trigger has fired two
     consecutive days (qid 591 yesterday, 592 today).
  2) REVIEW + APPLY PROP-142 (queue top-level schema guard + next_id fail-closed).
     Ratification calls: (a) 17-field allowlist incl. depth/last_updated_by,
     (b) shadow-flag soak then removal. ISS-3012 stays open until applied.

SELF-FINDING (moderate): Mode 0 scan has no pending-PROP cross-check, so it re-picked
ISS-3012 and this run nearly shipped a divergent duplicate fix (PROP-143 + direct code
edit — reverted in-run, nothing pushed). Recommended prompt fix: annotate Mode 0 items
with matching non-terminal PROPs and read the previous report BEFORE starting mode work.

HEALTH: pipeline green (decider ran 02:08, commit e81196a). Root-FS free 3393MB.
Backlog: 16 open, net velocity +7/7d, zero unassigned — WoW% trip (10→16) is small-base
noise, 3rd consecutive benign trip; recommended absolute floor (>=30) on percent tiers.
PROP auto-close: 154 walked, 0 closed. Directive auto-close: 0 pending. Commission
HNOTEs: 0. Inbound-burst Detector B: 0.
