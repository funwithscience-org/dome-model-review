TINKER RUN 2026-08-15T02-40 (Mode 3: cost engineering + PROP-092 escalation)

TOP LINE: One moderate finding, and it's an operator decision, not new work.
PROP-092 (dome-mirror overwrite-propagation) has been stuck in Phase 0
manifest-only for 63 days against a designed >=3-day soak. Every recent
dome-mirror cycle manifests monitor/changes/latest-poll-summary.txt as
'git-newer-overwrite' and writes nothing, so the FUSE copy is now ~16 days
stale — this run's Mode 1 quick-check tripped a false "poller stalled 386h"
signal on it (poller is fine: polled 00:08Z today, commit 733976e). The
false signal recurs every run until Phase 2 flips.

  ASK: flip overwrite_propagation.enabled=true in
  monitor/scripts/sync-workspace-step4c.config.json (single config push;
  rollback = same push back to false; 63 days of manifests show only the
  12-entry latest-*-summary allowlist as candidates). If declined, the
  fallback is dropping latest-poll-summary.txt from tinker.md's Mode 1
  staleness loop.

PRE-FLIGHT (first fully-scripted PROP-151 row): sanity check PASSED —
dry-run matched the 08-14 hand-verified shape on all 17 fields, real append
wrote exactly one row with correct run_id (08-09/08-10 bug class did not
recur). Noted a pre-existing semantics quirk (oldest_open_age_days only
counts literal status=='open' issues, hence 0) — inherited from the inline
era, info-only, left as-is for trend continuity.

BACKLOG: total 16 (flat), net velocity +29/7d, zero thresholds, detector-B
quiet. Root FS 3157MB free (+81 DoD). Auth 200; FUSE key files match HEAD.

COST (14d): pipeline sum ~$763 (stable vs ~$780). Decider #1 ($143/14d,
~$10.2/day) — under the $20/day PROP-forcing line; spike runs are the older
half of the window, p50 re-check stays scheduled ~08-19. No variance alerts
(analyst-baby 2.9 carried bimodal, benign). Context load byte-flat across
all four measured agents; CLAUDE.md 249L. Zero active re-work patterns;
1 nominal orphan proposal is pipeline-fresh (awaits today's decider pop).

CLAUDE.md AUDIT: agent table verified against list_scheduled_tasks this
run — 11 enabled/3 disabled, crons match. Integrity's 08-15 WARN (EXP-646/
661 stuck live-archive; hardcoded WIN-69 count in CLAUDE.md) routes via
decider; not duplicated here.

OPERATOR ACTIONS:
  1. NEW: PROP-092 Phase-2 flip decision (see ASK above).
  2. Review/apply PROP-148 (clone-target disk fallback; fold in ISS-3052).
  3. Review PROP-149 (ISS-3025 waits on it).

NEXT RUN: verify orphan proposal entered processed-proposals.json; if
PROP-092 flipped, confirm first overwrite cycle + Mode 1 goes quiet;
decider p50 re-check ~08-19.
