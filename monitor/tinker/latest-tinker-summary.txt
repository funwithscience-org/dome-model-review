TINKER RUN 2026-07-11T02-40 (mode: operator-directive)

DIRECTIVE-20260710-001 (status.json clobber investigation) — COMPLETED this run.
Headline findings:
 - "Phantom" SHA a88d026 is real — it's on origin/main (2026-07-09T04:10:12Z workspace-sync
   commit); it was invisible only because agent clones are --depth 50. No rebase, no typo.
 - The directive's suspect 72aa269 is exonerated (no status.json in its file list). The clobber
   happened a day before the directive assumed; poller detected on its next daily run (~20h later).
 - NOT improvisation: culprit cycle skips_total=81, identical to neighbors. workspace-sync followed
   its spec. Root cause is a two-writer/two-path race: poller committed poll fields git-only
   (07-05..07-09) while FUSE held 07-04 content; decider's FUSE write of last_run armed the mtime;
   whole-file smart_copy then published a chimera (fresh last_run + 5-day-reverted poll fields).
 - PROP-127 CHECK 3 could not have caught it (novel bytes, not a historic-commit reversion).
Recommendation: OPT-2-lite — PROP-129 authored: CHECK 4 monotonic-timestamp guard (last_poll,
last_run, last_analysis) in lint-workspace-sync-push.js, fail-open, override flag, ~60 lines.
If operator prefers zero new code, OPT-3 (self-heal) is defensible — directive said so — but the
race stays latent. Full report: monitor/tinker/reports/workspace-sync-status-json-clobber-2026-07-10.md

Pre-flights:
 - Directive auto-close (ENFORCE): closed DIRECTIVE-20260709-002 (PROP-128 terminal-implemented).
 - PROP auto-close (shadow): 140 walked, 0 would-close.
 - Backlog trend: open=13, net_velocity_7d=+22 (healthy). WoW +333% major trigger is the known
   small-denominator artifact (base was 3) — no action; suggested min-base gate for the threshold.
 - Root FS 3380MB free; no disk pressure; Mode 0 queue empty.

Remaining pending directive: DIRECTIVE-20260628-005 (low prio, disaster-recovery playbooks) — next run.
