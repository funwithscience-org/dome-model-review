TINKER 2026-06-20T06-26 — Operator Directive DIRECTIVE-20260620-001 (workspace-sync cost audit) -> PROP-110 authored.

HEADLINE: workspace-sync's $2.18/run is structural overhead, not work. Per-cycle it commits ~5 files / skips ~20-35 — yet costs 3x the sibling dome-mirror ($0.74/run) purely because it's a 1746-line, ~11-step LLM-walked prompt vs dome-mirror's 216-line single-action script call.

Cost split: 49% cache_write (176K tok, big prompt re-cached cold each hourly run), 26% cache_read (21 turns x 82K ctx), 25% output (42K tok reproducing inline bash blocks).

PREMISE CORRECTION: directive's "65K resurrection-skips per cycle" is the CUMULATIVE skip-log size (94,910 rows), not per-cycle. Skips run in bash, not as LLM turns — not the cost driver.

PROP-110 (propose-only; PROP-051 disaster-sensitive) recommends:
  - Phase 0: STEP_MARKER instrumentation + rescue-event counter (cheap, now).
  - Phase 1a: cadence hourly->2h active window. ~$6.5/day, zero disaster exposure, cron-only. Q8 baseline: rescues fire ~5% of cycles, 403-rescues rarer -> 2h latency acceptable. RECOMMENDED immediate win.
  - Phase 1b: extract FUSE->git pipeline into sync-fuse-to-git.js (mirror dome-mirror/PROP-074), thin prompt to ~300 lines. Ship shadow >=7d + smoke-test. Approaches dome-mirror profile.

OTHER PENDING DIRECTIVES (next runs): -002 integrity runtime audit (65min/run, $4.24), -003 PROP-104 Phase 0 analyze (9+ decider transcripts ready).

Pipeline health spot: open_issues=3, net_velocity_7d=+101, root-FS free 3291MB. All green. Auto-close shadow clean.
