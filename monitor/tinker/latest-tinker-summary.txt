TINKER RUN — 2026-07-21T02-41 — Mode 3 (cost engineering)
=========================================================

PIPELINE: GREEN. All 12 agents fresh on quiet-period cadence; FUSE matches git
HEAD (5/5 md5); root-FS 3195MB free; PROP-099 canary ALIVE and clean (run from
clone, RC=0 — yesterday's dead-canary fix verified working). Backlog healthy:
open issues 11→10 WoW (-9.1%), net velocity +6/7d, no burst signal.

HEADLINE FINDING (major): PROP-112 step-cost attribution is DEAD since the
2026-07-04/05 Sonnet 5 model pin. Decider: 17/17 runs at 0% attribution —
including the $19-27 runs the instrument was built to explain. Integrity:
10/17 near-0%, last 3 runs consecutively. Prompts unchanged (markers still
instructed 15x/29x); Sonnet 5 simply skips the freestanding echo breadcrumbs
(same skip-by-omission class PROP-066 fixed for workspace-sync). Compounding
it (moderate): write-self-cost's `find | head -1` transcript discovery prices
an arbitrary segment of multi-segment runs — 8 of 15 recent decider cost rows
are 4-12-msg fragments ($1.4-2) on days with verifiable full workloads (07-21:
a ~23-min 3-patch run recorded as $1.65/63s). Decider's recorded 14d sum
($148) is a floor; the p95/p50=13.8 variance alert this run is adjudicated as
this measurement artifact, not a runaway loop.

→ AUTHORED PROP-140 (high): Phase 0 analyzer-side fix, no prompt changes —
  (a) signature-based step inference fallback + attribution_mode telemetry in
  compute-decider-step-cost.js; (b) enumerate + sum ALL readable transcript
  segments in write-self-cost.sh / write-self-step-cost.sh. Phase 1
  (structural step-marker.sh folded into per-step command blocks) only if
  fallback attribution stays <70%.

REAL COST 14d (Phase 2 LIVE): pipeline total ~$1,322 (~$94/day). Top sums:
tinker $182 (p50 $10.96 — self-audit: now the #1 cost agent, runs remain
substantive), integrity $159, decider $148 (understated per above),
workspace-sync $140, social $138, analyst $136. curmudgeon-verify ratio 3.0 =
benign no-op bimodality; its 3-slot cadence stays justified — the re-pause
criterion does NOT fire (decider self-apply cycles keep feeding verify items:
qid=587 drained 07-20, qid=588 queued today).

CONTEXT LOAD: stable. CLAUDE.md 242L (+5.7% since 06-25, below bar). No agent
>10%. Housekeeping note: Step 4's decider formula predates PROP-119 — add the
two new reference files to the formula next Mode 3.

OPERATOR ATTENTION:
1. Fresh stranded patch stranded-patches-2026-07-21T02-15-55Z.json (ISS-3001
   3-surface ownership registration for clone-hygiene.sh; touches CLAUDE.md +
   build.js + workspace-sync.md). Apply in own clone + tombstone. (26min old,
   below the 24h bar — flagged early so it doesn't age silently.)
2. Review PROP-140 (two script edits; Phase 0 is prompt-change-free).

Auto-close walkers (enforce mode): PROPs 151 walked / 0 closed; directives
130 walked / 0 pending / 0 closed.

Previous findings: 07-20 FND-1 dead-canary FIXED (verified live); FND-2
clone-hygiene ownership IN_PROGRESS (stranded patch above); FND-3 stale FUSE
prune-integrity.js FIXED; FND-4 ISS-3001 routing NO_RECURRENCE.

Wasted compute: zero active re-work patterns; all multi-review hits historical
and pre-adjudicated. Trend STABLE.
