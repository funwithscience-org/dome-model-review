TINKER tinker-2026-06-28T08-03 — Mode: operator-directive (DIRECTIVE-20260628-002, Pre-Active-Period Readiness Audit)

Executed deep-dive #1 of 4 (Candidate C). Simulated a 3-WIN drought-break burst through
analyst -> curmudgeon -> decider at current quiet-period (1 run/day) cadence.

TOP FINDING (rank 1): The quiet-period throttle has NO automated un-throttle. Under a
3-WIN burst, curmudgeon reviews 1 fresh WIN/run (win-new is not batchable) at 1 run/day,
so the burst takes ~5-6 days to clear first-review+first-patch vs ~1 day at reverted
3-slot cadence. Recovery is entirely detection-latency-bound — a human must notice and
flip 3 crons + re-enable curmudgeon-verify. Nothing detects the burst automatically.

5 weak links named with prompt-line/cadence evidence (WL-1..WL-5), ranked by
likelihood x recovery-cost. Full report:
  monitor/tinker/readiness-audits/pre-active-period-2026-06-28.md

PROPOSALS (propose-only):
  PROP-117 (high) — Inbound-burst auto-detect + cadence-revert RECOMMENDATION signal
                    (poller primary detector + tinker fallback; never auto-flips crons).
  PROP-118 (medium) — Widen poller->analyst start spacing (~3min gap untested under load).

Pipeline health: GREEN. open_issues=3 (all pending-human), net_velocity_7d=+24,
root_fs_free=3295MB. No backlog thresholds fired.

NOTE FOR OPERATOR: 3 more deep-dives queued (DIRECTIVE-20260628-003 B / -004 A / -005 D),
sequenced for subsequent runs — NOT processed this run per the standalone-deep-dive constraint.
