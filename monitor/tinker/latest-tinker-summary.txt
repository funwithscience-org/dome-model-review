TINKER RUN 2026-06-07T10-35 (directive run: DIRECTIVE-20260607-003 → PROP-084, self-applied)

WHAT SHIPPED
- PROP-084 (implemented): clone headroom. New shared script monitor/scripts/clone-hygiene.sh:
  * `sparse` — non-cone sparse-checkout excluding monitor/integrity/ bulk (342 MB / 1,021 files,
    85% of the 401 MB working tree) for decider, curmudgeon, curmudgeon-verify, analyst(+baby).
    Per-agent re-includes from a read-surface audit (decider keeps report-*.json, integrity
    summary/alerts, prop-009-shadow.jsonl; curmudgeon keeps drift-audit.json). Fail-open:
    any error leaves the clone full. NO --filter=blob:none anywhere (PROP-051 lesson).
  * `preclean` — startup sweep of stale sibling clones (>1h), /tmp/*-clone + own-session
    dome-*-clone only. Wired into the 5 cloning prompts; dome-mirror already had one.
- Tinker stale-clone empowerment window tightened 24h → 4h (tinker.md + CLAUDE.md).
- Validation: decider sparse clone = 3,924 files / 61 MB worktree vs 4,875 / 401 MB full. Preclean
  functional test PASS.
- EXCLUDED from sparse, with reasons in PROP-084: dome-mirror (partial-clone ban + reads
  sync-workspace-runs sentinels + working-tree population check), poller/social (never clone),
  workspace-sync (non-goal, gate A1), integrity/prune-integrity/tinker (write monitor/integrity/).

DIRECTIVE HYGIENE
- DIRECTIVE-20260607-003 → completed (PROP-084).
- DIRECTIVE-20260531-009 → completed as OBE (PROP-070 already implemented + deployed).
- DIRECTIVE-20260602-001 → completed as OBE (PROP-075 already implemented + deployed).
- Pending directive queue is now EMPTY.

BACKLOG (PROP-030): GREEN, best on record — open issues 8 (was 56 a week ago, −85.7%),
net velocity +305/7d, zero items ≥14d. No thresholds fire.

WATCH NEXT RUN
- Decider / curmudgeon / curmudgeon-verify logs should show "[clone-hygiene] sparse: ... files
  materialized". Any fail-open message or integrity-read anomaly → single-line rollback per agent
  (see PROP-084.rollback).
- Root FS was at 93-97% during this run; PROP-084 + the PROP-082 drain should relieve this over
  the next week. If decider clone-create fails before then, that is the known disk-pressure mode.
