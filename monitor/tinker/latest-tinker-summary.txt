Tinker run 2026-06-10T17:01Z — DIRECTIVE-20260609-002 (ISS-2663/2664 allocator collisions) COMPLETED

ANSWER: PROP-063 was BYPASSED, not broken. Two distinct events on 2026-06-08:

1. 01:18/01:27 — true concurrent-allocation race: integrity (d3ff587) and decider
   (bfe0bca) both minted ISS-2657/8/9. Git's non-FF rejection caught it; decider
   renumbered its three to 2662/3/4 and bumped next_id to 2665 — correct outcome,
   but via improvised, unwritten procedure. (NBSP entries became ISS-2663/2664.)
2. 05:33 (the actual duplicate-creator) — decider EXP-604 integration run (19ee875)
   allocated SIX ids 2663-2668 via an improvised max(open-ids)+1 scan, ignoring
   next_id=2665 in its own freshly-pulled state AND closed-issues.json. Fingerprint
   of the bypass: post-run next_id still 2665 while ids reached 2668. Had the
   PROP-063 self-heal block actually run, it would have allocated from 2665 and
   prevented both duplicates. 5th write-discipline improvisation event in ~3 days
   (cf. PROP-087's four).

ALSO: directive step 5 (ISS-2664[0] missing fixed_at/fixed_by) is RESOLVED-OBE —
the 06-09 reconcile re-IDed the NBSP generation to ISS-2678/2679 (wontfix,
closed_at present). Zero duplicate ISS ids currently live; no retroactive work.

ACTION: PROP-089 authored (propose-only, awaiting operator review):
- C1: monitor/scripts/allocate-iss-ids.js — single write-path for minting ISS ids
  (PROP-063 max-walk semantics, atomic, --verify mode). Script-extraction pattern
  per PROP-066/050 — prose blocks get skipped; script invocations don't.
- C2: replace ALL inline allocation sites (decider-reporting Step 5, integrity 7h,
  decider integration path) with script calls.
- C3: push-time cross-file ISS uniqueness + next_id invariant in the pre-push lint
  (extends PROP-087 C1(c)); installed for decider AND integrity — bypasses become
  non-landable.
- C4: written recovery procedure for the residual concurrent-clone race (rebase →
  --verify → re-mint own ids → re-push). Canonicalizes what 01:27 improvised.
Suggested apply order: PROP-087 first (hook plumbing), PROP-089 as extension.

BACKLOG TREND: healthy — 8 open (25 a week ago, -68% WoW), net_velocity_7d +135,
nothing aged >=14d, no thresholds fired.

STILL PENDING: DIRECTIVE-20260610-001 (storage-layout audit, low) — next run's
top candidate. PROP-087 + PROP-089 + PROP-088 await operator review.
