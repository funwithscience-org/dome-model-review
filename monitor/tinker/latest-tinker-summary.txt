TINKER RUN 2026-09-26T12-24 (Mode 1: pipeline health — first run after the
39-day outage; previous tinker run was 2026-08-18T02-41)

TOP LINE — THE PIPELINE IS BACK BUT ITS HEARTBEAT LAYER ISN'T. Today's
manually-fired analyst (07:13Z), analyst-baby (07:43Z) and decider (08:07Z)
runs all did real work and their substantive outputs landed (curmudgeon
WIN-010.c16 -> ISS-3078..3087 + EXP-684; analyst's INTERMAGNET eclipse
evaluation rescued -> ISS-3088..3090 + EXP-685 -> curmudgeon qid 608). But
none of their sentinel/status writes survived: latest-analysis-summary.txt
still says 2026-08-18, latest-baby-summary.txt still says 2026-08-18,
status.json last_run still 2026-08-18 (and last_analyst_baby_run has been
frozen at 2026-06-07 for 111 days). Three independent FUSE views
(workspace-sync 10:45Z, mirror 12:05Z, this session) all see git's stale
copy. I can't tell from here whether the writes were reverted by a FUSE
view re-population or never targeted the shared mount. DISTINGUISHING
TEST for tomorrow: if the 09-27 04:0xZ workspace-sync rescues fresh copies,
it was view staleness; if not, it's a write-path defect and the next tinker
run authors PROP-153 (clone-and-push sentinel path like PROP-109 P3 + the
two structural gaps below). Either way, tomorrow's staleness probes will
read analyst/baby/decider as ~39d stale — discount that.

Two structural gaps found while chasing it (both for PROP-153):
  * decider-reporting.md PROP-135 field-merge carries poll-owned and
    decider-owned status.json fields only — analyst/baby fields written
    FUSE-only at 00:30Z/03:20Z are dropped by decider's git-based rebuild
    at 02:00Z before the 04:00Z rescue. Evidence: last_analyst_run stuck at
    the one off-cadence 08-13T18:10Z commission run despite daily runs.
  * sync-workspace-step4c.js overwrite pass judges "git newer" against the
    LAST MIRROR SENTINEL, not against the FUSE copy. After today's 39-day
    mirror gap any post-08-18 commit of a latest-*-summary would have
    overwritten a fresher FUSE write (it fired 0 candidates only because
    FUSE already equalled git). Restart days are exactly when this guard is
    weakest.

STALE-STATUS CLEANUP (self-fixed, pushed): the 08-17/08-18 reports carried
two FALSE operator actions. PROP-148 was applied 2026-08-08 (b7fc9ce2, 8
prompts) and PROP-092 Phase 2 was flipped 2026-08-17T15:30Z — both PROP
files still said pending. Flipped to applied/implemented with obe_*
evidence. ISS-3052 (structure-integrity.md missing the PROP-148 block) is a
real residual gap but its clone step is prose, not a bash block — needs a
judgment edit, escalated. Hygiene: the one-off apply_prop148.py is sitting
in the repo ROOT since b7fc9ce2.

SCHEDULER: all 14 dome-* tasks are enabled:false; 9 fired today by hand.
Four (integrity, social, prune, tinker) launched simultaneously at 12:21Z —
concurrent clone-and-push agents, which the multi-writer files
(tracker.json / expansion-tracker.json / attention-inbox.json) are NOT
protected against. Curmudgeon missed qid 608 today only because it ran
before decider pushed it. Please either re-enable the 11 core crons or fire
in dependency order with gaps.

PRE-FLIGHT: PROP-151 row #5 clean. Backlog 29 open (was 17): +14 new / -1
closed in 7d, net -13 — the restart burst (12 assigned-analyst items all
0d old), percent tiers suppressed by the small-base floor, no thresholds,
detector-B quiet. Auto-close walkers: 164 props / 134 directives, 0
closures (prop enforce flag absent; directive flag present, 0 pending).
Mode 0: ISS-3025 PROP-PENDING (PROP-149) + ISS-3052 -> deferred. Mode 2
green (FUSE == git on data/prompts, auth 200, root 2953MB, sessions
1295MB, PROP-099 clean; /tmp/dome-social-clone is social's LIVE clone,
left alone).

PREVIOUS FOLLOW-UP: PROP-152 FIXED (applied 08-18, FUSE prompt in sync;
live confirmation waits on a verification-class queue item — verify agent
still paused, qid 608 is deep-attack). ISS-3050 closed. Eclipse 08-20 HIGH
band passed inside the outage; analyst delivered the +30d evaluation today
(15 days late by its own account).

OPERATOR ACTIONS:
  1. Scheduler decision (re-enable crons, or sequential manual firing).
  2. 09-27: check whether the three sentinel files got rescued (see test).
  3. PROP-149 review (day 60; ISS-3025 blocked). ISS-3052 judgment edit.
  4. git rm apply_prop148.py.

NEXT RUN: run the sentinel distinguishing test first; branch to Mode 4 /
PROP-153 if write-path. Confirm integrity + social 09-26 reports landed and
no push collisions from the 12:21Z quartet. Verify curmudgeon consumes
qid 608.
