TINKER RUN 2026-04-21T08:40Z — Mode 4 (Proposals & Self-Fixes)

HEADLINE
--------
Wrote PROP-010 (FUSE post-push sync). Applied Mode 2 cp self-fix for 3 stale files inline. This was the 3rd consecutive day with the same FUSE-drift trigger; the PROP is now authored with full file-level patches and is ready to apply.

MODE SELECTION
--------------
Dispatcher quick-check said Mode 2 (3 git-owned files stale on FUSE). Overrode to Mode 4 per mode-selection rule 3 ("previous report has unresolved findings needing fixes"). Running Mode 2 a third day in a row would re-cp the files and defer the architectural fix that prior runs explicitly flagged as owed.

PIPELINE STATE
--------------
  - WINs: 71  |  Open issues: 73  |  Closed: 1097  |  Curmudgeon queue: 4
  - Agents: all on cadence (poller 12h, decider 2h BAU, curmudgeon hourly, analyst 4h, integrity daily, workspace-sync 4h, social daily)
  - 25 commits to main in last 24h; 208 in last 7d. Churn-and-burn is healthy.
  - CI last run green. Auth OK (PAT 93 chars, api.github.com HTTP 200).

INFRASTRUCTURE
--------------
  PRE-FIX: 3 files STALE on FUSE vs main
    data/wins.json       ws cc7faa2a -> gh d80899e9
    data/sections.json   ws 1b142b33 -> gh fad74b7d
    docs/index.html      ws 9a23f106 -> gh abd2b32b
  Workspace pinned at commit b4fa494; main at df4b231.
  Drift commits: df4b231, 67b14d5, b4fa494 (all decider self-applies).
  SELF-FIX: cp 3 files from /tmp/dmr-tinker clone to FUSE; all 3 now md5-match main.
  Disk: 82% used (1794 MB free). Flat at 81-83% for 9 days. Above WARNING but stable.

PROP-010 SUMMARY
----------------
  Category: workflow_redesign
  Target: build.js + monitor/prompts/reference/decider-patches-and-selfapply.md
  Problem: build.js publish is the only git->FUSE sync mechanism; decider self-applies
  push without calling publish. Result: FUSE goes stale between publishes. Recurring
  Mode 2 trigger - today is the 3rd consecutive day.
  Fix:
    1. Extract the sync block in build.js into syncToWorkspace() function (no logic change)
    2. Add a new `sync-workspace` target that calls only that function (no HTML/PDF/git)
    3. Decider calls `node build.js sync-workspace` immediately after `git push origin main`
    4. Soft-fail on decider side; workspace-sync's 4h cycle remains as backstop
  Verification pattern (runs from clean clone root):
    grep -q "target === 'sync-workspace'" build.js \
      && grep -q 'syncToWorkspace()' build.js \
      && grep -q 'node build.js sync-workspace' monitor/prompts/reference/decider-patches-and-selfapply.md \
      && echo FIXED || echo STILL_BROKEN
  Currently returns STILL_BROKEN (expected - patches not yet applied).
  Counter-arguments pre-addressed (5): publish-heavy vs sync-light, workspace-sync
  bidirectional option, per-agent clones instead, 20x frequency amplification,
  self-fix vs PROP classification.

PREVIOUS FOLLOWUP
-----------------
  PROP-005 (preflight):    STILL_BROKEN - 17th no-op. Escalate to close-as-wontfix.
  PROP-007 (OWNERSHIP):    FIXED and stable.
  PROP-008 (change gates): STILL_BROKEN - 2 days old. Not urgent (queue depth 4).
  PROP-009r2 (enforce):    FIXED factually; pattern has CLEAN_CLONE env gate that
                           makes auto-verification return false STILL_BROKEN.
                           Recommend pattern tightening.
  PROP-010 (reverse sync): WRITTEN this run.
  status.json schedule_note: FIXED by field removal.

NEW FINDINGS
------------
  1 (major): Recurring FUSE drift - self-fixed + PROP-010 written.
  2 (moderate): Open issues 38 -> 73 in 24h; decider +1 net today.
     8 new issues (ISS-1288..1295) from SEC-5.0.c2 review.
     Watch for 3+ day sustained net-positive; if so, Mode 3 deep-dive.
  3 (info): PROP-009r2 verification_pattern has CLEAN_CLONE env gate.
  4 (info): Disk 82% stable, no action.

RECOMMENDATIONS (priority-ordered)
-----------------------------------
  1. Apply PROP-010 - full find/replace text in monitor/tinker/proposals/PROP-010.json.
  2. Close/rescope PROP-005 (17 no-op cycles).
  3. PROP-008: apply when curmudgeon queue pressure rises (not urgent today).
  4. Tighten PROP-009r2 verification_pattern (drop CLEAN_CLONE env gate).
  5. Next Mode 3 tinker: monitor open-issues trajectory.

COUNTER-ARGUMENTS THIS RUN ADDRESSED
-------------------------------------
  - Dispatcher-override legitimacy: rule 3 explicitly permits.
  - Over-engineering the PROP: specificity is the guidance from tinker-proposals-and-fixes.md.
  - Two-thing-in-one-run: cp is 30s hygiene, not competing mode work.
  - 20x sync frequency risk: extract is byte-verbatim; soft-fail isolates failures.
