TINKER RUN — 2026-07-26T02:40Z — operator-directive DIRECTIVE-20260725-001

HEADLINE: PROP-141 Phase 2 SHIPPED. The OWNERSHIP-registration-gap defect class
(3 instances in one month: ISS-3000 check-wayback.js, ISS-3001 clone-hygiene.sh,
ISS-3011 monitor/baseline/) is now an auto-detected daily canary:
- NEW monitor/scripts/check-ownership-registration-gap.js — read-only lint; flags
  files touched by agent-clone-sized commits (last 14d) that exist on FUSE but are
  registered in NEITHER build.js OWNERSHIP nor workspace-sync.md nor an append-only
  dir. Allow-list = the 3 deliberately-unclassified multi-writer files. Skips bulk
  commits >200 files and the shallow-clone graft boundary (which fakes a ~4,991-file
  mass-add — verified live, and worth remembering when reading shallow-clone diffs).
- structure-integrity.md §9e runs it daily (exit 0 clean / 3 moderate finding /
  anything else = fail-loud "canary did not run", never silently clean).
- Script self-registered on all 3 surfaces at creation (build.js + workspace-sync.md
  + CLAUDE.md) so the lint is not an instance of its own defect class.
- node test.js: 3838 passed / 0 failed after the build.js edit.

FIRST LIVE LINT RUN already earns its keep: RC=3 with 2 true gaps —
monitor/analyst/analysis-records/ and the monitor/social/ root outputs
(report-*.json etc.) are registered nowhere. Follow-up classification queued as a
finding (next Mode 0/4 pass); §9e will fire moderate daily until registered.

ISS-3011: Phase 1 verified in HEAD (operator commit 665dcf49; build.js 10 baseline
entries, workspace-sync.md 10). Close routed via HNOTE-TINKER-ISS-3011-PROP141-
CLOSE-001 (close_iss_batch, dual-written FUSE+clone) — decider closes next run.
PROP-141 -> implemented. Directive -> completed. ISS-3012 untouched per directive
constraint (next-run Mode 0 top-pick).

OPERATOR ACTION REQUESTED: curmudgeon-verify re-pause criterion FIRED — priority
queue empty 3 consecutive days (07-24..07-26). Recommend re-pausing
dome-curmudgeon-verify (cron 30 1,5,9 * * *) until verify-class work returns;
saves 3 Sonnet runs/day in the quiet period.

BACKLOG: open 13 (12 wk-ago, +8.3% = info tier only; small base). Net velocity
+10/7d (14 closed vs 4 new). assigned-analyst 1. root-FS free 3344MB. Standing
low-prio suggestion: absolute floor (open>=50) on WoW percent triggers.
