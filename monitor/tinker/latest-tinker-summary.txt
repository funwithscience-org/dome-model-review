TINKER RUN 2026-07-17T02-41 (Mode 0 → Mode 4 self-apply)

PICKED: ISS-2986 (minor, assigned-tinker) — Priority 0c2 daily re-fire (audited_at can never
be stamped on append-only monitor/changes/; 5 consecutive re-audits of chg-20260712-0010-003).
FIXED + SHIPPED: PROP-137 self-apply, option (a). resolvePriorityAudit() now treats a change
as audited when any REACTIVE-AUDIT-*.json references it (dispatcher_flagged_files basenames +
topic chg-id regex); new covered_by_reactive_audit_count observability field; fail-safe
degradation to old behavior if reviews dir unreadable. Tested live (0c2 flips needed:false for
the looping change, rest of artifact identical) + synthetic 4-change matrix (uncovered critical
still fires; audited_at legacy path preserved; both coverage paths suppress). Closure HNOTE
filed for decider (HNOTE-TINKER-ISS-2986-CLOSE-001, close_iss_batch). If a 6th reactive-audit
cycle appears after this commit propagates, re-open ISS-2986.

ALSO RESOLVED: (1) stranded-patches-2026-07-10T02-08-04Z.json tombstoned — its CLAUDE.md
kill-shots patch landed in 369d3b8c 3h after stranding (operator skipped tombstone step 5);
audit record in applied-stranded-patches/. (2) Stale FUSE human-notes.json (consumed ISS-2987
HNOTE still pending on FUSE) synced to git content — resurrection hazard removed.

PREVIOUS FINDINGS: PROP-136 FIXED at HEAD (verification_pattern pass; ISS-2987 closed, HNOTE
consumed 02:16Z). Backlog WoW blip was small-N noise (10→10 this week, net velocity +20/7d).
Report-filename drift: this run back on report-<ts>.json convention.

PIPELINE: green. Backlog 10 open, 0 assigned-analyst, no thresholds fired. Root-FS 3385MB free,
clone-leak 0MB, FUSE matches HEAD, PROP-099 canary clean. Pre-flights: prop auto-close 148
walked / 0 closed (shadow); directive auto-close 129 walked / 0 pending (enforce); Detector B
no-op (0 unreviewed win-new).

MODE 0 QUEUE: empty after this run.
