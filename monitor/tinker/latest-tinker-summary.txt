TINKER RUN 2026-07-15T02-41 — Mode 0 (ISS-2984) -> Mode 4 PROP authoring

HEADLINE: The PROP-130 status.json chimera is no longer an occasional race — it fired
3 consecutive days (27bf1e4b 07-12, 19b70279 07-13, 25a4825 07-14), deterministic
under the quiet-period cadence. Root cause confirmed: status.json is workspace-owned,
so NO git->FUSE sync path ever refreshes it — the FUSE copy's poll fields are frozen
at 07-10 content. Decider's daily FUSE read-modify-write arms the mtime, and the
04:0x workspace-sync slot publishes the chimera over poller's same-day commit.

AUTHORED: PROP-135 — fresh-git-base write rule for decider's Step 8 status.json
update (base on clone git HEAD, field-merge decider-owned fields, newest-poll-wins).
One find/replace in decider-reporting.md; kills the chimera at the source. Closes
ISS-2984 when applied. Decider-self-apply eligible.

OPERATOR ACTION REQUESTED: PROP-129 (CHECK 4 monotonic guard in the ws-sync pre-push
hook) has sat unratified since 07-11 while the exact failure it prevents fired daily.
Recommend ratifying it as the enforcement backstop even after PROP-135 applies.

ALSO THIS RUN:
- PROP-131 (disaster-recovery playbooks) auto-flipped to implemented — verification
  pattern passes; both playbook files exist with DETECT/VERIFY sections.
- Backlog trend: 12 open (flat WoW), net velocity +25/7d, root-FS 3356MB free,
  Detector B clean, no thresholds fired. Queue-history row appended (closed-velocity
  counted per the 07-14 corrected rule: closed_at||fixed_at||migrated_at).
- Auto-close walkers: 0 PROP closures (shadow), 0 directive closures (enforce).
- Mode 0 queue remaining for next runs: ISS-2987 (check-wayback false-positive
  removal_alert, moderate), ISS-2986 (audited_at stamping, minor).
- Clone footprint 171MB — PROP-091 drain confirmed converged (was ~480MB 06-12).
- vp hygiene note: PROP-100/132/134 verification_patterns emit no output on their
  taken branch (missing echo/else) — flag for a future hygiene pass.
