TINKER RUN 2026-06-08T02-41 (Mode 2 — Infrastructure & FUSE; trigger: disk)

⚠ OPERATOR ATTENTION — ROOT FS 98% (284MB free at run start)
- Root cause is NOT clone leak this time: zero stale clones found (PROP-084 hygiene
  is working). Driver is system log growth: /var/log/journal 914MB + syslog rotation
  ~260MB, growing ~45MB/day combined.
- Pipeline CANNOT clean this: uid 5120, no sudo (no-new-privileges), /var/log not
  writable. Verified empirically this run — journal vacuum is impossible from inside.
- Headroom after this run's clone cleanup: ~737MB ≈ 10-16 days before the known
  clone-failure cascade mode (2026-05-07/08 class). Operator options: host-level
  journald vacuum / SystemMaxUse cap, or sandbox VM recycle.
- PROP-085 authored (proposed): adds root_fs_free_mb trending to queue-history +
  escalation thresholds (<500MB major, <350MB escalate) + fixes the PROJ_MB metric
  conflation (it was summing 610MB of FUSE content that doesn't live on this disk).
  One-commit, tinker.md-only; approve self-apply or apply operator-side.

PROP-084 POST-DEPLOY (yesterday's watch item): INDIRECT-POSITIVE.
- No leftover agent clones in /tmp; decider (01:06), curmudgeon-verify (23:43,
  pushed 9ae2599), workspace-sync (02:12, exit=0), dome-mirror (hourly sentinels
  through 02:35) all clean post-deploy. No fail-open messages. Rollback not needed.

OTHER FINDINGS
- FND-02 (minor): ws-sync sentinel filename with embedded spaces
  ('sync-run-2026-06-05T12- 09- 53Z.json') + 3 coexisting naming schemes in
  workspace-sync-runs/. Recommend pinning a strftime format in workspace-sync.md
  on its next touch — not worth its own PROP.
- step4c invocation reliability (06-01 FND-01): FIXED — mirror sentinels hourly.
- TF-002 stranded-patch sentinel (06-05): FIXED — none >24h non-tombstoned.
- TF-001 CLAUDE.md cron drift (06-05): deferred, Mode 3 item, carried forward.

BACKLOG (PROP-030): GREEN. Open 9 (WoW −76.9%), net velocity +259/7d, zero ≥14d,
verify-drain 21/7d (curmudgeon-verify now pulling real weight). No thresholds fire.

FUSE: 5/5 key files match git HEAD. Auth: 200. No abort sentinels since 06-04.
