TINKER RUN 2026-07-30T02-41 (Mode 0 defer -> Mode 4 self-fixes)

TOP LINE: Broke the ISS-3012 re-pick loop. Top-pick ISS-3012 deferred again (its fix is
PROP-142, operator-gated since 07-28), but instead of a third no-op run, this run
self-applied the two tinker.md fixes the 07-29 report recommended:

  SELF-FIX 1 — Mode 0 pending-PROP cross-check: the scan now annotates items whose id
  appears in a non-terminal PROP's source_iss as PROP-PENDING and routes them straight
  to outcome (c)/(d) with fall-through to Mode 1-4. Plus: previous run's report/summary
  must now be read BEFORE mode work starts. Functionally tested: ISS-3012 annotates
  "PROP-PENDING: PROP-142 (proposed)".

  SELF-FIX 2 — Backlog small-base floor: percent-growth WoW tiers now require
  open_issues_total >= 30 (4 consecutive benign trips at totals 10-17). Absolute,
  velocity, assigned, and age triggers unchanged. This run's 10->17 (+70%) trip was
  accordingly logged as info, not major.

Both edits pushed to git AND cp'd to the FUSE workspace (prompt-sync gotcha handled).

OPERATOR ACTION (carried, now the only one): review + apply PROP-142. ISS-3012 stays
open until it lands. The 07-29 curmudgeon-verify re-enable recommendation is OBE —
verify-class qid 592 (WIN-031 c11) was reviewed by main curmudgeon and popped by
decider 02:11Z today; queue is empty; verify stays paused per the standing criterion.

HEALTH: pipeline green — poller 00:10Z (NET QUIET), integrity 01:15Z (WARN: known
delete-propagation-backlog 239/200, ISS-3018), analyst 00:47Z, decider 02:15Z
(a9b4e7c), dome-mirror 00:35Z. Root-FS free 3467MB. Backlog: 17 open, 0 unassigned,
net velocity +5/7d. PROP auto-close: 154 walked, 0 closed (shadow). Directive
auto-close: 0 pending. Commission HNOTEs: 0. Inbound-burst Detector B: 0.
Mode 0 remainder: ISS-3020 / ISS-3025 / ISS-3029 (all minor) queue for next runs.
