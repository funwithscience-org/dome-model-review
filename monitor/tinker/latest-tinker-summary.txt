tinker-2026-05-31T06-24 — operator_directive_override (DIRECTIVE-20260531-003)
=============================================================================

Handled DIRECTIVE-20260531-003 (medium, pending): cross-session dome-sync-clone cleanup.

Diagnostic findings:
- Hypothesis (a) CONFIRMED. Cross-session sweep added 2026-05-29 cannot work — /sessions/<other-id>/ is
  owned nobody:nogroup mode 750; agent uid 4419 has no group access. find walks return Permission denied;
  the sweep's `2>/dev/null` silences this, returning 0 paths exit 0 (indistinguishable from "no orphans").
- Option (ii) ALSO unworkable. /tmp is sticky-bit 1777 with cross-session UID namespace mapping;
  foreign-owned /tmp files cannot be `rm`'d by this agent (verified by direct test against
  /tmp/clean_clone.txt — Operation not permitted, exit 1; restored after).
- Option (iii) out of scope per directive non-goal #1 (no system-level cron).
- Recommended (i)+(iv) hybrid: retire the misleading sweep, add same-session stale-clone recovery,
  document operator-side cleanup pattern.

PROP-067 authored. Three patches:
  P1: remove cross-session sweep block from workspace-sync.md (lines 67-78)
  P2: add same-session stale-clone recovery before the clone-fresh guard
  P3: add operator-side cleanup subsection to CLAUDE.md (with copy-pasteable one-liner)

Directive marked status=completed with prop_id_authored=PROP-067.

Backlog-trend metrics (PROP-030, every run):
  open_issues_total=65, assigned_analyst=43, assigned_tinker=0, age_ge_30d=0, oldest_open=0.47d.
  closed_velocity_7d=134, new_velocity_7d=65, net_velocity_7d=+69. No thresholds firing. Pipeline draining.

  Note: initial closure-ledger read used wrong field name (`o.ts` instead of `closed_at`),
  produced velocity_7d=0; corrected at write-time. queue-history row carries _note field documenting.

Action for operator:
- Review PROP-067 at monitor/tinker/proposals/PROP-067-cross-session-clone-cleanup-confirm-impossible.json.
- If approved, decider can self-apply all three patches (all are mechanical edits to git-owned files
  already in workspace-sync NEVER_PUSH).
- The 1-2 min/week operator-side cleanup pattern continues to be the right answer; PROP-067 makes it
  documented rather than implicit.

Clone cleanup: /tmp/tinker-clone (282MB) removed at end of run.
