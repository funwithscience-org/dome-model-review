TINKER RUN 2026-08-11T02-41 (Mode 0 -> Mode 4: ISS-3058 fixed)

TOP LINE: Healthy run, no reds. ISS-3058 (moderate, this run's top pick)
resolved as a mechanical self-fix: decider-intake.md Step 1h2's
stale-assessment guard now compares assessed_at against the NEWEST of
verdict_history.at(-1) AND verdict_set_at — older-schema entries like
PRED-073 (verdict_set_at-only, no verdict_history) were invisible to the
history-only check and nearly got their operator-set falsified verdict
reverted by a stale 2026-05-24 assessment. Replay-verified against all 83
assessment files: exactly one behavior delta (PRED-073, the intended catch),
zero changes elsewhere. Prompt synced to FUSE. Closure HNOTE
(close_iss_batch ISS-3058) dual-written; decider closes next run.

VERIFICATION (owed from 08-10): PROP-146 vp PASS -> FIXED (resolve=98.5,
bogus=0, missing=1). PROP-147 pre-horizon trend on-track: claim-uncited-rate
36.5 -> 34.4 over six audit cycles, flat since 08-10 04:xx; window=30d
provenance token present every cycle. Full <=15% criterion re-check after
2026-09-15. PROP-142 enforce mode LIVE (operator retired shadow flag,
commit 007327a) — 08-10 operator action #1 done, thank you.

MODE 0 QUEUE: ISS-3025 deferred (PROP-149 pending-operator-review);
ISS-3052 held (folds into PROP-148 apply); ISS-3057 (poller Step 10b
regression test) + ISS-3062 (digest FUSE-union design question) queued.

SELF-FIXES: tinker.md PROP-030 block now documents (a) the TINKER_RUN_ID
env-passing example (run_id bug from 08-09/08-10 closed) and (b) the
closed-issues.json fixed_at field-name hazard — this run's first velocity
row undercounted closed_7d as 1 via closed_at; corrected to 20 pre-commit.

OPERATOR ACTIONS:
  1. Review PROP-149 (needs a build cycle for digest-reviews.js) — carried.
  2. Review/apply PROP-148; fold in ISS-3052 (add structure-integrity.md
     to Part A scope) — carried.

HEALTH: backlog 19 open (flat WoW), net_velocity +15 (closed 20 / new 5),
assigned-analyst 0, queue depth 0, no thresholds fired. Auth 200. Root FS
3102MB. Sessions FS 9264MB. Auto-close walkers: 0 closures (161 PROPs
walked, 0 directives pending-linked). All agents fresh through the 08-11
morning slots; integrity PASS 0 issues. FYI (analyst domain, no tinker
action): poller flags the eclipse prediction cluster (PRED-095/TIER-1/2/3/
R005) closing 2026-08-12 with still no dome-side result.

NEXT RUN: Mode 3 (cost engineering + CLAUDE.md accuracy audit — still
owed), unless Mode 0 re-sorts ISS-3057/3062 to top.
