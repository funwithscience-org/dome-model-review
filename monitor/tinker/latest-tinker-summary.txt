TINKER RUN: 2026-05-31T21:31Z  |  MODE 4 — directive-driven  |  STATUS: SHIPPED

DIRECTIVE-20260531-013 (priority=high, PROP-073 implementation): COMPLETED.

What landed (one commit 15674330 + lifecycle commit 1df223fX + queue-history 79af9e0X):

  • Sub-fix #1 STAGE-DELTA GATING (workspace-sync.md Step 4c) — captures
    PRE_ADD_STAGED / POST_ADD_STAGED around `git add sentinel-globs`,
    computes ADDED_BY_4C, and only commits under "PROP-066 step4c
    sentinel" title when ADDED_BY_4C > 0. Defeats the mis-titled-commit
    failure mode (smoking-gun commits: c9e1d92 / 007d0fe / b7c36cb /
    fb52feb8 — all titled step4c sentinel but containing ZERO sentinel
    files).

  • Sub-fix #2 FILENAME-ISO TIMESTAMP PARSE (workspace-sync.md) —
    replaces the broken mtime-based SENTINEL_COUNT_THIS_CYCLE heuristic
    (false-positived because fresh clone resets all sentinel mtimes to
    within seconds; '>now-600' matched them all → counts of 3-4 even
    when zero sentinels were written this cycle).

  • Sub-fix #3 WRITE-PATH LOGGING (sync-workspace-step4c.js
    writeSentinel) — emits absolute path so future tinker audits can
    distinguish "invoked + wrong cwd" from "never invoked".

  • Sub-fix #4 AGENT_NOTES step4c-mis-titled BULLET (workspace-sync.md)
    — detective visibility hook for any sub-fix #1 regression.

Pre-deploy synthetic tests all passed (3 scenarios). Verification grep
returns FIXED. FUSE-bootstrap-sync of both files completed.

OPEN QUESTION (PROP-073 is detective, not preventive): the underlying
LLM-skip of `node monitor/scripts/sync-workspace-step4c.js` remains
unfixed after 6 prior prevention attempts. The next 3 hourly cycles
will show whether sub-fix #3's '[PROP-066] wrote sentinel:' log line
appears in stdout (= invocation working) or not (= time for PROP-074
option 2: architectural separation into dome-step4c-sync agent).

Backlog: healthy. open_issues=49, oldest 0.4d, net velocity +135/7d.
