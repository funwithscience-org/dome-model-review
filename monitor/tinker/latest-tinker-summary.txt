TINKER RUN 2026-08-14T02-40 (Mode 3: PROP-151 — backlog pre-flight extracted to script)

TOP LINE: Executed the 08-12 Mode 3 recommendation. The ~120-line PROP-030
inline-metrics block (plus the Detector-B block) moved out of tinker.md
into monitor/scripts/compute-backlog-metrics.js — PROP-151, self-applied.
The two recurring bug classes the inline block kept producing (RUN_ID
env-passing losses on the 08-09/08-10 rows; the fixed_at field-name
hazard) are now encoded in code, not prompt discipline. tinker.md 723->609
lines (-15.8%); tinker total context load -6.4%, clearing the near-moderate
growth flagged 08-12. Dry-run verified: script output matched this run's
hand-computed queue-history row on all 17 fields. Registered in
workspace-sync.md NEVER_PUSH (clone-invoked-only, per precedent).

08-13 WATCH ITEM RESOLVED: the decider queue-delay self-healed as
hypothesized — WIN-036.c6 processed, ISS-3057/3062 closed 08-13T06:09Z
(closure HNOTE consumed), and the 08-14 02:09Z slot ran on time (eclipse
Phase-1 geometric verdict integration + WIN-002.c8 fix). No escalation.

BACKLOG: total 16 (down from 18), net velocity +29/7d, zero thresholds.
Root FS recovered to 3076MB free (+489 day-over-day — 08-13 watch clears).

COST (14d): pipeline sum ~$780. Decider still top ($162/14d) but the
outage-catch-up spike is decaying (last 3 runs: 9.73/20.93/17.15) —
composite stays under the $20/day PROP-forcing line. Curmudgeon cost
telemetry confirmed alive post-PROP-150 (rows 08-13 + 08-14).
analyst-baby variance carried (bimodal no-op-vs-work, benign).

OPERATOR ACTIONS (carried, unchanged):
  1. Review/apply PROP-148 (clone-target disk fallback; fold in ISS-3052).
  2. Review PROP-149 (ISS-3025 waits on it).

HEALTH: auth 200; FUSE matches git HEAD on all checked files; clone-leak
0MB; sessions FS 9243MB free; auto-close walkers 0/0; no pending
directives; no stranded patches; zero active re-work patterns.

NEXT RUN: first scripted pre-flight — sanity-check the appended row.
Then Mode 3 rotation (decider p50 re-check ~08-19 once spike washes out).
