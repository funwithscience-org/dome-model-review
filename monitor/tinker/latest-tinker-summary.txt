TINKER RUN 2026-07-28T02-41 — Mode 0 top-pick ISS-3012 → Mode 4 (PROP-142 authored)

WHAT HAPPENED
- ISS-3012 Phase 2 (structural half of the priority-queue shadow-field cleanup): authored PROP-142.
  It (a) retires the poisoned next_queue_id fallback in lint-decider-surfaces.js — next_id becomes
  required, fail-closed; (b) adds a top-level schema-conformance check on priority-queue.json
  (unknown fields rejected; 17-field canonical allowlist), shadow-flag-gated for a 7-day soak
  (prop-142-queue-schema-shadow.flag, PROP-106 polarity); (c) syncs DATA-SCHEMAS.md (documents the
  4 previously-undocumented live fields incl. depth) and classifies the flag in build.js /
  workspace-sync.md / CLAUDE.md. All 9 find/replace blocks verified unique at HEAD; patched script
  tested this run: clean-pass (enforce, current file), block-on-injected-cruft (RC=1), shadow-pass
  with warn line. ISS-3012 stays open until you apply the PROP.

OPERATOR ACTIONS NEEDED
1. ⚠ RE-ENABLE dome-curmudgeon-verify: decider pushed qid 591 (class='verification', WIN-045
   patch-verify) at 02:16Z today — that's the documented re-enable trigger, one day after the
   07-27 re-pause. Existing cron `30 1,5,9 * * *` would pick it up at 04:30Z. If you skip,
   main curmudgeon gets it 07-29 ~01:05Z (~23h latency, Opus slot on Sonnet-class work).
2. Review + apply PROP-142 (monitor/tinker/proposals/PROP-142-queue-schema-conformance-lint.json).
   Two judgment calls flagged inside: ratify `depth`/`last_updated_by` as canonical (you kept them
   in the 07-26 cleanup), and remove the shadow flag after a clean 7-day soak.

HEALTH
- Pipeline green: all enabled agents fired their latest slots cleanly (poller 00:05Z … decider 02:03Z).
- Infra green: root FS 3371MB free; clone hygiene clean; PAT verified pre-clone.
- Backlog: open issues 15 (WoW 10→15 trips the moderate threshold mechanically — small-base
  artifact; net velocity +5/wk, assigned-analyst 0). Detector B: no inbound burst.
- Auto-close sweeps: props 153 walked / 0 closable; directives 131 walked / 0 pending.
- Remaining Mode 0 queue: ISS-3020, ISS-3025 (both minor, future runs).
