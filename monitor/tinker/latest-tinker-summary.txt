TINKER 2026-07-06T02-40 — Operator-directive run (DIRECTIVE-20260705-001: pages-deploy-freshness safety check)

PIPELINE: GREEN. open_issues_total=9 (3 open-status / 3 assigned-analyst), net_velocity_7d=+13, root-FS free 2614MB, closed-issues.json 3.14MB. No backlog/burst/commission thresholds fired. inbound_burst_winnew_unreviewed=0.

DELIVERABLE — shipped the pages-freshness safety net (companion to this session's pages.yml paths-filter change):
- NEW monitor/scripts/check-pages-freshness.js (200 lines). Compares a SEMANTIC signature from git HEAD against the live site: {win_count, catalog_total, failures_count, verdict_tally} — all four rendered verbatim in the deployed HTML and identically computable from wins.json + uncounted-failures.json. Chose semantic over hash because build.js stamps a daily BUILD_DATE (a full-HTML hash is non-deterministic).
- Calibration (the point of the directive — don't cry wolf): stateless commit-age grace window (default 20min) absorbs normal commit->deploy latency -> STALE-TRANSIENT (no finding). Fail-safe UNKNOWN when live is unreachable (no alarm during Pages backend brownouts). Severity escalates moderate->major only on >24h-old drift OR a prior STALE within 48h (read-only from report-*.json — no mutable state file, so no append-only ownership violation).
- WIRED into monitor/prompts/structure-integrity.md Section 9b (after Build Reproducibility) + checks.pages_freshness report-schema field + pages_drift category. dome-integrity picks it up on its next 02:00 UTC run. No operator action required.
- PROP-123 authored (status:implemented, grade-A VP). DIRECTIVE-20260705-001 flipped pending->completed.

VERIFICATION (grade A, all demonstrated this run): FRESH exit0 (fixture + live network), STALE exit1 with full evidence (differed fields + landing-commit sha + pages.yml filter-coverage split), STALE-TRANSIENT exit0 (within grace), UNKNOWN exit0 (unreachable).

COVERAGE NOTE (documented trade in PROP-123): the signature does NOT move on pure-prose sections.json edits or template-only build-scripts changes. Accepted per the directive's "calibration > fanciness"; the high-value data-drift class (wins/verdicts/failures) is fully covered. A section_count signal is a possible future add only if a prose-only drift is ever seen to slip through.

>>> STILL OUTSTANDING (unchanged, not touched this directive-preempted run): PROP-107 structural fix pending-integration; 6 ratify-close directives + bucket-D set awaiting operator ruling (HNOTE-TINKER-DIRECTIVE-AUDIT-2026-07-04T15-13).
