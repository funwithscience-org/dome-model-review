TINKER tinker-2026-06-13T07-59 — DIRECTIVE-20260613-002 (decider runtime creep) → PROP-095

HEADLINE: Decider's 32-min run is ROUND-TRIP COUNT, not script cost.
  - Observed run (decider-2026-06-13T10-00, session local_40acb957): ~131 bash + 4 Read
    round-trips in ~32 min ⇒ ~14s/round-trip (model gen + MCP latency).
  - Every recently-added script is sub-50ms: lint-decider-surfaces 0.03s, lint-close-records
    0.04s, allocate-iss-ids 0.02s, and node test.js = 0.16s for all 3831 tests. Even parsing
    the 3.07 MB closed-issues.json = ~25ms. Total script CPU per run < 2s.
  - The hook chain fired ONCE — single consolidated push confirmed (Q2 multi-push = FALSE).
  - Cause: fixed-overhead round-trips have crept up as PROP-081/083/087/089 each added a
    mandatory step the agent runs as its own bash call. PRELUDE/clone-setup ~21 calls;
    commit/push ceremony ~25 calls = ~46-round-trip floor paid EVERY run regardless of work.

FIX (PROP-095, propose-only, medium): two wrapper scripts collapse the deterministic
  ceremonies into 1-3 round-trips each — decider-commit-push.sh (add→lint→commit→push→
  api-fallback→sync) and decider-setup.sh (PAT-verify→rebase→hook-install→digest). Lints are
  RUN not weakened; the git pre-push hook stays as the independent backstop. Projected
  ~9-10 min/run saved (32→~22 min heavy; light runs toward the 10-min target).

OTHER SIDE: the 06:00 run did real work (EXP-620 + self-apply + 3 closures + queue push);
  32 min isn't pathological for that load. This is a fixed-FLOOR creep, not a runaway.

BACKLOG: GREEN. 9 open / 0 open-status / 3 assigned-analyst / net velocity +103 (7d). No
  threshold fires. The runtime issue is cost/efficiency, not throughput.

WATCH (not actionable yet): closed-issues.json 3.07 MB / 45,336 lines — fine at 25ms today;
  revisit rotation if it crosses ~30-50 MB.
