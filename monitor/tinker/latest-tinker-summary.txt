TINKER 2026-06-23T02-40 — Mode 2 -> Mode 4 (PROP-099 canary fired)

PIPELINE: GREEN. All agents fresh (<1h). Backlog healthy: 4 open issues, net +66/7d. Disk healthy (root-free 3.3GB). FUSE matches GitHub. Auth 200.

MAJOR FINDING — prune<->workspace-sync re-add loop is STILL LIVE after PROP-113 P3 (deployed 2026-06-22 16:15):
  - PROP-099 canary (--hours 48): RC=3, 23 narrative-cite-audit files resurrected.
  - Confirmed re-add commit 5a20dcd at 2026-06-22 23:09 (7h post-deploy): 17 tombstoned files re-added (status A).
  - PROP-113 Fix C self-assertion reported resurrection_status='clean' on that exact run — FALSE assurance.

ROOT CAUSE (reproducible): Fix C calls check-prune-resurrection.js --hours 2, which filters by TOMBSTONE AGE (script line 53). The loop re-adds files tombstoned DAYS ago (FUSE retains them; re-add event is fresh, tombstone is old). The 2h window is structurally blind to this — the dominant loop shape. The 48h tinker canary catches it; the 2h self-assertion cannot.

ACTION: Authored PROP-114 — replace tombstone-age window with re-add-event detection (intersect this run's HEAD added-paths with the already-built GIT_DELETED_SET). ~15 LOC, fires only on real re-adds, depth/age independent.

PROP-113 verification: STILL_BROKEN (--hours 48 RC=3, target RC=0). Progress real (prior 312 -> 23) but loop not closed. A second, separate MECHANISM defect remains: smart_copy re-adds in workspace-sync's shallow clone even though the depth-50 producer-fresh guard SKIPs correctly — flagged as a follow-on in PROP-114 complementary_work.

OPERATOR: the 23 residue files will re-tombstone next prune cycle, or can be tree-deleted via the Git Data API escape hatch. The detection fix (PROP-114) is the priority so future re-adds are caught on the causing run.
