TINKER RUN — 2026-07-25T02:40Z — Mode 0 (assigned-to-tinker: ISS-3011)

HEADLINE: ISS-3011 (monitor/baseline/ FUSE staleness) root-caused, remediated, and
PROP'd. Root cause: monitor/baseline/*.txt is registered NOWHERE — not in build.js
OWNERSHIP, not in workspace-sync.md, not in dome-mirror scope — so the git->FUSE
sync path is structurally blind to it. FUSE was serving a frozen 2026-05-24 snapshot
(7 of 10 files; 07-ai-context.txt still the 9,379-byte pre-fix stub, 12 days after
the b80c1c02 reseed). Reproduced independently on this session's mount — persistent
classification gap, NOT transient view staleness. This is the THIRD instance of the
OWNERSHIP-registration gap class (ISS-3000 check-wayback.js, ISS-3001 clone-hygiene.sh).

DONE THIS RUN:
- Interim backfill: copied 07/08/09/10 git-HEAD -> FUSE; all 10 baseline files now
  md5-identical to HEAD on the shared scheduled-session mount. Poller's next run
  reads correct baselines from FUSE (its GitHub-raw fallback stays as backup).
- PROP-141 authored (proposed): Phase 1 classifies the 10 baseline files git-owned
  across build.js OWNERSHIP + workspace-sync.md NEVER_PUSH + CLAUDE.md table (exact
  ISS-3000/3001 precedent pattern); Phase 2 proposes a registration-gap lint so this
  3x-recurring defect class becomes auto-detected. ISS-3011 stays open until Phase 1
  lands (Mode 0 outcome b).

DEFERRED: ISS-3012 (minor, priority-queue stray-fields / lint-decider-surfaces
next_queue_id fallback hardening) — next-run Mode 0 candidate; operator owns the
one-off queue-data cleanup per the issue notes.

BACKLOG METRICS: open 14 (week-ago 9; the +55% WoW trips the >20% major trigger but
is a small-base artifact — net velocity +8/7d, 13 closed vs 5 new, all new items
triaged same-day). Suggest (low prio) an absolute floor on the percent triggers,
e.g. only fire when open_total >= 50; third small-base false-positive this month.

OPERATOR WATCH: priority queue depth 0, day 2 (armed 07-24). If still empty through
tomorrow 2026-07-26, re-pause dome-curmudgeon-verify per quiet-period criterion
(~$6.5/day). PROP-138 (dome-mirror -> Haiku script-only, ~$2.5/day) still awaiting
decision since 07-18. PROP-141 now also awaiting review.

PIPELINE GREEN otherwise: all agents fresh overnight (poller NET QUIET 19th
consecutive; integrity warn -> ISS-3013/3014 intake, normal flow; decider WIN-031 c9
patch cycle clean). Auth 200, root-FS 3380MB free, clone-leak 1MB, PROP-099 canary
clean (RC=0, from clone). wins.json/index.html FUSE diff = expected decider->mirror
lag window (next mirror slot 04:30 UTC), not a fault. Auto-close pre-flights:
PROP 0/0 (shadow), directive 0/0 (enforce). No pending directives.
