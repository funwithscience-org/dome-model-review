TINKER RUN 2026-08-02T09-09 (operator directive DIRECTIVE-20260802-001 -> Stage-2 dangling refs CLEARED, directive COMPLETED)

TOP LINE: Stage 2 of the Mech3 narrative-cite audit is fixed and verified green.
Before (08:10Z workspace-sync run): resolve-rate 46.1%, bogus-anchor 35, file-missing 20.
After (audit-narrative-citations.js v1.2.0, applied this run): resolve-rate 97.1%,
bogus-anchor 0, file-missing 3 — all three Stage-2 axes inside the PROP-069 bars
(>=85% / <=20 / <=20). Production goes green on workspace-sync's next audit cycle.

HOW: all 55 offenders live in curmudgeon reviews — append-only, field-mutation
forbidden — so per your immutability caveat NOTHING was edited or deleted in any
review. Every fix is resolver-side, and every new match rule is EXACT:
  - 16 "missing" files all actually exist (bare/truncated names) -> existence-gated
    fallback dirs + unique EXP-id prefix resolution
  - 15 line-number anchors (raw-text/*.txt:38 style) -> verified against real line
    counts; out-of-range still fails
  - 11 record-id anchors are JSON VALUES not keys (wins.json stores '004', reviews
    cite 'WIN-004') -> exact id-field match + the WIN-NNN->bare-id convention
  - 4 sections.json anchors are HTML id=\"...\" anchors in prose -> quoted-exact match
  - ISS-2153 moved open->closed on closure -> sibling-ledger fallback (ISS-only)
  - 3 SURVIVORS (honest dead pointers, deliberately left failing): status_history.json
    + daily_review.json (dome-live artifacts never snapshotted) and
    /tmp/dome-fetched/02-wins.txt (ephemeral evidence, gone). These are what the
    metric SHOULD flag; they sit well under the <=20 cap.
No-gaming: no citation deleted, no threshold touched, WELL_KNOWN_PATHS untouched;
(wins.json:WIN-999) would still fail. Full 55-row classification in the run report.

PROP-146 authored (pending-operator-review) for the NEVER_PUSH remainder:
  1. state-verification.md Discipline 3 "Citation shape rules" (no /tmp paths, no
     un-snapshotted dome-live artifacts, exact filenames, full-or-bare record ids)
  2. curmudgeon.md one-line pointer
  3. workspace-sync.md NEVER_PUSH: register audit-narrative-citations.js — plus 11
     more unregistered monitor/scripts files found by this run's sweep (minor
     finding; same ISS-3000/3001/3011/3019 registration-gap class). Without this,
     a stale-FUSE round-trip could silently revert v1.2.0 and re-RED the metric.

NOTE: Stage-1 (claim-uncited 95.8%) untouched per your split — that's
DIRECTIVE-20260802-002, which was NOT yet on disk in this run's clone.

OPERATOR ACTIONS:
  1. Review + apply PROP-146 (3 NEVER_PUSH prompt/config targets).
  2. PROP-142 soak unchanged: zero SHADOW lines through ~08-06 -> delete
     monitor/decisions/prop-142-queue-schema-shadow.flag to go enforce.

CLEARED SINCE 02:41 RUN: PROP-145 applied by you (status=implemented at HEAD).

HEALTH: green. Root-FS free 3476MB. Backlog: 19 open, net velocity 0/7d (9 in,
9 out); WoW +46.2% suppressed by small-base floor (13 < 30 base). PROP auto-close
shadow: 0. Directive auto-close (enforce): 0. Commission HNOTEs: 0. Detector B: 0.
Stranded patches >24h: none. Mode 0 remainder: ISS-3025, ISS-3029 — deferred twice
today behind directives; top of queue next run unless DIRECTIVE-20260802-002 lands.
