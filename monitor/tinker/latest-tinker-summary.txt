TINKER RUN 2026-06-21T02-40 — DIRECTIVE-OVERRIDE (DIRECTIVE-20260620-003, PROP-104 Phase 0)

HEADLINE: PROP-104 Phase 0 per-transcript analysis CANNOT be run by tinker.
Cross-session sandbox isolation: a tinker session can read exactly ONE
*.jsonl transcript — its own. Decider's transcripts (other sessions) are
mode 750 nobody:nogroup => EACCES. This is the same constraint documented in
write-self-cost.sh L44-47 / PROP-101 Q1; the directive's Q1 cited that
discovery command but it only ever returns the caller's own transcript.
The analyzer itself WORKS (proven on tinker's own transcript) — the blocker
is access, not code.

DELIVERABLE: PROP-112 — relocate compute-decider-step-cost.js into decider's
OWN end-of-run (co-located with write-self-cost.sh, where the transcript is
readable); emit a committed per-step JSON; tinker aggregates cross-run.
Mirrors the PROP-101 self-cost pattern. PROPOSE-ONLY; operator decides Phase 1.

BOUNDED EVIDENCE (from committed decider/cost-history.jsonl, N=13):
  - cache_read 52.5% + cache_write_1h 30.7% + output 16.7% of USD.
  - fresh-input ~129 tokens/run (negligible).
  - Pearson r(assistant_msgs, cache_read) = 0.996.
  => PRECOMPUTE Phase-1 path is RULED OUT (no fresh state-file load cost).
     Lever is the cached prefix: bash-output-trim + fewer turns, then
     prompt-slim. CONFIRMS PROP-104 the_correction. Q8 confirmed: closed-
     issues.json (3.07MB) does not enter context as fresh input.
  - Q5 (H1/H2 confabulation): UNSATISFIABLE — decider summaries aren't
    archived per-run (single overwritten file; 3 git versions, 0 hits).

PIPELINE HEALTH (incidental): GREEN. open-issues=3, assigned-analyst=0,
net velocity +106/7d, root-FS free 3522MB, closed-issues 3.07MB. No
backlog-trend thresholds fired. No stranded patches, no FUSE/auth issues.

DIRECTIVE-20260620-003 flipped pending->completed (clone-side).
