TINKER RUN 2026-06-11T02:43Z — directive run (Mode 4)
DIRECTIVE-20260610-002 (dome-mirror delete-propagation gap) → COMPLETED.
Authored PROP-091: allow-list-restricted prune-orphan reconciliation pass in
sync-workspace-step4c.js. Candidate rule = prune-managed pattern AND absent from
git HEAD AND past retention (filename date) — the age guard keeps FUSE-born
unpushed sentinels safe. Gates: abort >500 or >50% of category; 200/cycle cap;
ships manifest-only (enabled=false). EPERM probe falls back to manifest-only if
FUSE refuses unlink. PROP-082 unchanged; narrative-cite-audit 7-count vs 14-day
window mismatch flagged as footnote. OPERATOR: review PROP-091 — two decisions
needed: (1) thresholds, (2) whether dome-mirror ever gets real delete capability
(allow_cowork_file_delete) or stays manifest-only forever.
Backlog: GREEN (7 open, WoW -41.7%, net velocity +119/7d). PROP-020/021
auto-closed as implemented (verification FIXED). PROP-085 verification_pattern
malformed (emits "0") — repair next Mode 4 run.
