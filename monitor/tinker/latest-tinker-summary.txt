TINKER RUN tinker-2026-06-03T05-03 — operator-directive mode

Mode: operator-directive (DIRECTIVE-20260603-001 — Mech-A-bypass audit + fix)

OUTCOME — directive completed end-to-end.

PROP-076 authored + self-applied:
- monitor/scripts/lint-close-records.js (NEW, 123 lines) — pre-push lint gate
- monitor/prompts/reference/decider-patches-and-selfapply.md (+18 lines, ~1 line) — invokes lint before push
- monitor/prompts/reference/decider-curmudgeon.md (+1 sentence) — PROP-076 pointer
- monitor/prompts/reference/decider-intake.md (+1 sentence) — PROP-076 pointer
- Total prompt-edit LOC: 21 (well under 80-LOC self-apply budget)

What the gate does: diffs HEAD's closed-issues.json against origin/main, then
applies the structure-integrity.md Step 7h Mech-A-bypass filter to every
newly-added entry. Any entry with status in {fixed, fixed-pending-verification},
fixed_by starting "decider-" (not OBE-allowlist), AND missing BOTH
verification_pattern AND audit_grandfathered causes the push to abort with the
violating IDs printed. Recovery: re-write with verification_pattern (canonical
template) OR set audit_grandfathered with a reason. The lint script's logic
mirrors the daily integrity audit exactly — same filter, same allowlist,
synchronous at push time instead of next-morning.

Retroactive annotation (Step 6a per operator preference): 23 violations bulk-
tagged audit_grandfathered with reason citing PROP-076 + DIRECTIVE-001. The
fixed_by paths span 14 distinct strings: 15/23 are forgot-verification_pattern
with custom fixed_by suffixes (cat a), 8/23 are designed-bulk-close paths (cat
b — archive-terminal-exps, verification-batch, exp553-phantom-fix, verified-
H7-already, prev-run-self-apply multi-ISS batches).

ISS-2519 closed with closure_resolution and verification_pattern.
DIRECTIVE-20260603-001 lifecycle-flipped to status=completed.

DEFERRED — DIRECTIVE-20260603-002 (FUSE-revert race anti-reversion-fix). Both
06-03 directives are high-priority; dispatcher took 001 first (tied on
priority, sorted by filename). 002 is independent per its own non-goals and is
the next run's first pick. Operator's near-term cron-offset mitigation
(dome-mirror :00 → :30) remains in effect.

QUEUE METRICS — open=22, oldest=0.24d, net_velocity_7d=+276. Healthy.
DISK — 98% / 215M free post-clone. End-of-run cleanup reclaims ~290MB.

NEXT TINKER RUN should:
1. Pick up DIRECTIVE-20260603-002 (anti-reversion guard hardening).
2. Verify PROP-076 verification_pattern lands on origin/main.
3. PROP Lifecycle Verification sweep (PROP-074/075/073/072/070).
