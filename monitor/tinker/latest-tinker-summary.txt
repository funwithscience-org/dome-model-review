TINKER 2026-06-14T08-21 — Directive cleanup + PROP-100

MODE: Operator-directive run (10 pending directives resolved).

KEY WORK:
- PROP-100 authored: root-caused the chronic expansion-tracker next_id
  collisions (DIRECTIVE-20260614-001). 3 collisions + 13 stuck-live days
  in 14d = chronic. Cause: the archive-aware next_id clamp is gated behind
  `if(typeof t.next_id !== 'number')`, so it never fires on a valid-but-stale
  next_id (the actual collision case). Same defect PROP-063 already fixed on
  the ISS side. Fix: unconditional clamp across all 4 EXP allocator sites
  (~24 LOC). Single-writer-path (allocate-exp-ids.js) deferred. Propose-only.

- 9 zombie-pending directives flipped to completed. Each had its deliverable
  PROP authored + cross-referenced but the directive was never closed
  (the FUSE-vs-clone write-side defect documented in tinker.md). Flips
  written to the clone and pushed so they actually reach git this time.

BACKLOG (PROP-030): open_issues=10, net_velocity_7d=+82, age>=30d=0.
Pipeline healthy. WoW +25% (8->10) is a small-N artifact, not a real trend.

OPERATOR ACTION: review + integrate PROP-100 (the collision fix is the one
that stops the recurring 'tracker housekeeping' bumps). PROP-091/092/095/096
also awaiting integration from prior directive work.
