TINKER RUN 2026-08-12T02-40 (Mode 3: cost engineering + CLAUDE.md audit)

TOP LINE: Pipeline healthy, backlog shrinking (19->18, net velocity +18),
but Mode 3 found a real regression: CURMUDGEON STOPPED PUSHING. Its prompt
says "unauthenticated clone, doesn't push" (curmudgeon.md:115) while the
PROP-101 block assumes a clone-side commit+push (:542-548). Through 08-01
the model improvised authenticated pushes daily (commits prove it); since
the post-outage restart it reads the prompt literally. Result: (1) cost
telemetry dead — last curmudgeon cost row 08-01 despite reviews on
08-09/10/11/12; (2) reviews reach git via the 04:00Z workspace-sync rescue,
2h AFTER the 02:00Z decider clone, so every review is processed a day late
(PRED-072.c1, WIN-002.c6, WIN-002.c7 all hit this; ISS-3062 was the
near-miss symptom). PROP-150 authored, ready to apply: authenticated clone
via PRELUDE $DOME_PAT + explicit Step 9b end-of-run commit+push
(explicit-path git add only; cost row stays clone-only per PROP-065).
ISS-3062 closes on apply — digest FUSE-union rejected, fix-at-source wins.

MODEL-PIN DRIFT (operator confirm needed): runtime models diverge from
CLAUDE.md pins — tinker running claude-fable-5 (documented Opus 4.8, 9
runs), curmudgeon on opus-4-8 since ~08-01 (documented 4.7). Same class as
the prune-integrity default-pin lesson. Please confirm intent + update the
CLAUDE.md Model pins section (operator-set; not tinker-editable).

COST (14d, whole pipeline ~$729 ≈ $52/day in drought): decider spike
08-02..08-11 ($23-24/run) was outage catch-up — today back to $9.73;
composite daily-cost ≈$13.2/day < $20 threshold, no forced PROP. Plumbing
lane (workspace-sync + dome-mirror ~$10/day) unchanged; PROP-138 still on
the table. Variance: analyst-baby 3.1x is benign bimodal no-op shape.

CONTEXT LOAD vs 06-25 baseline: CLAUDE.md 229->249 (+8.7%), tinker +8.6%
(self-fix docs) — both under alert lines; consider extracting tinker's
PROP-030 inline-metrics block next Mode 3. Analyst/curmudgeon/decider +2-3%.
Wasted-compute detectors: zero active re-work, 0 orphans (714 in ledger),
queue depth 0. Schedule table verified against list_scheduled_tasks — all
crons accurate; self-fixed tinker.md:70 stale "twelve enabled" -> eleven.

MODE 0 QUEUE: ISS-3025 deferred (PROP-149 pending), ISS-3052 deferred
(folds into PROP-148 apply), ISS-3062 resolved-pending-PROP-150-apply,
ISS-3057 (poller regression test) = next run's top actionable pick.

OPERATOR ACTIONS:
  1. Review/apply PROP-150 (curmudgeon push discipline) — medium-high;
     restores cost telemetry + same-day review handoff.
  2. Confirm model pins for tinker (fable-5?) + curmudgeon (opus-4-8?);
     update CLAUDE.md Model pins section.
  3. Carried: PROP-148 (+ISS-3052 fold-in), PROP-149 review.

HEALTH: all agents fresh through today's slots; integrity PASS 0 issues;
auth 200; root FS 3169MB; sessions FS 9432MB; clone-leak 1MB; PROP-099
canary clean; auto-close walkers 0/0; no backlog thresholds fired.

NEXT RUN: Mode 0 ISS-3057, or PROP-150 verification if applied.
