TINKER RUN tinker-2026-05-21T05-50
==================================

Mode 4 (operator-directive override)
Directive processed: DIRECTIVE-20260521-002-audit-files-no-round-trip (medium)
Authored: PROP-052-audit-files-round-trip.json (design-pending-operator-review)

=== Finding: git→FUSE round-trip starvation ===

monitor/integrity/ has 172 git-only files (~36 MB) NOT on FUSE:
  - narrative-cite-audit-*.json:        20 git-only (1.7 MB each → 34 MB)
  - workspace-sync-runs/run-*.json:     92 git-only
  - verify-pending-run-*.json:          85 git-only

All three categories stop on FUSE at mtime 2026-05-17T14:20Z. Git extends
through 2026-05-20T22:05Z. The round-trip is a side-effect of `node build.js
publish` / `sync-workspace`, invoked by decider after self-apply (per
decider-patches-and-selfapply.md L508). Between 2026-05-17T20:22Z and today,
only ONE auto-build commit fired (PROP-050 on 5/20). Workspace-sync's hourly
cycle kept pushing audit files to git, but they never round-tripped to FUSE
because workspace-sync.md never invokes syncToWorkspace().

=== Proposed fix (PROP-052) ===

Single-line addition at end of workspace-sync.md Step 4b:
    node build.js sync-workspace || echo "WARN: ..."
Plus a Rules-section update explicitly permitting sync-workspace (alongside
the existing html exception). Reuses PROP-010 plumbing. Idempotent. Append-
only-safe (only copies files not yet on FUSE). Non-blocking on failure.

=== Critical sequencing ===

MUST land AFTER PROP-051 Workstream C (monitor/integrity pruning). Without
pruning, this fix would repopulate FUSE with the historical audit-file
accumulation (~36 MB at current state; ~600 MB at pre-disaster state). That
bloat is exactly what triggered the 2026-05-21 disaster.

Operator apply order:
  1. PROP-051 Workstream A (safety gates) — authored, pending apply (blocks unpause)
  2. PROP-051 Workstream C (pruning)       — authored, pending apply
  3. PROP-052 (this PROP, round-trip fix)  — apply third

PROP-052's two patches sit in regions of workspace-sync.md that PROP-051
does not touch (P1 at L921, P2 at L932). No textual conflict.

=== Counter-arguments addressed ===

(a) Intentional clone-only? No — state-verification.md L139 + historical
    FUSE state refute. Tinker's grep paths expect FUSE-presence.
(b) Stale FUSE ls? No — fresh find at 5/21T05:45Z + three independent
    categories show identical cutoff.
(c) Re-create 720MB bloat? Yes if mis-ordered, no if sequenced correctly.
(d) Other unknown categories? Bucket-level diagnostic shows only
    monitor/integrity/ has the git-only-extra pattern.
(e) Sync-workspace from wrong clone? No — auto-detect handles same session.
(f) Pruning race conditions? Eventual consistency within 2 cycles.

=== Side observations (carried over from prior runs) ===

monitor/decisions/open-issues.json remains JSON-corrupt at byte 45760.
This blocks PROP-030 backlog-trend pre-flight. Separate decider-side bug
(FUSE-only degraded mode wrote partial JSON). Operator may want to file a
third directive specifically for this corruption.

=== Pre-flight (PROP-030 backlog trend) ===

Skipped — open-issues.json unparseable. Will resume when fixed.

=== No self-apply ===

Per DIRECTIVE-20260521-002 scope_authority: 'Tinker does NOT self-apply.'
This run authored PROP-052 and updated directive lifecycle fields via the
DIRECTIVE-LIFECYCLE additive-edit exception (status: pending → completed).
No clones created during this run (directive constraint: disk still
recovering from disaster). All diagnostics via FUSE + GitHub Tree API.

Operator + cowork-claude apply PROP-052 after PROP-051 Workstream A+C land.
