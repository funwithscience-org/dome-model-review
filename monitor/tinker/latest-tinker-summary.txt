TINKER RUN 2026-07-14T02-41 — DIRECTIVE-20260712-002 (CI hook-migration study) COMPLETED

Mode: operator-directive (preempted Modes 0-4). Deliverables shipped:
 - monitor/tinker/reports/ci-hook-migration-study-2026-07-12.md (Q1-Q4 with measurements)
 - PROP-134: recommendation STAY with client-side hooks; CI required-status-check migration declined.

Why STAY: required checks reject direct pushes at the platform level -> forces PR-flow re-architecture
of ~12 agent push paths (1-3+ min real latency, stale-base races in burst windows: 139/404 commit gaps
<120s). Incident coverage: CI catches 3/4 documented incidents vs 3/4 for installed client hooks — the
sole differentiator is the once-observed install-skip class (eab98c5 2026-07-01). Fail-open is
unpreservable: required-CI is fail-closed by construction; a broken workflow blocks all pushes until
operator intervenes, killing FUSE->git rescue (PROP-051 disaster-class regression — the 2026-05-21
recovery itself needed the direct-push paths enforcement would block).

Optional (operator-approvable separately): PROP-134 Phase 1 — detective NON-required CI job replaying
CHECK 1/2/3 on each push to main. Zero latency, zero fail-closed risk, alarms install-skip class in ~2 min.

Flip triggers to revisit: second install-skip incident; GitHub ships push-compatible required checks;
push cadence drops below ~5/day.

Backlog trend: HEALTHY. open 11 (was 14), net velocity +28/7d, root-FS 3412MB free, no thresholds fired.
Note: fixed a metric bug in this run's queue-history row — closed-velocity must count
closed_at||fixed_at||migrated_at (closed-issues.json entries mostly carry fixed_at).
Auto-close walkers: PROP 0 closures (145 walked); directive walker 0 (this run's directive closed manually
per auto_close_when_deliverable_proposed). Directive DIRECTIVE-20260712-002 flipped pending->completed.
