TINKER 2026-06-30T02-40 — Directive-lifecycle reconciliation + PROP-120

MODE: Pre-flight directive reconciliation + Mode 4 PROP authoring.

WHAT HAPPENED
- Operator-directive discovery surfaced 24 pending directives. The 06-28 deep-dive
  batch (the operator's current focus) is the top-sorted high-priority work.
- DIRECTIVE-003 (Candidate B, prompt-cost archaeology) was ALREADY DONE: its
  deliverable PROP-119 was authored 2026-06-29. Avoided a near-duplicate. My
  independent step-cost analysis reproduced PROP-119's top-2 buckets exactly
  (cross-validation): decider priority-4-completed-expansions ($24.59/13 runs) and
  end-of-run-B-queue-mgmt ($8.17/11 runs) are the stable top-2; integrity check-9 is
  non-slimmable (build tool-result, not prose).
- Flipped 3 verified-OBE directives to completed: 001 (->PROP-116), 002 (->PROP-117),
  003 (->PROP-119). This UNBLOCKS Candidate A (DIRECTIVE-004), whose gate was
  "003 marked completed."

KEY FINDING (PROP-120, propose-only, needs your decision)
- Root cause of the zombie-directive backlog: PROP-108 directive auto-close only
  closes when the linked PROP is {implemented|integrated|applied|self-applied|completed}.
  Propose-only authoring-directives (e.g. 001, 003) have deliverables whose TERMINAL
  state is "proposed" — never in the whitelist — so they can NEVER auto-close.
  They sit pending forever and re-enter the dispatcher every run.
- Fix proposed: per-directive opt-in `deliverable_is_propose_only: true` that lets
  auto-close accept "proposed" for THAT directive only (with a rejected/superseded
  guard). Plus RC2: backfill missing directive_id back-refs on shipped PROP-113/114
  (same omission class as PROP-091/092, FND-03 of 06-20).

OLDER ZOMBIE HIGHS (left for your confirmation, NOT auto-flipped)
- DIRECTIVE-019-001 (->PROP-108 shadow-shipped), -022-001 (->PROP-113 P3-shipped),
  -023-001 (->PROP-114 P2-shipped). All look OBE but phase-shipped status is
  ambiguous about full-intent completion — your call.

PIPELINE: all green. open-issues=4, net velocity +19/7d, root-FS free 2886MB,
no inbound burst, no stranded patches. Quiet-period cadence holding.

NEXT RUN: Candidate A (DIRECTIVE-004) full ~115-PROP dependency-graph walk — now
unblocked. This run captured only its single highest-signal finding (PROP-120).
