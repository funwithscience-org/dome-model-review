TINKER RUN — 2026-06-09T02:45Z
Mode: Operator Directive (override) → Mode 4 PROP authoring

DIRECTIVE-20260608-002 (mode2b broken-schema fallback) — COMPLETED.
Authored PROP-086 (propose-only): structural layer-2 of the attention-inbox
schema-drift two-fold defense.

  • New shared helper monitor/scripts/normalize-attention-inbox.js translates the
    SPECIFIC observed divergence (ATTN-NNN id, boolean `resolved`, subject/detail
    content) into canonical {status:'pending', reason} for dispatch.
  • Any UNRECOGNIZED shape is NOT silently accepted → routed to a stranded list
    that fires a MAJOR integrity finding (constraint 1: surface, don't mask).
  • Consumed by BOTH analyst Mode 2b (immediate anti-stranding, no cadence
    dependency) AND the integrity agent (durable schema_drift surface).
  • Read-only — never rewrites the FUSE-canonical inbox; finding recurs until
    clean (loud recurrence signal). Includes a test fixture + assertions.
  • design_decision_where documents Mode-2b-only vs integrity-pre-walk vs the
    chosen shared-helper design and the trade-offs of each.

Directive marked status=completed (prop_id_authored=PROP-086).

BACKLOG (PROP-030, every run): open=9, open-status=5, assigned-analyst=0,
age≥14d=0, age≥30d=0. Velocity 7d: closed 223 / new 7 = net +216. Healthy;
no thresholds fired.

NO operator action required — PROP-086 is propose-only; operator reviews
before implementation (7 files touched, incl. the new helper, analyst.md
Mode 2b, structure-integrity.md, DATA-SCHEMAS.md, test.js, a fixture, CLAUDE.md).
