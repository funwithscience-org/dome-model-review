TINKER RUN 2026-08-09T02-41 (Mode 0 -> Mode 4: ISS-3025 dispatched, PROP-149 authored)

TOP LINE: First non-red run since the outage. Pipeline recovered — decider,
integrity, poller, analyst, analyst-baby, prune-integrity all pushed
08-08..08-09; FUSE byte-identical to git HEAD (full prompt sweep + key files,
0 stale — integrity's 01:15 "8 stale prompts" finding is already resolved).
This run's own clone exercised the PROP-148 /tmp fallback live (sessions FS
still 0MB) and it worked.

WORK THIS RUN — ISS-3025 (deferred 4x) resolved as outcome (b), PROP-149
authored (pending-operator-review):
  Part B (structural): digest-reviews.js computes recurrent_defense_cycles —
    consecutive prior cycles where the same defense already survived at >=3.
    Decider escalates recurrent defenses one priority level, quotes the prior
    cycle's preemptive_recommendation verbatim in the EXP, and must grep
    wins.json for the landed text before closing. Kills the silent-age-out
    class (WIN-045 c4 sentence sat 83 days until c5 re-found it; the concrete
    sentence had been dropped in the EXP-499->514 consolidation).
  Part A (bounded cleanup): one-off audit script for citation parity across
    hardcoded-monitoring WINs. Ran the heuristic this run: 36 in scope,
    7 flagged (WIN-004/009/016/017/029 no dome-artifact citation;
    WIN-010/019 "appear to be" hedges). One decider batch clears the debt.
    Deliberately NOT a scheduled agent — bounded population, quiet period.

OPERATOR ACTIONS:
  1. URGENT (carried): /sessions host drain (PROP-148 Part C) — still 0MB;
     everything is living on the /tmp fallback.
  2. Review PROP-149 (needs a build cycle for digest-reviews.js).
  3. Delete monitor/decisions/prop-142-queue-schema-shadow.flag — soak
     criterion met (decider ran clean 2026-08-09T02-07, vp returns FIXED).
  4. When applying PROP-148, fold in ISS-3052 (add structure-integrity.md to
     Part A scope).

WATCH: workspace-sync + dome-mirror left no commit/sentinel since PROP-148
was applied (their UTC 00:00/00:30 slots today are traceless). Harmless right
now (FUSE in sync, nothing stranded) but their gate-A2 retarget path is
unverified — next run checks the 04:00/04:30 slots; 24h traceless WITH
FUSE-divergence = Mode 1 red.

HEALTH: backlog 20 open, closed_7d=8, net_velocity +3 (recovered from -1),
no thresholds fired. Auth OK. Root FS 3023MB. PROP-099 canary clean.
Self-note: first queue-history append this run had two bugs (run_id env var
not passed; closure velocity read closed_at but closed-issues.json uses
fixed_at/verified_at) — corrected in place before commit; field-precedence
note added to report findings for the prompt spec.

NEXT RUN: ISS-3029 top pick (Step 3a gate next_iss_id + EXP-596/666 archive
dedupe + stray tracker win entry). PROP-146/147 detailed verify still owed to
next Mode 3 run.
