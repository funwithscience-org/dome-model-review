TINKER 2026-06-16T02-39 — Mode 4 (operator-directive override)

DIRECTIVE-20260615-001 (decider working-set reduction, PROP-040 Phase 2): COMPLETED.
Authored PROP-104 (propose-only, design-pending-operator-review) answering Q1-Q8 with a
3-phase rollout: Phase 0 measure+shadow -> Phase 1 precompute ship -> Phase 2 prompt-slim
(gated on >=40% measured input-token reduction). Mirrors PROP-020/021 conventions.

KEY CORRECTION (operator please read): the directive's headline premise is partly wrong.
Decider's ~17M cache_read/run is NOT dominated by re-reading state files. closed-issues.json
(now 3.1MB, grown from 1.4MB) and the other big state files are read in bash node -e blocks
that print only summaries — their bulk never enters the LLM context. The cache_read is the
growing conversation prefix (prompt prose + accumulated tool-result output + 136-158 turns)
re-read each turn. PROP-104 reframes the levers and its Phase 0 is allowed to KILL Phase 1 if
measurement confirms state files contribute ~0 to cache_read. Honest revised savings estimate:
~$4-9/day (low end of directive's $8-12), with strategic value (reusable precompute lib +
source-attribution infra) likely exceeding the direct dollar figure.

Pre-flight (every-run): backlog healthy (open=26, net velocity +78, root-FS 3732MB free,
0 commission HNOTEs). PROP auto-close shadow: 0 closeable of 115 walked.

Watch: closed-issues.json doubled (1.4->3.1MB) in ~1 day — wall-clock concern for the 5+
full parses/run even though it is out-of-context. PROP-104 candidate #3 addresses if needed.

Directive flipped status pending->completed (written to clone + pushed; NOT FUSE, per FND-01).
