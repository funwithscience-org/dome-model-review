Tinker run 2026-05-28T02:46Z — operator_directive_override (DIRECTIVE-20260526-002)

PIPELINE STATE: GREEN. open_issues=0 (record low). net_velocity_7d=+36. No PROP-030 tier triggers.

DIRECTIVE EXECUTED: DIRECTIVE-20260526-002 (low priority) — Investigate 4 pre-existing
pending-verification problem entries. All 4 steps completed. PROP-062 authored.

PER-ENTRY DIAGNOSIS (4 entries, all pre-PROP-059 legacy):

  ISS-2033 → D-deletion-inverse. Patch (DELETE row) DID land; verification_pattern was
            written with the un-inverted `grep -qF` form. Rec: invert pattern to
            `! grep -qF "live.html footer"`.

  ISS-2036 → D-html-tag-split. Patch (ADD sentence) DID land; verification_pattern
            substring `registry_integrity_notes entries — one covering` crosses the
            inline `</code>` tag in the actual file. Rec: repoint to
            `one covering the two WIN-058 claim keys`.

  ISS-WIN062C6-H2 → O-non-standard-id + M-missing-pattern. Patch (WIN-058 'at least
            three' → 'three') DID land. Rec: strip pattern, flip to fixed (option d) OR
            close as wontfix-OBE-non-standard-id (option e). Tinker prefers (d).

  ISS-2122 → D-id-collision + D-misrouted-closure. THREE distinct bug-reports share
            id='ISS-2122'. Entry 1 (live-state cleanup pipeline bug) has a closure_reason
            describing entry 2's docs/llms.txt patch — misrouted close. Underlying entry-1
            bug may be unfixed. Rec: operator investigates EXP-426 downstream history,
            picks (a1) reopen, (a2) repoint closure_reason, or (a3) mark closed-duplicate.

CLASS GENERALIZATION (Step 3):
  - 1,589 status=fixed entries with no verification_pattern (silent-skip by verifier)
  - Of those, only 1 closed after PROP-059 deployment (2026-05-25T05:20Z) → PROP-059
    working as designed. Going-forward defect rate is rare.
  - 29 duplicate IDs in closed-issues.json: ISS-2120/2121/2122/2123 each have 3 entries;
    25 other IDs have 2 entries. Mostly centered on the 2026-05-18 REPAIR cycle.

PROP-062 AUTHORED:
  monitor/tinker/proposals/PROP-062-fingerprint-html-split-and-legacy-pending-verification.json
  Scope: (1) per-entry disposition menu (operator-applied, not batch-executed) for the
  4 directive entries; (2) ~5-line documentation addition to decider-patches-and-selfapply.md
  for the inline-HTML-tag-split gotcha (so future decider runs don't reintroduce ISS-2036);
  (3) optional one-shot legacy sweep script if needed. Estimated operator effort: ~35 min.

RECOMMENDED FOLLOWUPS (out of scope for PROP-062):
  - PROP-063 — decider next-ID intake collision + retroactive dedup of the 29 duplicate-ID
    entries (touches ISS-2120/2121/2122/2123 and 25 others).

OPERATOR ACTION ITEMS:
  1. Review PROP-062 per-entry recommendations and apply individually to closed-issues.json
     (4 entries × ~5-10 min each).
  2. Optionally apply PROP-062 change_2 (~5-line prompt edit to decider-patches-and-selfapply.md).
  3. Decide whether to file PROP-063 for the ID-collision class.

DIRECTIVE STATUS: marked completed (status: pending → completed) per
DIRECTIVE-LIFECYCLE additive-edit exception in CLAUDE.md.

NO DECIDER PUSHES / PATCH APPLICATIONS — tinker authored a PROP-062 proposal only.
Operator applies all recommendations.
