Tinker run 2026-06-10T02:41Z — DIRECTIVE-20260609-001 (queue-schema complaint) COMPLETED

ANSWER: Case (a) — real decider schema-drift, NOT a verify-dispatcher bug.
- Provenance corrected vs the directive's guess: broken qids 510/511/512 came from
  decider's 2026-06-09T00:19Z run (commit 3d9675d), not the 06-08T16:22 run (6dc8cf9,
  whose queue was clean). Items used improvised fields (target/type/priority/added_at/
  added_by), omitted target_id/target_type/pushed_at, and next_id wasn't bumped.
- Verify behaved CORRECTLY (Mode A gate reads target_id, which was absent). No
  curmudgeon-verify.md change.
- Decider's 04:03Z run purged them but (i) REUSED qids 511/512 for different work —
  archive now permanently holds two generations of each; (ii) silently DROPPED the
  WIN-013.c10 patch-verify intent. No WIN-013 review exists after c10.

ACTIONS:
1. PROP-087 authored (propose-only, awaiting operator review): generalized decider-
   write-site schema lint chained onto the existing PROP-083 pre-push hook. Validates
   new/changed priority-queue items, attention-inbox records, ISS-id uniqueness;
   blocks qid reuse against the archive. Rationale: 4th schema-improvisation event in
   ~2 days, one landing ~8h AFTER the decider.md:812 DO-NOT-DEVIATE warning — prompt
   exhortation is empirically insufficient; producer-side enforcement per the
   directive's if_case_a_followup.
2. HNOTE-TINKER-REPUSH-WIN013-VERIFY-001 filed → decider should re-push the dropped
   WIN-013 patch-verify (class=verification) via the canonical template.
3. Directive marked completed (prop_id_authored=PROP-087).

BACKLOG TREND: healthy — 6 open issues (9 prev run, 25 a week ago, -76% WoW),
net_velocity_7d +335, assigned-analyst 2, nothing aged ≥14d. No thresholds fired.

STILL PENDING: DIRECTIVE-20260609-002 (ISS-2663/2664 allocator collisions despite
PROP-063) — top candidate for next run. PROP-087's open-issues uniqueness check gives
partial push-time coverage but the allocation-race root cause remains that directive's
scope.
