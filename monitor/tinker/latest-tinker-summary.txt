TINKER RUN 2026-08-02T02-41 (operator directive DIRECTIVE-20260801-001 -> PROP-145 authored, directive COMPLETED)

TOP LINE: The recurring 1-2-month FUSE-drain chore now has its structural fix on
paper. PROP-145 "transient integrity artifacts off FUSE" (Option 1, as you chose):
per-run integrity .json files stop being mirrored git->FUSE at the source — a
13-pattern FUSE_EXCLUDE_TRANSIENT regex array + one guard line in build.js
walkAppendOnly, which is the single git->FUSE conduit (publish, sync-workspace,
and dome-mirror's step4c all route through syncToWorkspace).

PART A (the load-bearing verification): NO agent reads any per-run integrity .json
from FUSE — zero, across all prompts + scripts. Decider reads integrity reports via
its sparse-clone re-include; step4c + the PROP-125 canary read sentinels from the
invoking clone; workspace-sync-runs/ soft-complaint greps run from tinker's clone.
The only ${WORKSPACE} hits are degraded-path abort-sentinel WRITES (workspace-sync.md,
dome-mirror.md, decider push-failure) — FUSE-born, rescued FUSE->git, read git-side.
So no Option-2 fallback subset is needed for any pattern.
PART C: every *-archive.jsonl is double-protected (fails the walker's .json filter
AND flows via explicit 'git-append-only' copyIfExists) — untouched. workspace-sync-
skips.jsonl ('git', integrity §7d FUSE read by design) untouched.
PART D: flip delete_propagation.enabled=false + rewrite the config _notes honestly.
KEEP the pass manifest-only — its candidates count feeds the PROP-125 canary, which
now becomes a free exclusion-regression detector (new unregistered per-run pattern
-> candidates grow -> canary fires at 200).

HEADS-UP baked into the PROP: one FINAL tail drain ~2026-09/10. ~142 files currently
on FUSE match prune patterns while still git-fresh; as prune deletes them from git
over 7-90d they become the last orphan cohort. After that: candidates ~0 permanently
(only rare abort/crash sentinels, ~10 per 2 months historically).

BONUS FINDING (minor): prune-integrity never prunes its own run reports —
monitor/integrity/prune-integrity-runs/ is at 119 files, +1/day, no policy. Suggested
policy included in the report; can ride along with the PROP-145 apply.

OPERATOR ACTIONS:
  1. Review + apply PROP-145 (operator-apply — targets are NEVER_PUSH: build.js,
     sync-workspace-step4c.config.json, CLAUDE.md ownership paragraph, mirror
     comments in prune-integrity.js/step4c/workspace-sync.md).
  2. PROP-142 soak unchanged: if zero SHADOW lines through ~08-06, delete
     monitor/decisions/prop-142-queue-schema-shadow.flag to go enforce.

CLEARED SINCE LAST RUN: PROP-144 applied (status=implemented at HEAD) — last run's
#1 action done. ISS-3018 closed by your ~260-file drain 08-01 (candidates=0 in the
08-02 00:35Z sentinel); PROP-145 is the fix that makes ISS-3018 the last of its kind.

HEALTH: pipeline green — all agents fired cleanly overnight (poller 00:08Z quiet,
workspace-sync 00:10Z, dome-mirror 00:35Z, analyst, curmudgeon, integrity, decider
02:1xZ). Root-FS free 3499MB. Backlog: 19 open (3 unassigned-status, 1 assigned-
analyst), net velocity 0/7d (9 in, 9 out); WoW +35.7% suppressed by small-base floor
(base 19 < 30). PROP auto-close shadow: 0 would-close. Directive auto-close
(enforce): 0. Commission HNOTEs: 0. Detector B: 0. Stranded patches >24h: none.
Mode 0 remainder: ISS-3025, ISS-3029 (minor) — top of queue next run.
