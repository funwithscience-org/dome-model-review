TINKER RUN 2026-08-08T18-15 (Mode 2 Infrastructure — 6-day outage diagnosed, stranded work rescued, PROP-148 authored)

TOP LINE: The 2026-08-02..08-08 pipeline outage is the /sessions device (shared
scheduled-sandbox, 9.98 GB) hitting 100% full — 0 MB available — at HOST level:
5,701 accumulated session dirs, ~9.1 GB unreadable/un-deletable by any sandbox
uid. Every agent cloning to ${SESSION}/<agent>-clone ENOSPC'd; root FS had
~2.8 GB free the whole time (workspace-sync abort sentinel: clone_fs=0,
root_fs=2782). This run itself hit the same wall and completed via /tmp
fallback — the escape path poller validated earlier today.

WHO FAILED HOW (6 days):
  - workspace-sync x4 + dome-mirror x2: aborted FAIL-CLOSED per PROP-051 gates
    (correct behavior). Both sync directions dead since 08-04/08-05.
  - decider, social, tinker: silent no-shows 08-03..08-07 (no gate, no sentinel).
  - curmudgeon (08-05, 08-08) + analyst (08-08): NO disk gate -> improvised
    degraded-FUSE-only writes (the 2026-05-07/08 class). Gate coverage audit:
    only workspace-sync.md/dome-mirror.md have disk gates; 7 prompts have none.
  - poller, integrity, prune-integrity, analyst-baby: recovered 08-08 via /tmp.

RESCUED THIS RUN (verified superset/new/union before every copy):
  - Reviews WIN-021.c6.json (stranded 83h) + WIN-002.c5.json -> git
  - analyst ISS-3043 expansion + issue-proposal -> git
  - tracker.json + tracker-archive.jsonl (+2 rows) -> git
  - attention-inbox.json UNION merge (1 FUSE-only entry preserved, git entries kept)
  - status.json FIELD merge (analyst fields lifted; no timestamp regressions)
  - 3 latest-*-summary.txt -> git
  - FUSE-stale files refreshed git->FUSE: expansion-tracker.json (FUSE was a
    pre-archival 07-17 snapshot — "missing" EXPs are in the archive, NO data
    loss; a blind FUSE->git copy would have regressed next_id 678->670),
    pending-digest.json, cost-history files.
  Next decider run intakes the two rescued reviews via digest normally.

PROP-148 (pending-operator-review) — clone-target disk-pressure fallback:
  A. All clone agents: prefer ${SESSION}, fall back to /tmp when sessions-FS
     < 700 MB (preclean both parents). workspace-sync/dome-mirror gate A2:
     retarget instead of abort when root FS >= 1000 MB.
  B. Gate parity: fail-closed disk pre-flight + "never improvise FUSE edits"
     prohibition on curmudgeon/analyst/decider/curmudgeon-verify/social.
  C. OPERATOR/HOST (root cause, urgent): drain /sessions — no sandbox
     workaround exists. Device stays at 0 MB until this happens; the outage
     WILL recur.
  D. Telemetry shipped this run: sessions_fs_avail_mb in queue-history rows
     (first sample: 0).

OPERATOR ACTIONS:
  1. URGENT: host-level /sessions drain (PROP-148 Part C). Everything else is
     mitigation.
  2. Review PROP-148; Parts A/B are NEVER_PUSH prompt edits (operator applies).
  3. PROP-142 flag deletion postponed: outage consumed the soak window — allow
     one clean decider run post-recovery, then delete
     monitor/decisions/prop-142-queue-schema-shadow.flag.
  4. PROP-146/147 verification deferred to next Mode 3 run (infra red today).

HEALTH: backlog 19 open (0.0% WoW), net_velocity -1 (outage artifact, first
negative — no threshold). Auth OK. Git-owned FUSE spot-checks all match HEAD.
Mode 0 remainder: ISS-3025, ISS-3029 deferred (4th time, critical-outage
preemption) — top picks next non-red run.
