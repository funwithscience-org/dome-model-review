=== Tinker run 2026-06-13T14:24Z ===
Mode: DIRECTIVE-20260613-004 -> Mode 4 PROP authoring (pending directive preempts Mode 0/1-4).

DELIVERABLE: PROP-097 — generalized WRITE-VERIFY for analyst status.json artifact-delivery claims.

Root cause of the 08:14Z phantom (EXP-621/ISS-2729 claimed, no artifact):
  PATH-ANCHORING ASYMMETRY. The status P2 write uses an absolute ${WORKSPACE} path and survived;
  the summary, EXP file, proposal, and tracker writes all use RELATIVE paths and were orphaned to a
  non-canonical working dir (mechanism (b) write-misdirection). Push-loss ruled out (analyst is FUSE-only;
  the status rode the same workspace-sync FUSE->git channel any EXP file would have). Status DID land on
  origin at commit 09e06aa (09:21:39Z). Run = scheduled UTC-08 Opus analyst slot.

7-day backfill (Q6): 1 true phantom (EXP-621, already recovered by the 09:52Z re-author). First-of-class.
  EXP-622 is NOT a phantom — real file in FUSE, awaiting tonight 21:00Z workspace-sync (daytime-skip window).

Fix shape: Mechanism 1 = normalize ALL analyst writes to absolute ${WORKSPACE} paths (root-cause).
  Mechanism 2 = WRITE-VERIFY (sibling to PROP-014 Mech 1b): verify each claimed artifact exists before
  writing the cheerful last_analyst_mode; if missing, write downgraded status + analyst-artifact-verify-fail
  sentinel for tinker. Conservative (no auto-remediation). Phase 1 analyst-only; Phase 2 cross-agent gated.
  PROPOSE-ONLY — operator integrates; open question: ship both vs Mechanism 1 alone (minimal, defensible
  given first-of-class).

Backlog: 8 open issues, 0 aged, net velocity +94/7d. Pipeline healthy. No threshold fired.
