TINKER RUN tinker-2026-06-06T05-35 — operator-directive mode (DIRECTIVE-20260606-001, high)

DONE: PROP-080 authored + implemented same-run. The PROP-076 close-record lint
gate was being silently skipped by the decider (3 bypasses in 2 days:
38deae25, f52d3ae0, 196dde8d). Forensics confirmed H1 — LLM skip-by-omission
(script + prompt block existed and the filter catches all 10 violating
entries on replay; no lint trace in any summary; unmasked exit-1 means
execution would have aborted the push). H2/H3 ruled out.

FIX (structural, per directive design a):
 1. Pre-push git hook installed at decider clone setup — 3-line wrapper that
    execs lint-close-records.js. Enforced by git on EVERY push from the
    clone, no matter which prompt blocks the LLM ran. --no-verify forbidden.
 2. push-via-api.js now runs the same lint when closed-issues.json is among
    --files (the API fallback never runs 'git push', so hooks alone can't
    cover it).
 3. Prompt-level call retained as layer 1. lint-close-records.js unchanged.

SIMULATION: all 3 historical bypass shapes BLOCKED; compliant control push
ALLOWED; API-path guard fires before any network call. Evidence in PROP-080.

CANARY: watch next 2-3 decider runs. Zero new Mech-A-bypass ISSs expected
from integrity dailies. A 4th bypass = the LLM found a path around both
layers (most likely the ungated universal-pusher FUSE rescue — out of scope
this PROP per directive non-goals) — escalate immediately.

ALSO: root FS at 99% (184M free) even with no stale clones — system image +
FUSE dominate; one moderate artifact away from the decider-clone-fails
failure mode. Backlog healthy (open=6, net velocity +344/7d); noted a PROP-030
small-N calibration nit (WoW% trigger fires on 3→6).

REMAINING PENDING DIRECTIVES (medium): 20260531-008 (implement PROP-069),
20260531-009 (author PROP-070), 20260602-001 (analyst starvation guard).
