TINKER RUN — 2026-07-22T02-41 — Mode 4 (proposals & self-fixes)
================================================================

PIPELINE: GREEN. All 12 agents fresh; integrity pass (0 issues); root-FS
2998MB free; PROP-099 canary clean; backlog 12→10 WoW, net velocity +18/7d,
no burst, no thresholds fired. FUSE wins.json/index.html trail git HEAD by
one commit (73a5520, 02:12Z) — verified as by-design mirror lag (last mirror
slot 00:35Z, next 04:30Z heals), NOT staleness-class.

HEADLINE: PROP-140 Phase 0 SELF-APPLIED — step-cost attribution revived +
multi-segment cost pricing. Four files:
  * compute-decider-step-cost.js (schema /2): accepts multiple transcript
    segments (ordered by first_ts), adds SIGNATURE-based step inference as
    fallback when STEP_MARKER discipline is absent (the Sonnet 5 failure
    mode — decider 17/17 runs at 0% attribution). Markers take precedence;
    once seen in a segment, signatures are suppressed for that segment.
    summary gains attribution_mode + segments_analyzed.
  * compute-integrity-step-cost.js: regenerated structural copy with the
    integrity signature table (+check-prune-resurrection.js added).
  * write-self-cost.sh: enumerates ALL readable transcript segments and sums
    cost (fixes the find|head-1 fragment pricing that understated decider's
    recorded 14d sum). Row gains segments_found/segments_priced/per_segment;
    total_usd stays backward-compatible.
  * write-self-step-cost.sh: passes the full segment list to the analyzer.
TESTED: synthetic 2-segment fixture (hand-verified cost math, mixed
attribution) + live runs on tinker's own transcript. All syntax-clean.
VERIFIES MECHANICALLY TOMORROW: next decider (02:11Z) + integrity (01:15Z)
rows should show attribution_mode != 'none'; PROP-140's verification_pattern
reads the last decider row, so tomorrow's Step 2b walk adjudicates it.

Known coarseness (minor finding, watch next Mode 3): signature test includes
tool_result text, so prose mentions of signature filenames can open windows
(tinker's own transcript attributed 71% that way). If decider/integrity
fallback windows look prose-polluted, narrow to command-body-only.

Step 2b walk: PROP-096 auto-closed (pending → implemented; isLive fix
verified live in analyst.md). PROP-132/PROP-134 verification patterns
executed empty — not grade-A; flag for next touch. Staged-status PROPs
(021/091/092/093/104) returned FIXED on shipped portions; left to walkers.

Previous findings: 07-21 FND-1 (dead attribution) + FND-2 (fragment pricing)
→ FIX_APPLIED_THIS_RUN; FND-3 variance alert → CLOSED_AS_ADJUDICATED
(historical decider rows 07-05..07-22 remain pre-PROP-140-unreliable — do
not retro-edit); FND-4 ISS-3001 stranded patch → FIXED by operator
(ef0a8a3a + tombstone 82b9ce6b, ISS closed 07-21).

OPERATOR ATTENTION:
1. PROP-140 Phase 0 self-applied (requires_human_judgment:false + 17-run
   instrument outage justified acting). Revert = 2-file git revert if you
   wanted review-first; otherwise nothing to do.
2. Poller recommends closing the PROP-130 chimera tracking thread (7th
   consecutive clean cycle) — decider/operator call.

Auto-close walkers: PROPs 151 walked / 0 closed (shadow); directives ENFORCE
/ 0 pending / 0 closed.
