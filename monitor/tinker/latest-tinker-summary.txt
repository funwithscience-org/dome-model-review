TINKER RUN tinker-2026-06-07T06-10 (operator-directive mode)
=========================================================
DIRECTIVE-20260607-002 (PROP-080 hook persistence) — COMPLETED.

ROOT CAUSE (forensically confirmed): operator's H1 was RIGHT this time,
with a sharper mechanism. PROP-080 put the hook install immediately after
an UNCONDITIONAL `git clone` in the self-apply block. Decider's clone
persists across runs -> every post-deploy run hit "destination path already
exists", skipped the whole block, and pushed hook-less. decider.md's
existing-clone prelude branch never had an install step. The hook never
existed in the pushing clone on ANY post-deploy run.
  Ruled out: H2 (lint replay fires 11/11 on a4e464f entries, md5-identical
  script), H3 (api path is lint-patched + unused that day), H4 (no
  --no-verify), H6 (violations on every run = persistence, not race).
  H5 moot, covered defensively by the fix anyway.

FIX (PROP-083, implemented + validated this run):
  - decider.md prelude: per-cycle idempotent hook install (content+exec check)
  - selfapply.md: clone step reuse-safe -> hook install runs every pass
  - Sims: install-on-reuse PASS, idempotent no-op PASS, exec-bit heal PASS,
    lint blocks the 11 historical violations PASS.

DATA: 937 pre-deploy + 17 post-deploy violating close-records bulk-
grandfathered per pre-approval. closed-issues.json now lints clean (0
violations). ISS-2621 (critical) CLOSED. Integrity should NOT re-fire.
NOTE: ISS-2563 was pre-deploy (in the 937); ISS-2600 is wontfix (lint
correctly ignores it) — integrity's 18/19-ID listing was slightly over-broad.

QUEUE: open=8, net velocity +310/7d. Healthiest on record. No thresholds.

INFRA (moderate): root FS hit 100% at run start; recovered by sparse-clone
strategy. monitor/integrity = 382MB is the repo-size driver — candidate
future PROP on prune-integrity retention. /tmp/edit-clone (469MB, fresh)
left untouched per safety rules.

REMAINING PENDING DIRECTIVES (next runs): 20260531-008 (PROP-069 impl),
20260531-009 (PROP-070 author), 20260602-001 (analyst starvation guard).
