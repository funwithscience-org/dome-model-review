TINKER RUN SUMMARY — 2026-06-13T05:06Z (Mode 4: directive-driven PROP authoring)

WHY MODE 4: pending operator-directive DIRECTIVE-20260612-001 (medium) found in
pre-flight (read from fresh clone, authoritative). A pending directive preempts
Mode 0 and Mode 1-4. Task: propose a safe fix for dome-mirror's overwrite-mode
git->FUSE propagation gap — the sibling to PROP-091's delete-propagation gap.

DELIVERED: PROP-092-dome-mirror-overwrite-propagation.json (propose-only).
Adds overwritePropagationPass() to sync-workspace-step4c.js mirroring PROP-091's
Phase 0/1/2 shape + PROP-051 safety gates. Two-guard rule: content-hash diff AND
git-last-commit-time > lastSentinelAt (direction guard — uses durable git commit
time + sentinel baseline, NOT raw FUSE mtime per PROP-082). Explicit 12-entry
allow-list of latest-*-summary.txt surfaces. Ships enabled=false (manifest-only).
EPERM->manifest-only fallback. Embedded code passes node --check.
  Q1 content-hash (chosen). Q2 explicit allow-list (OWNERSHIP-derivation rejected:
  it would miss latest-tinker-summary.txt, which is append_only_glob-frozen, not
  'workspace'). Q3 canonical=latest-in-git, OWNERSHIP unchanged, direction guard
  protects local unpushed writes. Q4 sibling PROP (not folded into PROP-091),
  ranked by ship-safety; overwrite is lower-risk than delete (copyFileSync works
  on FUSE; no unlink dependency; recoverable from git).

ROOT-CAUSE NOTE: the gap is broader than the directive framed. build.js
syncToWorkspace is create-only in TWO ways — (a) 'workspace' files skipped
wholesale; (b) append_only/_glob .txt either filtered (.json-only walker) or
copied-once-then-frozen. All 12 latest-*-summary.txt surfaces affected.

BACKLOG: 10 open (0 in 'open' status, 0 aged >=14d), net velocity +103/7d.
WoW +42.9% (7->10) nominally trips the PROP-030 'major' %-tier but assessed INFO
— pure low-volume noise; recommend gating %-tiers behind an absolute floor (>=50).

DEFERRED (Mode 0, not lost): ISS-2725 (assigned-tinker, MAJOR, verification) —
attention-inbox status-vocabulary mismatch stranding 3 analyst verification items.
Take it next run unless a higher-priority directive lands.

OPERATOR NUDGE (still live): ISS-2341 (~14d) + ISS-2543 (~10d) pending-human.

PROP-091 sibling: Phase 0 landed (enabled=false). Pair PROP-092's Phase-1
manifest observation with PROP-091's when it reaches review (same helper/sentinel).
