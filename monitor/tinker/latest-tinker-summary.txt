TINKER 2026-06-14T16-33 — directive-override -> Mode 4 (PROP authoring)

DIRECTIVE-20260614-003 (prop-lifecycle auto-close) COMPLETED.
Authored PROP-102: PROP-lifecycle auto-close.
  Mechanism A — auto-close on verification_pattern PASS (Case 1: status stale despite own VP passing).
  Mechanism B — deliberate-supersedes graph (Case 2: solved via a sibling PROP, original VP still fails).
  Answers Q1-Q10: status whitelist/denylist, A/B/C pattern-grading rubric, obe_* closure metadata
  (mirrors today's bulk-close), prop-auto-close-ledger.jsonl (git-append-only), do_not_auto_close opt-out,
  shadow->enforce rollout via prop-auto-close-enforce.flag, every-run frequency, out-of-scope boundary,
  + Q10 mark-prop-superseded.js manual tool. Single PROP; A/B seam preserved for staged integration.

Phase 0 shadow dry-run (first real result): 115 PROPs walked; grade dist A:37 B:3 C:40; eligible:0;
  WOULD auto-close: 0. Matches directive prediction (~0-3) — today's 54-PROP sweep already cleared every
  whitelist-status PROP. ~50% grade-C share is the safety argument for grade-gating.

Backlog-trend: open_issues 12, net_velocity_7d +75, root-FS 3907MB free. No threshold fired. Pipeline healthy.

Next run: resume normal Mode 0-4 dispatch (directive consumed). PROP-102 is propose-only — operator integrates.
