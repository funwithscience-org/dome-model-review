TINKER 2026-06-15T02-47 — Mode 2 (Infrastructure) — selected by PROP-099 canary RC=3

FINDING FND-01 (prune-resurrection, moderate; canary raw=major):
45 prune-tombstoned monitor/integrity/ files (18 narrative-cite-audit + 27 verify-pending-run)
are back in git HEAD. Deep trace: NOT an ongoing loop. ALL 45 came from ONE anomalous
workspace-sync run at 06-14T16:22 (commit 8b358ede) that bulk-added 39 tombstoned files and
self-labeled "Clean run: 59 new files added" with ZERO git-deleted-authoritative skips.
The PROP-094/099 guard is WORKING in steady state — 96 "do not resurrect" skips in the recent
skip-log, firing every cycle on the exact files. 7 ws-sync cycles since 16:22 = 0 new
resurrections. Files are stuck in HEAD only because smart_copy's dst-exists branch no-ops once
they're present. Root cause: the per-run GIT_DELETED_SET build is a single point of failure with
a PERMISSIVE failure mode (empty/short set -> falls through to date proxy -> bulk resurrect),
no fail-closed check, no persisted git_deleted_count.

ACTION: Authored PROP-103 (propose-only) — fail-closed when GIT_DELETED_SET is empty/short
while prune archives are non-empty (skip prune-managed integrity rescues, write a sentinel),
plus persist git_deleted_count in the run report for same-day detection. ~20 LOC, PROP-051
gates untouched, prune untouched.

NO MANUAL DRAIN NEEDED: the 45 residue will be re-deleted by the next scheduled prune
(~08:00 UTC today) and the working guard will keep them gone. >>> NEXT TINKER RUN: verify
check-prune-resurrection.js drops to 0 after that prune; if it re-leaks, escalate PROP-103. <<<

Backlog-trend: open_issues 23 (9 open / 9 assigned-analyst), net_velocity_7d +55, age>=30d 0,
root-FS 3847MB free. WoW 9->23 (+155%) mechanically trips 'major' but is a small-base artifact
(queue is draining, not growing) — FND-02 notes a candidate WoW%-absolute-floor calibration.

Pipeline otherwise GREEN: FUSE matches GitHub, auth 200, no stranded patches, all agents fresh.
