TINKER run tinker-2026-06-13T06-19  (Mode 4 via operator directive)

DIRECTIVE-20260613-001 (high) — prune-integrity "push gap" — COMPLETED.

VERDICT: The directive's framing is wrong, and the operator's own "honor the
other side" caveat (constraint #3) is the real answer. prune-integrity is NOT
failing to push. It pushes its deletes successfully every single day. The files
come back because dome-workspace-sync re-adds them ~1 hour later.

This is the same prune <-> workspace-sync re-add loop PROP-082 was built to kill
(2026-06-07). PROP-082 is deployed and its self-test passes — but its
is_prune_expired() filter uses a 14-DAY date window for narrative-cite-audit,
while prune's actual policy for that category is COUNT-based (keep last 7 files).
At ~150-220 narrative-cite-audit files/day, "keep last 7" = a few minutes of
files, so prune deletes files minutes-to-weeks old; workspace-sync's 14-day proxy
judges anything <15 days "still fresh" and resurrects it. That 2-to-15-day gap is
the loop. ~85% of the 457-file / 387 MB backlog the operator drained today is this
one mis-tuned category.

Smoking gun: narrative-cite-audit-2026-05-29T0006Z.json — DELETE(prune)→
RE-ADD(workspace-sync) pairs on 6 straight days, all after PROP-082 shipped.

AUTHORED: PROP-094 (propose-only). Proposes replacing the brittle per-category
date proxy with a git-deletion-authority check (policy-agnostic, drift-proof,
fixes all 9 categories + the date-boundary leak), a minimal count-aware stopgap
as fallback, a resurrection-canary detection check (compare prune's archive
ledger vs git HEAD presence next day — the signal that was missing for 6 days),
and confirms the operator's manual backfill is sufficient.

Backfill: already done by operator (git commit f3d8c46 + FUSE allow_cowork_file_delete drain).
PROP-051 safety gates untouched; prune-integrity.js eligibility untouched per constraints.

Queue health: 10 open issues, net velocity +105/7d. GREEN. No backlog-trend finding.

Operator action: review + integrate PROP-094. Until then, FUSE may slowly
re-accumulate narrative-cite-audit copies; one more allow_cowork_file_delete drain
is the holding action if it bites before the fix lands.
