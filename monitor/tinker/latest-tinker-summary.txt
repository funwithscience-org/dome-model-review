TINKER 2026-06-23T05-53 — operator-directive mode (DIRECTIVE-20260623-001)

PRIMARY: Authored PROP-114 root-cause investigation + amendment (propose-only).

ROOT CAUSE FOUND (corrects prior PROP-114 guess):
  The residual 23/day integrity resurrection is NOT the Fix C tombstone-window
  blindspot and NOT a producer-fresh shallow-clone blob fail-open (refuted via a
  faithful depth-50 clone at runtime HEAD, which SKIPs correctly). It is
  cross-bash-session loss of the un-exported $GIT_DELETED_SET variable when the
  workspace-sync agent splits the ~600-line Step 2 block across bash-tool calls
  (PROP-066 class). Empty set -> grep matches nothing -> fresh prune batch falls
  through to cp -> resurrection. Confirmed by empty-var simulation (re-add) vs
  populated set (SKIP), matching the SKIP@21:12 -> re-add@23:09 of path 0009Z.
  Secondary: the 23:10 run persisted 0/625 skip records to the jsonl (same
  ephemeral-loss disease on $SKIP_LOG) which hid the evidence.

FIX (propose-only, recommend P2 = combined):
  A. Deterministic in-clone set path (.git/dome-git-deleted-set.txt) — survives
     bash-call splits.
  B. smart_copy self-heal default + fail-closed guard for prune-managed integrity
     when the set is unavailable (harmless skip; opposite is the disaster).
  C. Fix C v2 = re-add-event detection (commit-added paths INTERSECT fresh set),
     replacing the blind --hours 2 window. Replay vs 5a20dcd flags 15 (correct);
     old Fix C reported 'clean'.
  All diff old_strings verified vs workspace-sync.md @4c76b43. 3/5 smoke cases
  executed + passed this run. requires_human_judgment=true — operator picks phase.

PROP-113 LIFECYCLE: recommend mark 'partially-implemented' (it closed the
  over-count class, 312->23) and transfer residue=0 criterion to PROP-114.

BACKLOG: healthy. open=4, net_velocity_7d=+66, root-FS free 3024MB. No thresholds.
PRE-FLIGHT: PROP-102 + PROP-108 auto-close both shadow, 0 closures.
