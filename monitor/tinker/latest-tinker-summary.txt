TINKER tinker-2026-06-14T02-40 — Mode 0 (assigned-tinker) -> Mode 4 (PROP)

TOP-PICK: ISS-2725 (major, class=verification). Re-picked for the 3rd consecutive run.

WHAT I FOUND:
- ISS-2725's actual content fix is DONE and verified (PROP-093 C1 widened analyst.md Mode 2b
  filter to accept status=open; analyst-baby has no Mode 2b so nothing to fix there).
- BUT ISS-2725 stays open because the close HNOTE that the 16:57 run filed
  (HNOTE-TINKER-ISS-2725-CLOSE-001, action=close_iss_batch) has NO CONSUMER. The decider
  has no close_iss_batch handler at all — grep proves it's referenced only in tinker.md.
  A decider run (commit 91249dc) passed in between without touching it.

>>> OPERATOR ESCALATION (major, 3-runs-stuck): The PROP-060 close_iss_batch convention was
    shipped without its decider handler ("PROP-058 follow-up" never landed). Every Mode 0
    close tinker files is currently a no-op. Authored PROP-098 to add the handler
    (decider.md End-of-Run Step A0 region, ~25 lines, modeled on the existing
    set_curmudgeon_mode consumed-note pattern). Once PROP-098 lands, the existing ISS-2725
    close HNOTE is consumed automatically and ISS-2725 closes — no re-work needed.

ISS-2725 left status=assigned-tinker (Mode 0 outcome b: auto-closes when PROP-098 applies).

BACKLOG: GREEN. open_issues=22 (info-tier blip up from 8 same-day; net_velocity_7d=+60,
closed_7d=79 vs new_7d=19, 0 aged>=30d, 0 assigned-analyst). No moderate+ trend fires.
