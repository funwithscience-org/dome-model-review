dome-analyst-baby — run 2026-07-13T04:37Z

Mode 1 BAU drain triggered: 2 pending baby-owned tracker items (EXP-664, EXP-665), 0 orphan ISSs.

Items processed (1): EXP-665 (ISS-2973)
Consolidated into: EXP-666 (monitor/analyst/expansions/EXP-666-baby-batch-1.json)

- ISS-2973 (poller CRITICAL pipeline-health finding: config.json's "ai-context"
  page entry pointed at a dead /ai-context URL — HTTP 404, GitHub Pages default
  template, dead since at least 2026-05-05 — while the real AI Context page at
  /context (HTTP 200, 32,918 bytes) was never baselined; every poll had been
  diffing the 404 template against itself and vacuously reporting "unchanged").
  Fixed: corrected the URL in monitor/config.json to /context, re-seeded
  monitor/baseline/07-ai-context.txt from a live fetch (verbatim raw HTML,
  matches the 32,918-byte figure in the ISS description exactly), and did the
  requested independent content-security full-read. Confirmed poller's finding
  of 0 active injection-directive phrases: the live page's old imperative
  "RULES FOR CLAUDE" text now only survives inside a "Historical Internal
  Agent Notes (Superseded)" block rewritten into descriptive past tense, and
  is explicitly disclaimed by two newer sections ("Canonical V51.1 Entry For
  AI" and "2026-04-17 Context Clarification") instructing auditors to treat
  it as archive, not instructions. No change to any WIN, section, or verdict.
  Left the separate, unrelated raw-text/07-ai-context.txt archival snapshot
  (2026-03-12, V50.6, still correctly flagged HIGH for that old snapshot)
  untouched — out of scope for this ISS.

Escalated to analyst (1): EXP-664 (ISS-2972) — PRED-FIELD-2026Q2 fresh verdict
  pass (window closed d=12, requires comparing dome-predicted TTB decay
  against INTERMAGNET + IGRF-14 and setting a new our_verdict). Tracker had
  this marked review_class=verification (decider-step1i-poll-triage), but
  it is verdict-changing prediction work — Mode 1b territory that analyst-baby's
  prompt explicitly reserves for the Opus analyst regardless of the tracker's
  mechanical class field. Reclassified to deep-attack, unclaimed, marker file
  written: monitor/analyst/expansions/escalate-to-analyst-ISS-2972.json.

OBE dismissals: 0
Issue-proposals written: proposal-ISS-2973-resolution.json (patch)

Tests: node test.js — 3837 passed, 0 failed. lint-exp-allocations: PASS.

Budget used: well under 70% threshold — no carry-over to next cycle.

Commits pushed: de81f5a (claim EXP-665 / escalate EXP-664), b80c1c0 (EXP-666
batch + config.json fix + baseline reseed + tracker + proposal).
