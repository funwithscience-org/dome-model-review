{
  "id": "STRANDED-2026-05-31T05-35-55Z",
  "created_at": "2026-05-31T05:35:55.965Z",
  "reason": "EXP-477 P4 accuracy-denominator text fix targets build-scripts/generate-html.js (NEVER_PUSH file). Decider cannot self-apply. Operator must apply manually.",
  "requires_human_to_apply": true,
  "related_iss": "ISS-2351",
  "related_review": "monitor/curmudgeon/reviews/SEC-part1-sublabel-and-accuracy.c1.json",
  "do_not_treat_as_actionable": false,
  "operator_action": "Apply the find/replace below to build-scripts/generate-html.js, then run node build.js html && node test.js, then commit+push.",
  "per_file_diffs": {
    "build-scripts/generate-html.js": {
      "find": "the silent-failure set is the list of claims the dome has visibly removed or suspended but excluded from its own accuracy denominator",
      "replace": "the silent-failure set is the list of claims the dome has removed from its predictions page or suspended without counting as failures in its own accuracy denominator",
      "context": "In the accuracy denominator hover text template, around line that renders the silent-failure description"
    }
  },
  "broader_strawman_note": "After applying P4, also see ISS-2373 (major) for systematic audit of [REMOVED]/[SUSPENDED] bracket framing across all of docs/index.html — 6 additional occurrences beyond this one paragraph.",
  "verification": "After applying: grep -c \"removed from its predictions page or suspended without counting as failures\" build-scripts/generate-html.js should return 1; grep -c \"visibly removed or suspended\" build-scripts/generate-html.js should return 0. Run node build.js html to regenerate docs/index.html, then verify same conditions there."
}