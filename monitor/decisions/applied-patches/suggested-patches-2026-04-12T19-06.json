{
  "generated_at": "2026-04-12T19:30:00Z",
  "patches": [
    {
      "issue_id": "ISS-750",
      "win_id": null,
      "file": "data/sections.json",
      "field": "part1.html",
      "find": "<h3>The 0.20 coefficient: fitted, not derived</h3>\n<p>The coefficient 0.20 in the formula has no physical derivation. The model page describes it as a \"V13 optimization\" — meaning it was adjusted to reduce distance errors. No property of the aetheric medium, no wave-propagation analysis, no optical experiment produces 0.20. It is a free parameter tuned to match known distances.</p>\n\n<p>This matters because a free coefficient in a correction formula can always be adjusted to improve one measurement while degrading others. The dome's own coordinate page shows this: the V13 system achieves 6.2% RMSE on cross-equatorial routes (using 0.20), but what happens if you change it to 0.25? Or 0.15? The page never reports a sensitivity analysis. Without one, we cannot know whether 0.20 is a unique optimum dictated by physics or an arbitrary choice that happens to minimize error on a particular set of calibration routes.</p>\n\n",
      "replace": "<h3>The 0.20 coefficient: fitted, not derived</h3>\n<p>The coefficient 0.20 in the formula has no physical derivation. The model page describes it as a \"V13 optimization\" — meaning it was adjusted to reduce distance errors. No property of the aetheric medium, no wave-propagation analysis, no optical experiment produces 0.20. It is a free parameter tuned to match known distances.</p>\n<p>The dome's own Height Table still lists values computed from an earlier coefficient (~0.27): n = 3.49 at the ice wall and n = 28.8 at r = 40,000 km, versus the current formula's 2.84 and 21.53. The table was not updated when the coefficient changed — creating 19–31% internal discrepancies (<a href=\"#part2\" onclick=\"showTab('selftest');return false\">Section 2.9</a>). More revealing: the equatorial ring table value n = 1.40 requires a coefficient of ~0.097 — inconsistent with <em>both</em> the current 0.20 and the older ~0.27. This suggests the table was assembled piecemeal from multiple formula versions calibrated at different radii, each fit independently to match a particular route.</p>\n<p>This matters because a free coefficient in a correction formula can always be adjusted to improve one measurement while degrading others. The dome's own coordinate page shows this: the V13 system achieves 6.2% RMSE on cross-equatorial routes (using 0.20), but what happens if you change it to 0.25? Or 0.15? The page never reports a sensitivity analysis. Without one, we cannot know whether 0.20 is a unique optimum dictated by physics or an arbitrary choice that happens to minimize error on a particular set of calibration routes.</p>\n\n",
      "rationale": "EXP-083 integration: adds coefficient change history (~0.27→0.20), equatorial ring n=1.40 anomaly (requires c=0.097, matches no dome version), and table-vs-formula discrepancy as evidence of piecemeal fitting. Defense_survives reduced from 3 to 1."
    }
  ]
}
