MORNING BRIEFING — 2026-05-08
Generated: 2026-05-08T06:10:00Z
Mode: DEGRADED — bash workspace out of disk space
Previous decider run: 2026-05-07T21:20:10Z

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  CRITICAL INFRASTRUCTURE ALERT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BASH WORKSPACE IS OUT OF DISK SPACE.
Error: "useradd: No space left on device" — the sandbox can't even create a user, so ALL shell commands fail.

Root cause: Accumulated git clone directories (dome-review-clean and possibly others) have filled the /sessions/ filesystem. Each failed or aborted decider run since the last successful cleanup left a clone behind.

What this means:
  ❌ git clone impossible → no fresh pull from origin
  ❌ node build.js html → HTML not rebuilt (docs/index.html is stale)
  ❌ node test.js → tests could not be run
  ❌ git push → cannot commit or push directly

OPERATOR ACTION REQUIRED:
  1. The /sessions/ volume needs to be freed. This likely means the disk cleanup
     is needed at the infrastructure level (Cowork/scheduled-task sandbox reset or
     session garbage collection).
  2. Alternatively, the stale clone dirs (dome-review-clean, dome-curmudgeon-clone,
     dome-sync-clone) are accumulating across sessions. If you can delete these
     via a fresh Cowork session with adequate disk, do so.
  3. Once bash is restored, immediately run: cd dome-review-clean && node build.js html
     to rebuild docs/index.html from the patched data files.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHAT HAPPENED THIS RUN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Despite the disk failure, 2 patches were applied directly to FUSE files.
workspace-sync (hourly) will push these to git. HTML rebuild needed separately.

Patch 1 — ISS-1877 TLDR fix (MAJOR → partially fixed):
  File: data/sections.json
  Change: "(E-PRED-C, timing lag)" → "(E-PRED-C, gravity null)"
  Rationale: E-PRED-C is a gravity null prediction (SG gravimeters 0.0 µGal), not
  a timing lag. The TLDR was factually wrong — readers see TLDRs first.
  Remaining: ISS-1877 body-prose work (anchor E-PRED label-presence to 2026-03-12
  snapshot) is still open and needs an analyst EXP (EXP-292 analogy for SEC-4.2.4).

Patch 2 — EXP-293 integration (ISS-1332 major → fixed-pending-verification):
  Files modified: data/wins.json (WIN-064 detail_extra annotation)
  Files created: raw-text/02b-wins-V51-win064.txt (V51-era WIN-064 verbatim archive)
  Change: Added <span class="archive-cite"> citation after the two V51 dome quotes
  in WIN-064 detail_extra, citing the new archive file + MD5 hash corroborated by
  34 consecutive byte-identical poller hashes through 2026-05-07T11:05:55Z.
  ISS-1332 status: fixed-pending-verification (verification_pattern: grep for
  'raw-text/02b-wins-V51-win064.txt' in origin/main:data/wins.json).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SITE HEALTH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Build: ⚠️ STALE — build.js could not run; docs/index.html not updated
Tests: ⚠️ SKIPPED — bash unavailable
Data files: ✅ Patched correctly in FUSE (awaiting workspace-sync push → git)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
EXTERNAL STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Dome changes: NONE (per last poller note — all 7 pages byte-identical, 34th
  consecutive no-change poll as of 2026-05-07T11:05:55Z)
Manual-commit drought: >20 days (crossed 20d threshold at 2026-05-07T12:02Z)
Canary status: UNCHANGED — OTS wrong-side error still not fixed by dome author
8 April-EOM PREDs: d=8.46+ past close — NOW IN HIGH-BAND per Step 10b.
  Next HOLD (#17) expected ~2026-05-08T07:30Z. Analyst should pre-position for
  HIGH-band dedicated chg-* when HOLD #17 fires.
PRED-105: tacit_withdrawal locked

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INTERNAL STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Open issues: 236 (critical: 0, major: 10 approx, rest moderate/minor)
Issues patched this run: 2 (ISS-1877 TLDR partial, ISS-1332 full)
Issues formally closed this run: 0 (bash required for closed-issues.json move)
Curmudgeon queue: 0 items (BAU mode, interval: 30 min per schedule_state)
Queue pop: N/A — queue was already empty; no reviews to process via FUSE
EXP-294 (ISS-1875): status=pending, still assigned to analyst
EXP-190 (Scoreboard): status=draft_awaiting_human_group_review — still on hold

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOP 3 PRIORITY ACTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. [OPERATOR — URGENT] Free disk space on the bash sandbox. All scheduled agents
   that use bash (decider, curmudgeon, analyst, workspace-sync) are at risk of
   similar failures. Delete stale clone dirs (dome-review-clean, dome-curmudgeon-clone,
   dome-sync-clone) and ensure cleanup steps run at end of each agent session.
   After freeing disk: run `node build.js html` in a fresh clone to rebuild
   docs/index.html from the patched data/sections.json and data/wins.json.

2. [ANALYST] EXP-294 (ISS-1875, MAJOR) — OTS cross-page tension: Timestamp Error
   tab vs OTS WIN prose. Evaluate Option A vs B and propose fix path. This is the
   most critical pending analyst work.

3. [ANALYST] HIGH-band PRED escalation: 8 April-EOM PREDs are now at d>8.0 (HIGH
   band). Analyst should write HIGH-band dedicated chg-* when HOLD #17 fires
   (~2026-05-08T07:30Z per last poller estimate). PRED-105 at d>20 also needs
   verdict check.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CURMUDGEON QUEUE STATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
mode: bau
depth: 0
current_schedule_interval_minutes: 30 (schedule_state shows fast intervals still
  set from 2026-04-17 churn-and-burn period; queue is empty so no work to do but
  BAU mode with 30-min intervals means curmudgeon still runs every 30 min)
action_taken: none (queue already empty, no pop needed this run)
schedule_action_needed: review-intervals — mode=bau but intervals still at fast
  (30/30/60 min). Human should restore to BAU defaults when convenient.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WORKSPACE-SYNC DEPENDENCY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Files modified this run (pending workspace-sync push to git):
  - data/sections.json  (ISS-1877 TLDR fix)
  - data/wins.json      (EXP-293 citation annotation)
  - raw-text/02b-wins-V51-win064.txt  (NEW — V51 WIN-064 archive)
  - monitor/decisions/open-issues.json  (ISS-1332 + ISS-1877 status updates)
  - monitor/status.json  (this run's state)
  - monitor/decisions/morning-briefing.txt  (this file)

workspace-sync should push all of these within 1h of this run.
HTML rebuild (node build.js html) MUST happen before site reflects the sections.json
and wins.json changes in the rendered page.
