{
  "generated_at": "2026-04-13T01:45:00Z",
  "patches": [
    {
      "issue_id": "ISS-769",
      "win_id": null,
      "file": "data/sections.json",
      "section_id": "part2b",
      "field": "html",
      "find": "<p><strong>What the code actually does.</strong> We examined <code>monitor.py</code> directly at commit <code>8e4bf9b</code> (V51.0, dated 2026-04-05). The script defines 39 monitoring domains — each a dictionary with <code>predicted</code>, <code>observed</code>, <code>error_pct</code>, <code>tolerance_pct</code>, and <code>pass</code> fields. We classified each domain and cross-referenced to the corresponding WIN. Of the {{CA_REVIEWED}} WINs we have audited, {{CA_HARDCODED}} use hardcoded validation — both the predicted value and the observed value are static within the code, so the check confirms that a fixed number equals itself. For example, the Schumann Frequency domain (WIN-002) reads: <code>predicted: 7.83, observed: 7.83, error_pct: 0.0, pass: True</code>. The SAA Decay Rate domain reads <code>predicted: 77.0, observed: 77.0, pass: True</code>. Even domains that compute a prediction from dome constants (e.g., the Tesla domain computes <code>VA/(2*DISC_R)</code> where both are locked parameters) compare against a static observed value (<code>1/0.08484</code>), so neither side varies between runs. Another {{CA_NONE}} have no validation code at all; they appear in the WIN list but <code>monitor.py</code> contains no corresponding domain. Only {{CA_LIVE}} of {{CA_REVIEWED}} actually fetch live data from external sources.</p>",
      "replace": "<p><strong>What the code actually does.</strong> We examined <code>monitor.py</code> directly at commit <code>8e4bf9b</code> (V51.0, dated 2026-04-05). The script defines 39 monitoring domains — each a dictionary with <code>predicted</code>, <code>observed</code>, <code>error_pct</code>, <code>tolerance_pct</code>, and <code>pass</code> fields. We classified each domain and cross-referenced to the corresponding WIN. The results: <strong>{{CA_NONE}} of {{CA_REVIEWED}} reviewed WINs have no validation code at all</strong> — they appear in the WIN list but <code>monitor.py</code> contains no corresponding domain. Over half the dome's \"confirmed predictions\" are not monitored by any mechanism. Of the remainder, {{CA_HARDCODED}} use hardcoded validation — both the predicted value and the observed value are static within the code, so the check confirms that a fixed number equals itself. For example, the Schumann Frequency domain (WIN-002) reads: <code>predicted: 7.83, observed: 7.83, error_pct: 0.0, pass: True</code>. The SAA Decay Rate domain reads <code>predicted: 77.0, observed: 77.0, pass: True</code>. Even domains that compute a prediction from dome constants (e.g., the Tesla domain computes <code>VA/(2*DISC_R)</code> where both are locked parameters) compare against a static observed value (<code>1/0.08484</code>), so neither side varies between runs. Only {{CA_LIVE}} of {{CA_REVIEWED}} actually fetch live data from external sources.</p>",
      "rationale": "EXP-089: Restructure 'What the code actually does' paragraph to lead with the most shocking finding — that {{CA_NONE}} WINs (majority) have no validation code at all. Previously, hardcoded validation was presented first. Cognitive primacy: leading with 58.6% unmonitored changes reader takeaway. Source: analyst EXP-089 HOL-COMPLETENESS fix."
    },
    {
      "issue_id": "ISS-830",
      "win_id": null,
      "file": "data/sections.json",
      "section_id": "part2b",
      "field": "html",
      "find": "Twelve are misleading (duplicated, circular, or non-discriminating).",
      "replace": "{{TALLY_MISLEADING}} are misleading (duplicated, circular, or non-discriminating).",
      "rationale": "ISS-830: Hardcoded count 'Twelve' is stale — wins.json currently has 22 WINs with verdict=Misleading, all with monitoring=none. Replace with template variable {{TALLY_MISLEADING}} so this auto-updates."
    }
  ]
}
