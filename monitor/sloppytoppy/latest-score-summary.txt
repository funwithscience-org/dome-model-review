=== Sloppytoppy-Score run 2026-05-21T02:36:06Z — ABORTED ===
Run ID: sloppytoppy-score-2026-05-21-1-ABORTED
Rubric version: sloppytoppy-rubric-v1
Status: ABORTED — no scoring performed, no git push.

⚠️⚠️⚠️ CRITICAL: GIT ORIGIN/MAIN IS CATASTROPHICALLY TRUNCATED ⚠️⚠️⚠️

The previous workspace-sync run committed a mass-deletion that has not been
reverted. Origin/main HEAD = df96a76, and the offending commit is:

    ea785c4  Workspace sync: 2026-05-21T02:11:18Z
    Author:  steve <russelst@melrosecastle.com>
    Stat:    4715 files changed, 274 insertions(+), 14,904,949 deletions(-)
    Parent:  1d25627 (workspace-sync: curmudgeon tracker/review updates, sloppytoppy score summary)
    Body:
      "Auto-committed workspace-only files from FUSE mount.
       Files from: analyst, curmudgeon, poller, decider, integrity, data (universal-pusher).
       Note: disk space constrained this run (sessions FS 100% full, root FS 95%).
       Used no-checkout clone + mtime-only guard. monitor/integrity/ workspace-sync-runs
       and verify-pending-run files skipped (606MB dir, only new run report synced).
       docs/index.html not regenerated (node not available without checkout)."

What survives at origin/main (14 files total):
  data/wins.json
  monitor/analyst/latest-analysis-summary.txt
  monitor/changes/chg-20260520-2309-001.json
  monitor/changes/latest-poll-summary.txt
  monitor/curmudgeon/priority-queue-archive.jsonl
  monitor/curmudgeon/priority-queue.json
  monitor/curmudgeon/reviews/WIN-012.c9.json
  monitor/decisions/latest-decider-summary.txt
  monitor/decisions/open-issues.json
  monitor/integrity/alerts.txt
  monitor/integrity/latest-integrity-summary.txt
  monitor/integrity/workspace-sync-runs/run-2026-05-16T09-16-28Z.json
  monitor/integrity/workspace-sync-runs/run-2026-05-21T02-11-44Z.json
  monitor/status.json

What is MISSING from origin/main but present on FUSE (verified intact):
  CLAUDE.md, README.md, SESSION-CONTEXT.md, build.js, test.js
  build-scripts/* (all infrastructure: generate-html, digest-reviews, etc.)
  data/sections.json, data/predictions.json, data/uncounted-failures.json,
    data/kill-shots.json, data/wins.json.bak
  docs/* (index.html, assets/, fonts/, llms.txt, robots.txt, sitemap.xml)
  monitor/prompts/* (ALL agent prompts — decider, analyst, curmudgeon,
    poller, integrity, social, tinker, workspace-sync, sloppytoppy-score,
    sloppytoppy-rewrite, analyst-baby, curmudgeon-verify)
  monitor/prompts/reference/* (ALL rubrics and reference docs)
  monitor/sloppytoppy/* (scores.json with 484 records, rubric-config.json,
    math-dense-surfaces.json, content-dense-surfaces.json, rewrites/, punts/,
    calibration-audits.jsonl, rewrite-attempts.json)
  monitor/analyst/new-wins/, expansions/, category-proposals/,
    globe-fingerprints/, issue-proposals/, expansion-tracker.json,
    attention-inbox.json, human-notes.json, processed-proposals.json
  monitor/curmudgeon/reviews/* (except WIN-012.c9.json),
    tracker.json, pending-digest.json
  monitor/decisions/closed-issues.json, applied-patches/, decider-mode.json,
    closure-ledger.jsonl, prop-009-enforce.flag, human-notes.json
  monitor/integrity/workspace-sync-skips.jsonl, workspace-sync-runs/* (older)
  monitor/social/, monitor/tinker/, monitor/changes/* (other change files),
    monitor/scripts/

Why I aborted this run:
  - My rubric reference file is gone from git (monitor/prompts/reference/
    sloppytoppy-rubric.md exists only on FUSE).
  - My input data is gone from git (data/sections.json, scores.json).
  - Committing a score update on top of the truncated tree would only add
    noise to recovery — the operator's revert path (force-push to 1d25627
    or git revert ea785c4) needs a clean slate.

Why this is recoverable:
  - FUSE workspace mount holds the full pre-disaster tree (verified above).
  - The disaster is in workspace-sync's no-checkout + mtime-only fallback,
    which interpreted "files not present in the no-checkout area" as
    deletions instead of skips.
  - The disk-space pressure note in the commit body (sessions FS 100% full,
    root FS 95%) is the root cause — workspace-sync improvised an unsafe
    fallback to avoid OOM-disk.

Recommended recovery (for operator / tinker / decider, NOT for me to do):
  1. Free disk space first (/tmp has dome-sync-clone 72M, kev-publish 80M
     leftover from prior sessions — operator-side discipline per CLAUDE.md).
  2. Revert: 'git revert ea785c4' (preserves df96a76 small history line)
     OR 'git reset --hard 1d25627 && git push --force-with-lease origin main'
     (cleaner but rewrites two commits).
  3. After revert, verify scores.json is 484 records intact and FUSE↔git
     diff is empty.
  4. Patch workspace-sync.md to refuse no-checkout + mtime-only fallback
     under disk pressure — fail loud instead of fail-deletion. (PROP candidate
     for tinker.)

Surfaces scored this run: 0
  First-time: 0
  Re-scored: 0

Carry-over to next run: TBD (cannot enumerate without FUSE-sourced
  sections.json and rubric — both intact on FUSE but normal scoring flow
  reads from clean clone which is git-canonical, currently broken).

Length overrides applied: 0
Math-dense surfaces in batch: 0
Content-dense surfaces in batch: 0

Worst-scoring surfaces this batch: N/A (run aborted)
Best-scoring surfaces this batch: N/A (run aborted)
Next-run carryover (first 10): N/A (run aborted)

Calibration baseline scores: N/A (run aborted)

Possible prompt injection: NONE observed (no scoring done).

End of aborted-run report.
