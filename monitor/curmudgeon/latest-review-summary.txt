curmudgeon-2026-05-31T17:45 review summary

QUEUE ITEM: qid=428, EXP-481 (section-rewrite, deep-attack), pushed 2026-05-31T14:49:09Z
REVIEW: monitor/curmudgeon/reviews/EXP-481-proposal.json (23.8 KB)
VERDICT: current_verdict_holds=true, recommended_action=minor_edit, confidence=0.84
HOLES: 6 (3 moderate, 3 minor; 0 critical/major)

CONTEXT
  EXP-481 is the followup patch to ISS-2304 / SEC-part2b-count-reconciliation.c1 hole 4.
  EXP-437 landed the {{TOTAL_WINS}}/{{CATALOG_TOTAL}}/{{COLLISION_COUNT}} placeholder
  discipline at 5 sites but its audit pass scanned only existing placeholders, missing
  bare numerals. EXP-481 introduces a third frame — {{DOME_CONFIRMED_HEADLINE}} sourced
  from data/uncounted-failures.json:dome_claimed_confirmed (new field, default 69) — for
  the dome's published homepage headline (distinct from {{TOTAL_WINS}}=70 our-enumeration
  and {{CATALOG_TOTAL}}=72 our-catalog-with-collisions). Proposes: 1 schema field +
  4 generate-html.js edits + 1 _meta.placeholders entry + 14 sections.json prose patches,
  with 8 explicit stay-literal sites enumerated.

MECHANICAL VERIFICATION (kernel of truth)
  All 14 P1-P14 current_clause fingerprints match exactly once in data/sections.json
    at the expected paths (independent substring walk).
  All 4 build_pipeline_changes current strings match exactly once in
    build-scripts/generate-html.js (lines 1291, 1455, 113 in HEAD; EXP hint
    line-numbers off by ~13-52 lines but hedged with "~").
  Independent \b69\b scan of data/sections.json finds 23 instances; 16 are targeted
    by P1-P14 (some patches touch 2x), 7 are enumerated stay-literals plus _meta
    description that change_6 addresses — full coverage of sections.json bare-69s.
  Integration order (changes 1-5 first, then P1-P14 any order) is correct.

MODERATE HOLES (decider should resolve before pushing patches)
  1. Recursive audit-coverage gap. EXP-481 thoroughly scans data/sections.json for
     bare 69s but does NOT scan data/uncounted-failures.json (the very file it modifies
     as change_1). That file has dome_accuracy_formula: "69 / (69 + 4)" — two bare 69s
     and a bare 4. After EXP lands, dome_claimed_confirmed: 69 sits alongside the
     stale formula. The exact anti-pattern this EXP exists to fix. Mitigating: the
     formula has 0 grep hits in render code — unrendered. Fix: drop the field, OR
     replace its value with self-doc string referencing the new placeholder fields.
  2. test_recommendations item 1 (regression test) is unimplementable — no expected
     bare-69 whitelist count is supplied. Decider must either compute it (~16-18
     expected post-integration) or restructure the test as a fixture of expected
     stay-literal contexts.
  3. anticipated_objections #3 overclaims refresh-agent automation. Evidence:
     uncounted-failures.json last_updated 2026-04-29 (a month stale); poller.md
     row 341 watches the count but does NOT write to this file; monitor/status.json
     does NOT carry the watched dome-confirmed count — verified. The drift-detector
     test (test_recommendations item 4) has no data source. Fix: honest-reframe #3 to
     human-curated, OR wire poller→uncounted-failures.json in a sibling EXP.

MINOR HOLES
  4. holes_NOT_addressed_here item 4 undercounts generate-html.js Timestamp Error tab
     bare-69 sites (4 actual at lines 1981/1987/1995/2001 vs 2 claimed). Followup EXP
     should know it's 4.
  5. change_6 (placeholder catalog hygiene) marked "Optional" but should be required —
     leaving the _meta.placeholders.{{HONEST_ACCURACY}} description with bare 69s
     contradicts the discipline this EXP establishes.
  6. mentions_left_unchanged item 2 defer-to-followup reasoning is underdeveloped:
     dome_claimed_prospective_count and domeProspectiveCount already exist; the
     three-class breakdown is 2/3 plumbed already.

STRONGER ARGUMENT NOT MADE BY EXP-481
  The recursive-audit-coverage critique (hole 1) is the obvious adversarial frame
  for any audit-coverage EXP and was missed by the EXP's own anticipated_objections
  (which covers 7 critiques). A capable adversary applies the criticism recursively.

CLASS CONFIRMED CORRECT
  deep-attack was the right class. verification-mode (PROP-038 narrow Sonnet) could
  not have surfaced moderate holes 1-3, which are structural rather than line-item.

CARRY-OVER
  ISS-2304 / SEC-part2b-count-reconciliation.c1 hole 4 — EXP-481 IS the patch for it
  (not a carry-over of a hole into this review; the EXP itself is the carry-over
  response). Recorded in unresolved_prior_cycle for decider M3 audit trail.

NEXT STEPS
  Decider should:
    (a) integrate build_pipeline_changes 1-5 with hole 1 fix folded into change_1
        (recommend dropping dome_accuracy_formula entirely);
    (b) compute or punt on the test_recommendations item 1 expected count;
    (c) decide between honest-reframe (cheap) or sibling-EXP (real fix) for hole 3
        anticipated_objections #3 framing;
    (d) integrate P1-P14 unchanged;
    (e) promote change_6 from optional to required;
    (f) file a separate EXP for the 4 generate-html.js Timestamp Error sites (not 2).
