WIN-036 Cycle 5 Adversarial Review — 2026-05-07

Trigger: priority-queue item 308 (decider, pushed 2026-05-07T09:18:06Z)
Reason: EXP-291 integrated — single-sentence insertion into detail_evidence to
preempt the c3 advocate steel-man defense (defense_survives=3 → expected 1-2)
on the dome's "we predicted the trajectory before WMM2025" line.

VERDICT HOLDS: "Std Model Explains" (confidence 0.92)
DEFENSE_SURVIVES: 3 → 2 (upper end of EXP-291's predicted drop)
PATCH LANDED: Yes — detail_evidence grew 925 → 1154 chars exactly as specified
in EXP-291.replacement_word_count.

Tag set unchanged from c4 (monitoring=hardcoded, relabels_standard=true,
post_hoc=true, derives_from_dome=false). All four tags continue to hold.

KERNEL OF TRUTH: EXP-291's inserted sentence is a real and substantive
improvement. The c3 steel-man — "our exponential trajectory was committed
before WMM2025 was published" — exploited a real gap: nothing in the prior
prose explicitly demanded a divergent prospective prediction as the test of
independence. The new sentence closes that gap by naming the discrimination
criterion ("distinguishable predictions at any future epoch") and observing
it has not been demonstrated. Textbook defense neutralization.

HOLES FOUND (2 actionable, 1 cosmetic):

  MAJOR (1) — Cross-page OTS-canary tension PERSISTS post-ISS-1867 closure.
  ISS-1867 was scoped narrowly and only updated WIN-035/036/037/038/039
  prose (NEW-LANG: "status_history.json...per-domain bundles prediction-side
  fields (formula, predicted value, tolerance threshold, source label)
  together with observation-side fields"). The Timestamp Error tab in
  build-scripts/generate-html.js:1832 still reads "OpenTimestamps anchors
  status_history.json, the file containing reference data: observed values,
  pass/fail results, and statistical comparisons. These are observations —
  the outcomes. The prediction parameters — the formulas, expected values,
  and tolerances — live in monitor.py source code and docs/model.html."
  These two characterizations are mutually inconsistent: the WIN prose lists
  "formula" as a status_history.json field; the Timestamp Error tab says
  formulas live ONLY in monitor.py. Third consecutive cycle (c3/c4/c5)
  flagging this hole. ISS-1867's verifier was scoped to data/wins.json grep
  only — it did not enforce cross-page consistency. RECOMMEND: reopen as
  new ISS scoped to either updating the Timestamp Error tab to acknowledge
  prediction-side descriptor fields in status_history.json (option A) or
  reverting WIN prose to high-level framing without the field-list (option B).

  MODERATE (2) — EXP-291 inserted sentence asserts technically imprecise
  common-source claim. The sentence reads "Both NOAA's WMM2025
  secular-variation forecast and the dome's purported exponential trajectory
  are fits to the same public NOAA NP.xy time series." WMM2025 is built from
  Swarm satellite vector measurements and ground observatories via Gauss-
  coefficient inversion (IGRF process); NP.xy is a DERIVED OUTPUT, not the
  INPUT. The dome IS plausibly fit to NP.xy; WMM2025 is not. EXP-291's
  anticipated_objections #3 explicitly acknowledged this and offered
  "constrained by the same NOAA-authored North Magnetic Pole trajectory
  record" as acceptable wording — analyst chose not to adopt it. A
  geomagnetism-literate defender can rebut on input/output grounds. This
  residual is why defense_survives drops 3→2 not 3→1. RECOMMEND: one-
  sentence wording fix, replace "fits to the same public NOAA NP.xy time
  series" with EXP-291 author's own preferred fallback wording.

  MINOR (3) — "secular-variation forecast" used as undefined technical term
  in detail_evidence prose. Not blocking; cosmetic.

CITATION CHECK: All in-repo citations verified. Two unchecked external URLs
(live status_history.json endpoint, NOAA WMM build-pipeline documentation) —
the WIN-036 prose now commits to a position about status_history.json's
contents, so an empirical fetch becomes load-bearing for our own consistency
on the next pass.

CROSS-WIN CONSISTENCY: WIN-035-039 batch is internally coherent post-ISS-1867
sweep (NEW-LANG verified across all five). Cross-PAGE coherence with
Timestamp Error tab is the open issue (hole #1). WIN-031 still uses different
OTS framing — out of scope for this review, worth a separate glance.

QUANTITATIVE VERIFICATION: 8 claims checked, all verified. EXP-291 length
delta exactly 229 chars as predicted. Arithmetic (139.298 − 120 = 19.298;
19.298 − 18 = 1.298) unchanged from c4.

TLDR REVIEW: Both TLDRs unchanged (correct per EXP-291.integration_notes).
Ironically, the TLDRs use the more general "NOAA trajectory data" framing
that AVOIDS the input/output conflation — they are technically more careful
than the new detail_evidence sentence.

RECOMMENDATION: minor_edit. Two scoped patches:
  (a) Update Timestamp Error tab OR revert WIN prose to harmonize OTS framing.
  (b) Replace "fits to the same public NOAA NP.xy time series" with
      "constrained by the same NOAA-authored North Magnetic Pole trajectory
      record" in WIN-036 detail_evidence.

Cycle 6 should be the closing pass on WIN-036.

Review file: monitor/curmudgeon/reviews/WIN-036.c5.json
