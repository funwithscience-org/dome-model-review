EXP-451 PROPOSAL REVIEW — HOL-STRESSTEST c2 falsifiability-hardening (qid=421)
================================================================================
Reviewed: 2026-05-30 (cycle 3 / first proposal review on this EXP)
Target: 5 prose patches (P1-P5) + 3 deferred items targeting docs/index.html
Mapped to: ISS-2284 (major), ISS-2285 (major), ISS-2286 (moderate)
Verdict: minor_edit — conditional approval, 2 blocking defects need decider fix

BLOCKING (must fix before operator-apply):

1. P1.sublabel_change.find_text DOES NOT MATCH the actual ds-sc-sublabel for
   Survive Scrutiny on line 704. The actual text wraps "What would change this?"
   in an <a href="#eg-falsifiability" onclick="showTab('evaluate');return false">
   anchor. The patch find_text omits the anchor entirely. Verbatim apply either
   silently fails or strips the cross-tab navigation link. Fix: include the
   anchor in find_text+replace_text, or narrow find_text to end before the anchor.

2. P4 replace_text INVENTS A BRACKET-MARKER CONVENTION the dome does not use.
   The patch claims silent failures are "visibly marked [REMOVED] or [SUSPENDED]."
   grep of raw-text/: zero [REMOVED] occurrences; SUSPENDED appears only once as
   a section header "=== SUSPENDED — Under Revision ===" in 03-predictions.txt.
   Dome's actual surface signals: "FALSIFIED", "refined", "PENDING-PROSPECTIVE",
   prose-level removals. A dome defender screenshotting his front page can
   falsify the claim, STRENGTHENING the symmetric-methodology critique the patch
   was meant to neutralize. Fix: use the c2 reviewer's own suggestion (window-
   closure+silence) or name the 4 entries by their actual dome-side action.

NON-BLOCKING (decider may self-apply or defer):

3. P2 worked example mischaracterizes 22.6 Hz as "the dome cavity treated as a
   flat-plate resonator." The 22.6 Hz value comes from linear radial averaging of
   H(r), not from the dome cavity at H(0) (which gives 8.77 Hz). Recommendation:
   use 8.77 Hz pole-floor instead, or annotate the averaging scheme explicitly.

4. P3b updates the visible sublabel but NOT the surrounding aria-label on the
   same div (line 446). Screen-reader users miss the "~1 nT" detail. Fix: add
   a sub-patch updating the aria-label too.

5. P2 anchor line drift: claim "line 678", actual line 669. Drift suggests
   stale local build. Pure-insertion patch still applies correctly.

6. P3a caption rewrite weakens "is sufficient to upgrade" to "if any one is met"
   — minor sufficiency-language loss. Optional polish.

DESIGN STRENGTHS (preserved):
- 5 patches map 1:1 onto the 5 c2 holes (cleanest coverage I've seen)
- No WIN verdict changes; all 70 verdicts preserved
- Cross-reference overlap with HOL-NARRATIVE c2 (P5) and HOL-TAXONOMY c2 (DEFER-01) correctly flagged
- DEFER-01 (verdict-category placeholder) sound risk management given eclipse
  resolves ~Aug 2026 (~10 weeks out)
- Three deferred items appropriately surfaced for operator decision

NEXT STEP for decider:
Self-apply minor edits to P1+P4 (and optionally P2+P3b aria-label fix), then
re-push the patched EXP to operator-apply. Three deferred items remain on the
operator's table per analyst recommendations.
