CURMUDGEON REVIEW — 2026-05-30 (queue_id 418, SEC-part2b-stratification-paragraph c1)
=====================================================================================

Item: Part 2b §2b.1 stratification paragraph (EXP-441 integration)
Trigger: priority-queue, class=deep-attack, FIFO position 3 of 4 (416 and 417 already reviewed)
Verdict: current_verdict_holds=false. recommended_action=major_rewrite.
Holes: 2 critical, 2 major, 3 moderate.

THE HEADLINE:
EXP-441 was authored to ELIMINATE the visible-arithmetic-failure mode in §2b.1's
stratification paragraph (old version visibly summed to 29 ≠ 43). The integration
shipped the templating but NOT the required build_invariant assertion, and the
templating itself uses inconsistent filter contracts. Live result on the deployed
site as of 2026-05-30: the prose reads "sum to 43" while the visible bucket counts
sum to 45 (2 unfalsifiable + 12 self-contradicted + 10 misleading + 12 std-model +
8 refuted + 1 not demonstrated = 45). The exact failure mode the EXP existed to
prevent has been re-introduced in a new form.

ROOT CAUSE:
  - {{CA_NONE}} computed via reviewed.filter(monitoring==='none') where
    reviewed = baseWins.filter(reviewed-flag=true) — excludes sub-WINs (057b, 058b)
    and unreviewed entries. Today: 43.
  - {{CA_NONE_X}} (six bucket placeholders) computed via raw wins.filter(...) —
    includes 057b (Std Model Explains) and 058b (Not Demonstrated). Today's
    bucket sum: 45.
  - EXP-441 required_template_additions.build_invariant explicitly mandated a
    build-time sum-check assertion ("fail the build if not"). Never implemented.
    grep -rn 'sum.*CA_NONE|sumCheck|assertSum' build-scripts/ build.js test.js
    returns zero hits.

CRITICAL HOLES (2):
  1. Visible arithmetic failure (43 vs 45) live on the deployed site.
  2. Filter-contract mismatch makes the sum-to-{{CA_NONE}} invariant
     structurally unenforceable as built.

MAJOR HOLES (2):
  3. Required build_invariant assertion missing entirely.
  4. "1 not demonstrated" has no ID list (refers to 058b — sub-WIN excluded
     from {{CA_NONE}} denominator).

MODERATE HOLES (3):
  5. WIN-057b cited in CA_NONE_STDMODEL_IDS but reviewed=false in wins.json.
  6. "Monitoring would expose the contradiction" overclaims for derivational
     SC WINs (mathematical inconsistencies, not empirically detectable).
  7. Closing sentence attributes intent ("avoids") where availability could
     equally explain the pattern. Relabelling paragraph handles this, but
     stratification paragraph by itself doesn't carry the load.

KERNEL OF TRUTH:
The structural argument is correct — within monitoring=none, the verdict
distribution genuinely is heavily Self-Contradicted / Refuted, and 29/43 also
relabel standard physics. These survive the review. The holes are entirely in
the implementation plumbing.

DECIDER GUIDANCE:
Fix is small (~10 lines in build-scripts/generate-html.js): change caNoneX
filters at lines 1392-1404 to use `reviewed` instead of `wins`, then add a
sum-check throw at line ~1404. Bundle into one patch — these are structurally
the same fix. Adjacent EXP-442 (queue_id 419, contrast paragraph) uses the
same SC_TOTAL/SC_MON_* template family and has the same risk pattern; verify
fix lands for both in one commit. Prose itself does not need to change.

QUEUE STATUS AFTER THIS RUN:
  - 416 (part2b-count-reconciliation): reviewed (c1, pre-existing)
  - 417 (overview-timestamp-reading-guide): reviewed (c1, pre-existing)
  - 418 (part2b-stratification-paragraph): REVIEWED THIS RUN
  - 419 (part2b-contrast-paragraph): unreviewed, decider will surface
    on next push
