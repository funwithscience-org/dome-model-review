CURMUDGEON RUN 2026-05-11T06:40Z — WIN-013.c6 (verification class, queue_id 331)

QUEUE STATE AT RUN START (3 items):
  1. queue_id 333 — p6-614-kappa-cluster (section-new, deep-attack) — deferred (see queue-id housekeeping below)
  2. queue_id 331 — WIN-013 (win-detail-rewrite, verification) — PROCESSED this run
  3. queue_id 332 — WIN-028 (win-detail-rewrite, deep-attack) — deferred (singleton class; next run)

WHY ONE ITEM: gate 1 (class-based batching) only batches 'verification' items. 332 is deep-attack → singleton.
333 was effectively re-reviewed at 05:18Z (SEC-6.14-kappa-cluster.json carries queue_id:330) but PROP-009 strict
match fails because the queue's queue_id was rewritten 330→333 at 05:56Z. Deferred to decider with a clear
housekeeping note rather than write a redundant c2 of section content reviewed <2h ago.

WIN-013.c6 SUMMARY:

  PATCHES VERIFIED LANDED:
    [OK] Commit 2916dad — ISS-1944 (Van Camp 2001 DOI 404) annotated as unverifiable in WIN-013 detail_evidence.
         Conservative annotate-but-retain approach is the right call.
    [OK] Commit 2d76e6a — tldr_evidence widened ~5–10 nT → ~5–15 nT, closing c5 hole #4.

  NEW MODERATE FINDING (introduced by the followup patch, not present before):
    The widened ΔB range 5–15 nT × κ=1.677 = 2.98–8.94 µGal, not the 3–6 µGal still shown in tldr and detail.
    Two acceptable fixes proposed; either works; decider can self-apply.
      (a) widen Δg to ~3–9 µGal in tldr + detail (2 string replacements), OR
      (b) keep 3–6 µGal and add a Strestík-as-Membach-analog clarifying clause.

  current_verdict_holds: true  |  confidence: 0.82 (up from 0.78 at c5)  |  recommended_action: minor_edit

OUT OF SCOPE this run (still open, tracked):
  ISS-1945 (Tomás 2018 URL → WIN-014 / SEC-6.14 scope)
  ISS-1946 (Domain #7 source-code vs Domain 5 dashboard)
  ISS-1947 (Hvoždara ResearchGate-only)

DECIDER ATTENTION:
  queue_id 333 needs manual reconciliation — SEC-6.14-kappa-cluster.json (queue_id 330) covers content but
  PROP-009 strict match fails after the 330→333 reassignment. Either pop 333 referencing the existing review,
  or clarify reassignment semantics in priority-queue schema.

OUTPUT: monitor/curmudgeon/reviews/WIN-013.c6.json  (queue_id:331, cycle:6, trigger:priority-queue-win-rewrite)
TRACKER: WIN-013 reviewed record appended to tracker-archive.jsonl (no live-points entry to remove).
