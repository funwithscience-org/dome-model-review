Curmudgeon run 2026-05-30T09:18Z — qid 419 (SEC-part2b-contrast-paragraph)
================================================================================

Item reviewed: Part 2b §2b.1 contrast paragraph (EXP-442 integration)
Trigger: priority-queue-section-rewrite, class=deep-attack (singleton)
Cycle: 1
File: monitor/curmudgeon/reviews/SEC-part2b-contrast-paragraph.c1.json

VERDICT: current_verdict_holds = true; recommended_action = minor_edit.
The contrast paragraph now renders correct templated counts (20 SC: 12 none + 8
hardcoded). The original HOL-COMPLETENESS.c3 hole 2 numbers are closed.

5 HOLES FOUND (1 major + 3 moderate + 1 minor):

[MAJOR] EXP-442 required_template_additions.build_invariant NOT integrated.
  The decider integrated the four placeholder substitutions
  (build-scripts/generate-html.js:77-80, 1405-1408) but skipped the spec-mandated
  sum-check assertion (SC_MON_NONE + SC_MON_HARDCODED == SC_TOTAL).
  At HEAD this is invisible (12+8=20), but the system is one classification flip
  away from re-opening the prior cycle's hole. This is the THIRD consecutive
  EXP in this decider batch (EXP-437, EXP-441, EXP-442) where the
  build_invariant block was dropped — see sibling reviews SEC-part2b-count-
  reconciliation.c1 hole 1 and SEC-part2b-stratification-paragraph.c1 hole 2.
  Fix: one decider patch adding all three assertions together.

[MODERATE] SC_MON_HARDCODED_IDS rendered with wrong format.
  Spec: slash-joined "WIN-002/012/029/038/045/046/049/067" (single WIN- prefix).
  Implementation: comma-space joined "WIN-002, WIN-012, ..." (8x WIN- prefix).
  Renders correctly factually but breaks the prose rhythm the spec was explicit
  about preserving (required_template_additions rationale).

[MODERATE] list_of_winids helper NOT extracted (EXP-441 + EXP-442 cross-EXP
  spec requirement). The inlined .filter(...).map(w=>'WIN-'+w.id).join(', ')
  pattern is duplicated 8+ times in generate-html.js lines 1399-1408.
  Extracting the helper now closes the slash-format issue at one site.

[MODERATE] Adjacent stale paragraph exposed by EXP-442's templating.
  The "Even the 2 live-fetch WINs" paragraph immediately after says
  "All four data sources" and names WIN-004, WIN-006, WIN-007, WIN-024 — but
  only WIN-006 and WIN-007 currently carry monitoring=live_fetch. The
  contradiction between the templated "2" and the hardcoded narrative is
  glaring. Recommend new ISS / EXP to template this paragraph too.

[MINOR] Mild scope ambiguity in "The contrast with the hardcoded WINs is even
  more revealing" — could read as contrast against the entire hardcoded cohort
  before the next sentence narrows it. Low-priority polish.

PATTERN OBSERVATION (for tinker / decider operator attention):
  Across EXP-437, EXP-441, EXP-442 the decider integrated placeholder
  substitutions but skipped build_invariant blocks 3/3 times.
  Not a one-off — process gap worth investigating in the integration pipeline.

QUEUE STATE: All 4 queue items now reviewed (qid 416, 417, 418 already had
reviews; qid 419 = this review). Decider will pop on its next run.
