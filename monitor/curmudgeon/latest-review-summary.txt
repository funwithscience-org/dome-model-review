CURMUDGEON RUN 2026-05-29T04:17Z — qid=410 SEC-3.1b (EXP-436 NPxy cluster hoist)
================================================================================

Queue state at run: 2 items (qid=410 part3 deep-attack; qid=411 part2b verification).
Per FIFO + class rules: processed qid=410 (deep-attack singleton) only;
qid=411 (verification, small template fix) deferred to next run.

WHAT EXP-436 DID
- Appended a new "Worked example — the NOAA NP.xy cluster" paragraph at the end
  of Part 3.1b (data/sections.json:part3.html, #posthoc-npxy anchor; +2121 chars).
- Replaced the ~1000-char duplicated post-hoc paragraph in WIN-006/007/022
  detail_verdict_text with a ~500-char one-sentence reference linking back to
  #posthoc-npxy (per-WIN compression -1581 chars total).
- WIN-036 EXPLICITLY NOT MODIFIED — separated on OTS-canary grounds; its
  post-hoc failure runs through OpenTimestamps, not the hardcoded-monitor pattern.
- Net char delta across four touched fields: +540 (Part 3.1b grew, per-WIN shrunk).
- Closes ISS-2214 (WIN-006.c6 hole #2: duplicated post-hoc paragraph cluster).

VERDICT: APPROVED WITH MINOR POLISH
recommended_action = minor_edit (not blocking). The structural move is correct
and well-motivated. Tests pass (3693 passed, 0 failed). All anchors and
cross-tab links work. Quantitative claims verified.

HOLES FOUND (4 minor)
1. Opening "same source: NP.xy" overstates source unity — WIN-036 actually
   uses WMM2025, which is a related but distinct US/UK NOAA product. Tighten
   to "same family of NOAA pole-position products (NP.xy and WMM)."
2. WIN-007 compressed reference "the post-1990 acceleration target" is more
   specific than what is actually hardcoded — Domain #11 hardcodes the 130°E
   longitude threshold (trivially passing) and the 55.0×exp(-0.08×(year-2015))
   formula parameters, not an "acceleration value." Either tighten to name
   the actual hardcodes, or revert to the original generic "target number."
3. WIN-022 compressed reference "no monitor.py entry exists for this value at
   all" preserves a pre-existing tension with WIN-022 detail_evidence
   ("Domain #11 in monitor.py uses a hardcoded exponential decay formula").
   Pre-existing — not introduced by EXP-436 — but now more visible because
   the compressed reference is shorter and bolder. Recommend separate ISS to
   reconcile, or clarify "no DEDICATED WIN-022 monitor.py entry."
4. Net char delta +540 means total review size grew, not shrank. The EXP
   anticipated_objections section already addressed this with the TF-IDF
   argument (one argument told once > same argument told three times).
   Defensible but worth flagging in the decider report.

STRONGER ARGUMENTS (for next EXP cycle on this cluster)
- Mention Domain #11 explicitly in the cluster paragraph — it's the shared
  monitor.py domain that services WIN-006/007/022 (and WIN-036 indirectly via
  parent PROS-002). Naming it would ground "hardcoded target" concretely AND
  resolve the WIN-022 "no entry" tension by clarifying "no DEDICATED entry."
- Add IGRF-14 release date (Nov 2024) symmetric to WMM2025's Dec 2024 — both
  are public model products long predating any plausible dome monitor commit.

KERNEL OF TRUTH
EXP-436 is the right move for the right reason. The four-WIN cluster was
litigating-the-same-point-four-times, and a reader experienced it as four
independent post-hoc arguments. Hoisting to Part 3.1b makes the cluster-level
argument visible and lets per-WIN panels carry only WIN-specific evidence.
The explicit separation of WIN-036 (OTS canary + WMM2025 pre-publication) is
also correct — its post-hoc failure routes through a different mechanism.

NEXT-RUN HANDOFF
- qid=410 review complete; decider can pop it on next run.
- qid=411 (part2b verification, ISS-2216 self-patch) ready for next curmudgeon
  fire — looks small (template-glitch fix); verification class; likely <1 KB
  diff. Verify subject-verb agreement, WIN-057 near-miss framing, CA_DOME=1
  rendering.
