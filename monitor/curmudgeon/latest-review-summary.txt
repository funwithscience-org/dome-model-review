Curmudgeon run — dome-curmudgeon scheduled (2026-06-01)
Reviewed: queue_id 433, target audit-exp-tracker-gaps (PROP-053 / EXP-515 deliverables)
Cycle: 1. Verdict: current_verdict_holds=false. Recommended action: major_rewrite.

8 holes found (2 critical, 3 major, 2 moderate, 1 minor):

Critical
  1. Script does NOT read expansion-tracker-archive.jsonl, only live items[].
     Result: 380 reported "orphan_file" entries vs ~60 true orphans (in neither
     live items[] nor archive). 6.3x over-report defeats PROP-053's purpose.
  2. Script does NOT implement the no_trace category required by PROP-053 spec.
     no_trace was the LARGEST category (41 of 94) in the original ISS-2105
     audit; the script never reads next_id or scans 1..next_id, so phantom IDs
     are invisible.

Major
  3. Output schema does not match PROP-053 spec (missing total_slots, present,
     missing_total, missing_ranges, summary, and the no_trace/referenced_only
     by_category keys). Integrity consumers following the spec cannot parse
     actual output.
  4. Reference doc canonizes the archive-blind bug. Claims a "pre-tracker era"
     that does not exist (EXP-001 has an archive entry today). Triage advice
     instructs integrity to ignore 380 alleged orphans, masking real lost-work
     in EXP-001..450 range if it occurs.
  5. PROP-053 implementation_note misdescribes the shipped script: claims
     --summary / --json-out flags (actual: --orphans-only / --json); claims
     severity_hint emission (grep returns zero matches); claims smoke-test
     produced 50 orphans (current head produces 380).

Moderate
  6. Cross-reference scan only walks monitor/analyst/expansions/; spec required
     monitor/decisions/ and monitor/curmudgeon/reviews/ as well.
  7. PROP-053's third deliverable (structure-integrity.md edit) was never
     shipped but PROP status was flipped to "implemented" anyway, hiding the
     residual work from tinker dispatch.

Minor
  8. --orphans-only flag hides tracker_only "complete-status-no-file" lost
     deliverables, contradicting the reference doc's own triage guidance.

Carry-over from prior cycle: none (first review of this target).

Recommendation to decider: file follow-up ISS for archive-aware script
rewrite + reference-doc rewrite + integrity prompt edit. Consider flipping
PROP-053 status from "implemented" back to "partial" until script works
against live data. The shipped artifacts pass the verification_pattern in
PROP-053 (`test -f && test -f`) only because that check confirms presence,
not correctness — a meaningful verification would run the script against a
fixture and compare output.

Review file: monitor/curmudgeon/reviews/audit-exp-tracker-gaps.c1.json
