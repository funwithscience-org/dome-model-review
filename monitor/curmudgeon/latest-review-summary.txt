Curmudgeon run: priority queue, queue_id=426, EXP-462 (pre-integration section-rewrite review)
=============================================================================================

TARGET: EXP-462 — HOL-REDUNDANCY c2 Hole 1 eclipse/Chapman consolidation
QUEUE CLASS: deep-attack (singleton — not batched, per Step 8a gate 1)
EXP STATUS: ready-for-integration (proposal not yet applied; pre-integration review)

RECOMMENDED ACTION: minor_edit — fix two execution-level cross-reference holes before integration.

HOLES FOUND: 2 major, 2 moderate, 2 minor (6 total).

KEY FINDINGS:

[MAJOR #1] P0 cross-references §6.14 for "internal BOU/W004/monitor.py-KAPPA-squared
formula heterogeneity" — but §6.14 does NOT contain the "KAPPA applied twice" finding
(-10.9 × 1.672 ≡ -18.22 baseline with explicit 1.672 multiplier) or the inject_ai_layer.py
PRED-001 Ebro calculation. Verified via grep across data/sections.json: those findings
appear ONLY in §8b.3 L3a #2 (the paragraph being collapsed). If consolidation lands
as-written, these unique findings disappear from the document entirely. The "three
dome-authored formulas coexist" premise underlying the consolidated paragraph's
"selection from coexisting dome formulas rather than forced derivation" claim becomes
an assertion without supporting evidence.

  Recommendation: either restore ~50 words inline in P0's consolidated paragraph naming
  the KAPPA-squared and inject_ai_layer.py findings, OR coordinate a P2 patch that adds
  these findings to §6.14 (between "How κ was derived" and "What κ predicts during
  past eclipses").

[MAJOR #2] P1 §6.14 pointer claims the magnitude-band non-discrimination argument
"lives at §4.2.5" — but §4.2.5 covers three structural problems (no dome geometry
enters formula, Kp<2 quiet filtering, third-attempt widening), NOT the magnitude-band
shared-arithmetic argument. That argument currently lives only in §8b.3 L3a #2 (being
removed) and §2b briefly. The pre-existing §2b stratification paragraph already
commits the same mismatch ("canonical to §4.2.4 and §4.2.5") which is also wrong.

  Recommendation: either re-target P1 to §2b.6, OR coordinate a P2 patch adding the
  magnitude-band non-discrimination argument to §4.2.5 explicitly, then land P0+P1+P2
  together.

[MODERATE #1] P0 removes the defender-dialogic "we accept that scope-limit" structure
that lets readers see the fair-auditor adjudication style §8b.3 uniquely contributes.
Restore as one sentence (~30 words). Low-stakes.

[MODERATE #2] P0 drops the inline link to PRED-095's cherry-picking quote (the dome
author's own admission preserved in data/predictions.json). Easy restore as a
parenthetical (~15 words). Low-stakes.

[MINOR x2] V12 H(r)/r qualifier dropped; W004 clustering sub-clause dropped. Both
covered elsewhere in the consolidated paragraph; no action needed.

KERNEL OF TRUTH: EXP-462's diagnosis is correct and the consolidation strategy is the
right tool. Preserved content (four-layer adoption framing, codified Pass band, dual-Sq
Fail-clause analysis, ADOPTED label) covers §8b.3's genuinely unique fair-auditor-list
contribution. The honest accounting (~1,200 words recovered vs c2's ~3,000 estimate)
and explicit deferral of Hole 8 and the §5.0 sweep to follow-up EXPs is exactly the
right granularity discipline.

WHY IT DOESN'T SAVE THE CLAIM: Strategy right, execution wrong in two load-bearing
places. The EXP's anticipated_objections[0] asserts "every empirical binding is
preserved" — this does not hold for the KAPPA-squared and inject_ai_layer.py findings,
which currently appear only in the paragraph being removed.

CONFIDENCE: 0.78.

DECIDER ACTION: hold P0/P1 integration until either inline restoration (~80 words)
or coordinated P2/P3 patch lands. Once cross-references carry their assigned loads,
this is good to integrate. Tests will pass either way (no numeric claims change).

REVIEW FILE: monitor/curmudgeon/reviews/EXP-462-proposal.json (23 KB).
