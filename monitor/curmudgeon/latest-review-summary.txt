EXP-451 proposal review (curmudgeon c1, queue_id 460, 2026-06-02)
================================================================
Target: HOL-STRESSTEST c2 patches P1-P5 + 3 deferred items addressing ISS-2284/2285/2286
Class: deep-attack
Recommended action: major_rewrite (8 holes: 1 critical, 4 major, 2 moderate, 1 minor)

Headline: substantive analysis is sound; patches as authored cannot integrate cleanly.

CRITICAL — target_file is the build output, not the source-of-truth
  All 5 patches set target_file=docs/index.html. The source-of-truth for these
  strings is build-scripts/generate-html.js (lines 1574, 1841, 1608, 1618, 1669,
  1563), regenerated on every `node build.js`. apply-patches.js explicitly skips
  non-wins/sections targets. Recent sibling EXPs (455/456/457/459) correctly
  target generate-html.js. EXP-451 is the outlier.

MAJOR holes:
  - P1 sublabel_change find_text never appears in any file (git log -S confirms).
    The desired replace state is already present at docs/index.html:704 / 
    generate-html.js. The sub-patch is redundant.
  - P4 find_text 'visibly removed or suspended but excluded from its own accuracy
    denominator' matches the Silent Failures sublabel (line 714), not the
    accuracy-math-section the patch claims to target (line 497 / 
    generate-html.js:1669, which says 'removed from its predictions page or 
    suspended without counting as failures...').
  - P2 'flat-plate resonator' shorthand contradicts the careful WIN-002/038 
    framing (quarter-wave + Bessel modes + pole-floor). The 22-35 Hz numbers are 
    correct (linear radial avg of H(r) gives 22.6 Hz; area-weighted gives 35 Hz; 
    pole-only floor gives 8.78 Hz; Bessel TM01 gives 5.73 Hz). Use the review's 
    own language.
  - P3a 'set before the data' overstates temporal pre-commitment. The 
    ds-falsifiability-module was authored April 2026, after most underlying 
    measurements existed. Only Condition 2 (eclipse Aug 12, 2026) is genuinely 
    prospective. Soften to acknowledge the eclipse asymmetry.

MODERATE:
  - P3b ~1 nT discriminating window: surface with Sq-baseline context lest a 
    dome defender quote the raw '1 nT' as a 3% margin admission.
  - P5 stance reframe extends paragraph from ~50 to ~80 words; pushes scorecard 
    below the fold on mobile. Trim or move to tooltip.

MINOR:
  - P3a 'upgrade the verdict' tension with P5 'conclusion-from-evidence' framing.

Stronger arguments offered:
  1. Update meta description / og:description (generate-html.js:1497, 1500) to 
     mirror new hero — currently the SEO/social preview will dissonate with the 
     in-page hero if only the in-page text is patched.
  2. Consider Condition 2 (eclipse) as the primary worked-example target, not 
     Condition 1 (Schumann) — the eclipse is genuinely prospective and dodges 
     the chronological-asymmetry critique.
  3. Reconsider STRESSTEST-DEFER-01 (Genuine-Prediction-pending verdict 
     category) deferral. Shipping the placeholder NOW with PRED-eclipse-001 as 
     its single occupant is the strongest available architectural fix to the 
     four-zero stack optic; the post-eclipse rework either flips the entry's 
     status or folds the category back. Operator weighs two-reworks vs 10-week 
     zero-stack persistence.

Kernel of truth: the c2 critique is real and EXP-451 targets the right axes 
(P1 hero/architecture, P2 tautology, P3a/b pre-commitment, P4 denominator 
symmetry, P5 stance procedural). The 22-35 Hz physics claim in P2 IS supported 
by existing WIN-002 detail_evidence. Once target_file is re-pointed and 
find_text discipline is applied, P1 (hero only), P2 (with WIN-002 framing fix), 
P3a/P3b (with softening + context), P4 (with corrected find_text), and P5 
(with optional trim) are approve-shaped.

Path forward (operator option):
  C) Patch EXP-451 in place: re-target to generate-html.js, update find_texts 
     to template-literal-aware versions, drop P1 sublabel_change, rewrite P4 
     find_text, soften P2/P3a wording. ~30 lines of EXP-file edits.
  -or- route back to analyst Mode 2/sloppytoppy for c2.

