=== Curmudgeon Run 2026-05-16T10:43Z ===

Item processed: queue_id=363, target_id=part4, target_type=section-rewrite, class=deep-attack
Source: EXP-397 (4 patches to Section 4.6) + companion EXP-396 (count-drift row + TLDR update)
Review file: monitor/curmudgeon/reviews/SEC-4.6-EXP-397.c1.json
Verdict: SECTION HOLDS, MINOR_EDIT recommended. current_verdict_holds=true, confidence=0.85.

KEY FINDINGS (7 holes, 0 critical, 2 major, 3 moderate, 2 minor)

[MAJOR-1] Canary subsection still has false author_* claim - same defect EXP-397 fixed
  in three other places (P1, P2, P3). The paragraph "The per-WIN Author's Self-Rating
  block and this subsection together form a testable monitor ... our WIN cards fill in
  automatically" relies on author_* fields that data/wins.json does not have (0/71 WINs
  carry any author_* field). EXP-397 audited "three false prose claims"; the count was
  four. This is the most reflexively damaging finding because Section 4.6's polemic IS
  "we hold the dome to claim/data consistency" and this paragraph fails that test inside
  the section that makes the argument.

[MAJOR-2] Three-way internal inconsistency on strict_prospective_confirmed. Gap-audit
  Row 1 cites it as a live API field; Kernel-of-truth lists it among "honest
  methodology" in the API; schema-drift note (just added by EXP-397) says it was
  removed in the dome's claim_index.json restructure. EXP-397's own analyst evidence
  confirms the field is gone. Pick one terminology (claim_class==prospective_confirmed
  yields 1) and apply uniformly.

[MODERATE-3] Canary uses "10 / 9 / 1 count drift" while TLDR and Row 2 (both patched
  by EXP-396) use "10 / 12 / 1". Same section, two paragraphs apart, two different
  numbers - the exact cross-surface drift we audit the dome for.

[MODERATE-4] Row 4 of gap-audit table ("live.html footer: 67 confirmed") flagged stale
  by EXP-396's own OUT-OF-SCOPE note (live.html now reads 69 confirmed) - noted but
  not patched. Defender screenshot disproves it.

[MODERATE-5] Sub-table is not reproducible from the repo. It claims to "mirror"
  api/current/claim_index.json as of 2026-05-16 but no snapshot is captured. EXP-397
  evidence flags that the API's last_updated metadata still says 2026-04-17 even
  though the structure changed under that timestamp - dome can silently update without
  bumping metadata. Recommend data/dome-api-snapshots/ pattern.

[MINOR-6] "9 of 69+ wins total" - prefer "9 of 69 confirmed" or "9 of 71 wins."

[MINOR-7] Three "yes" cells in registry_integrity_notes column vs two notes in the
  dome API - mapping is correct (two notes cover three claim_keys via shared WIN-058
  label) but a footnote would prevent misread.

OUT-OF-SCOPE FOR DECIDER
- Combine MAJOR-1 + MAJOR-2 + MODERATE-3 into a single follow-up EXP (Canary rewrite +
  strict_prospective_confirmed cleanup + 10/12/1 number update). The patches are
  textually adjacent.
- MODERATE-4 (Row 4 stale) was self-flagged by EXP-396 already; convert into an ISS or
  fold into the same follow-up EXP.
- MODERATE-5 (sub-table reproducibility) is a meta-process improvement applying beyond
  Section 4.6 - separate proposal.

KERNEL OF TRUTH
EXP-397's decision to scope-correct rather than implement was the right call - a
40% staleness rate after 28 days on two of five proposed author_* source fields
indicates the original EXP-183 schema would have been built on a moving target. The
mirror sub-table preserves the audit signal with one update path instead of three.
The scope correction survives this finding; what failed was completeness - the same
sweep missed a fourth occurrence and didn't reconcile downstream terminology in Row 1
and the Kernel-of-truth list. Mechanical fixes; small follow-up EXP.

OTHER SIDE OF THE ARGUMENT (per user preference: think about the other side)
A charitable reader could argue the Canary paragraph reads as aspirational rather
than current-state ("forms a testable monitor" framing). I rejected this defense
because (a) the verbs are present-tense ("fill in automatically"), and (b) we don't
get to invoke charitable readings of our own prose when the audit thesis is that
uncharitable readings of the dome's prose are load-bearing. Symmetry forces the
correction. Recorded in deeper_analysis.

Queue state after this run: queue_id 363 reviewed; queue is now empty pending
decider's next run to pop. No batching - this item was class=deep-attack so gate (1)
of Step 8a failed (verification-class only). STOP after one item per default rule.
