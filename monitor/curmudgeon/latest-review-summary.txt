Curmudgeon run 2026-05-30 — queue_id 416 (part2b-count-reconciliation, EXP-437 integration)

Verdict: PARTIAL — 5 of 6 prose patches landed, build-pipeline placeholders work, but P6 + build-invariant + meta-docs omitted.

Holes found:
  1. major   — EXP-437 P6 (Overview "Two numbers, 70 and 72" explanatory <details> block) NOT integrated.
                Search across sections.json for 'Two numbers, 70 and 72' / 'ds-counts-reading-guide' returns
                zero matches. Reader sees verdict bars totaling 72 next to KPI "Claims Made: 70" with no
                inline structural-evidence framing. The dynamic verdict-bars caption (build-scripts/
                generate-html.js:519) does carry a terse one-line reconciliation, which slightly weakens
                the severity, but the closing-loop link to Part 8b.1 (claim_key annotations) is now MIA.
  2. moderate — Build-time invariant `throw new Error('coverage gap')` (EXP-437 change_2) NOT integrated.
                generate-html.js:1220 sets catalogTotal = baseWins + collisionWins with no wins.length
                comparison. Future ID shape (e.g., '057bb', '0100') silently corrupts CATALOG_TOTAL math.
                EXP-437's anticipated-objection defense — "It does, by construction... a future ID shape
                will fail the build loudly" — is now false in HEAD.
  3. minor    — _meta.placeholders documentation for {{CATALOG_TOTAL}} and {{COLLISION_COUNT}} not added.
  4. moderate — Hardcoded literal "69 confirmed" / "9 of 69" drift persists in §2b.6, Part 8b.1, the
                Taxonomy paragraph, and elsewhere. EXP-437's audit only scanned {{TOTAL_WINS}} placeholder
                occurrences (44 of them) and missed bare numerals. A dome defender citing
                "indexes only 9 of 69 confirmed claims" alongside "72 catalog rows we review" can
                rhetorically attack the document's own counting consistency. Separate root cause from
                EXP-437; recommend a child EXP for hardcoded-numeral count drift.
  5. minor    — §2b.6 audit-status paragraph enumerates 12+20+16=48 of 72 verdicts without explicitly
                noting that the remaining 24 (Std+NotDemo+Unfalsifiable) sit elsewhere. Defensible
                editorial choice but a reader running the arithmetic hits a 24-claim gap.

Verified working:
  • generateVerdictBarChart caption renders as "72 catalog entries = 70 dome-claimed wins plus 2 numbered-
    collision sub-claims" (dynamic, tracks wins.json).
  • §3.1 Verdict Tally header reads "Verdict Tally (72 catalog rows = 70 dome-claimed + 2 collision
    sub-claims)".
  • §2b.6, §8.2, §9.1 Pattern 6, §9.3 Pattern 11 all now use {{CATALOG_TOTAL}} as the denominator for
    tally-count numerators.
  • Verdict math reconciles: catalog 12+20+16+18+3+3=72; basewins 12+20+16+17+2+3=70; diff is WIN-057b
    (Std Model Explains) and WIN-058b (Not Demonstrated), matching EXP-437's stated 1+1 split.

Recommended action: minor_edit
  Three small follow-ups: (1) self-apply P6 verbatim from EXP-437.prose_patches[5]; (2) add the 3-line
  throw guard at generate-html.js line 1221; (3) child EXP for hardcoded "69" literals.
