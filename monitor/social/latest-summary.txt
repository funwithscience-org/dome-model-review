Social Analyst Summary — 2026-07-14
Generated: 2026-07-14T04:20:00Z (approx)

STATUS: ACTIVITY DETECTED (a script false-positive worth documenting, not real dome-side activity)

## HEADLINE: Wayback removal_alert fired, but it's a false positive

Today's check-wayback.js run reported live.html (a dome page) flipping from
archived:true to archived:false — per protocol this is supposed to be a
HIGH-signal "someone requested a Wayback takedown" event. I did not take this
at face value. Verification:
  1. Re-ran the script a second time — same result, not a network blip.
  2. Called the exact API request the script makes (URL-encoded) directly —
     consistently returns an empty archived_snapshots object.
  3. Called the SAME endpoint with the URL unencoded in the query string —
     consistently returns a valid snapshot at timestamp 20260709182001.
  4. Cross-checked the independent CDX Server API — confirms 3 live
     snapshots for this exact URL, all HTTP 200, most recent matching the
     same timestamp.
Conclusion: the snapshot is still there. This is a URL-encoding-sensitive
quirk in the Wayback Availability API for this one request shape, not a
real removal, and not evidence the dome author did anything. Logged per
protocol (removal_alerts must be surfaced) but should NOT be read as author
activity. Wrote up full reproduction + a suggested fix (CDX fallback before
declaring removal) in a draft for decider/tinker — check-wayback.js is
git-owned source I can't edit directly.

## GitHub Activity (dome repo john09289/predictions)
- Manual commit drought: DAY 88 (last manual: 2026-04-17)
- 20 most recent commits (last ~26h) all auto-pipeline; zero manual activity
- Stars: 0 / Forks: 0 / Watchers: 0 (unchanged)
- One "open issue"/PR is still the stale 2026-03-06 bot PR — not new
- ECLIPSE COUNTDOWN: 29 DAYS (Aug 12, 2026 — PRED-ECLIPSE-TIER3)

## Author Activity
None detected on any platform (YouTube/TikTok/X/Reddit/forums), beyond the
Wayback false-positive discussed above, which is NOT genuine author activity.

## Claim Overlap
None new. ANIMA Framework and Journal of Geocentric Cosmology re-surface for
firmament/aetheric searches, as in every prior run — different terminology,
different numbers, confirmed parallel development, not derivative.

## Our Discoverability Infrastructure
- llms.txt: unchanged (23,002B, SHA f66459ea). Verified identical across
  FUSE, fresh git clone, and live curl — no staleness this run.
- ClaimReview / meta tags / sitemap.xml / robots.txt: all verified correct
  and unchanged. Confirmed the page's own "70 vs 72" explainer correctly
  documents why our catalog (72 rows) differs from the dome's claimed count
  (70) — this is intentional, documented, and not a drift bug.
- GSC: VERIFIED (re-confirmed via live curl, 21 days live)
- Sitemap submission: STILL UNKNOWN — same open question since 2026-07-05,
  now 9 days unresolved. site:funwithscience.net dome-model-review still
  returns zero relevant results.
- Bing: still not verified (confirmed via live 404 on BingSiteAuth.xml)

## Dome Discoverability (Unchanged)
llms.txt, ai_manifest.json, sitemap.xml all byte/SHA-identical to every
baseline check since 2026-07-03 (11+ days of stasis). No new AI-steering
infrastructure.

## Action Required
HIGH: Confirm in the GSC console whether the sitemap was actually submitted.
  If yes, use URL Inspection -> Request Indexing given the narrowing
  pre-eclipse window (29 days).
MEDIUM: Bing Webmaster Tools verification (genuinely still pending).
LOW: Decider/tinker should review the check-wayback.js false-positive
  writeup in monitor/social/drafts/check-wayback-encoding-quirk-2026-07-14.md
  and consider the suggested CDX-fallback fix. Not urgent — no data lost, no
  real removal occurred — but it will cry wolf again if unaddressed.
