{
  "name": "dome-model-review",
  "version": "6.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:html": "node build.js html",
    "build:pdf": "node build.js pdf",
    "test": "node test.js",
    "publish": "node build.js all && node build.js publish"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
